1.扫描完成后自动保存没生效，还是需要手动保存。同时关于这个自动保存，有一个问题需要考虑后解决，如果再一次会话中多次触发全量扫描，怎么办，每次扫描完都自动保存吗

2.工作台页面如果矩形图渲染后，切换为排行，关系后，底层的矩形图渲染依旧会闪过，这个你可以快速截图查看，比如0.01ms截图一张，多试几次就能找到我说的问题。如图所示

![image-20260907105312519](.\img\image-20260907105312519.png)

3.舒适列表和紧凑列表这个功能删掉，通过视觉选择合适的效果放上去就行了

4.右侧排版需要优化一下，看起来太随意了，同时在扫描时，右侧的文字过于拥挤，没有自适应显示范围的大小，不同的大小的浏览器下的效果不一样。
![image-20260907105440355](.\img\image-20260907105440355.png)

5.对比页面的功能没问题了，但是如果存在增量时，他会直接把最深的目录显示出来，告诉最深目录增长了多少，希望可以和工作台的目录一样，可以手动选择深度，查看目录下的总增量，也支持进入更深的目录

6.当没有增量时也会自动把一些目录放上来，显示0增量，不够友好

7.基线和目标意义不明，同时初次打开，没有显性让用户输入哪个盘

8.对比不够友好，希望可以选择多个快照文件，做成折线图，反映趋势

9.对比和快照页面的UI还需要重新设计一下，看起来还是像demo，不像一个成熟的产品

10.主题切换，依旧不是根据鼠标位置开始扩散，同时当扩散动画的底部接触到浏览器底部时，两端会瞬间扩散完毕，需要修复

## 2026-09-13 第二轮实测反馈（已处理，留痕）

11. 对比页（空间对比）页头控件条是水平居中的，与左对齐的标题形成两条视觉中轴——不应该居中设计。**已修**：`.page-head-compare .compare-controls` 改 `justify-content:flex-start`，控件从卡片左内边距（16px）起排。
12. 存储概览卡的盘符（C:/D:）没有联动，点击后左侧视图区没反应。**已修**：chips 点击由「只切环形数据 + 回写输入框」改为 `goBrowseRoot()` 收口——切环形/图例 **并且** 左侧真正浏览该盘（与「浏览此盘」同一实现）；扫描中仅已完成盘可浏览。旧 D15 口径废止，`AGENTS.md` 第 3.12 条登记，smoke A14 同步改写。
13. 快照页缺少可视化，希望一眼看出某天有没有快照、有几个。**已加**：快照页新增「快照日历」月历热力图（`components/snapshot-calendar.js`）——格色深浅 = 当天份数（四档）、角标 = 份数、今天描边、点日期筛选下方列表、‹ › 翻月/回本月、本月统计（份数/活跃天/最密/最近）。
14. 希望记录部分使用设置，并先总结哪些能做设置、哪些适合落地。**已加**：`web/static/js/app/prefs.js` 统一登记 6 项使用偏好（默认视图/合并阈值/默认内容类型/默认排序/对比深度/隐藏零变化），单一 localStorage 文档 `pds_prefs_v1`；设置弹窗新增「使用偏好」区（可改、可一键恢复默认）。不宜落地的候选（快照保留上限、自动保存触发条件、默认对比快照、快照页筛选、数据目录与 everything_*）与理由见 `README.md`「使用偏好」节。

## 2026-09-13 第三轮实测反馈（已处理，留痕）

15. 设置界面太长。**已改**：设置弹窗分三分页「常规 / 使用偏好 / 危险区」（`#tab-*` + `#pane-*`，键盘 ←/→ 可切）。实测 1366×768：原单页内容 1045px、整个弹窗被迫滚动（标题与底部按钮一起滚走）→ 现面板固定 486px、内容区独立内滚、整窗不再滚动。一键清空按钮随「危险区」分页迁移（id `#btn-wipe-open` 不变）。
16. 快照界面排版：显示会话的地方太小。**已改**：① 找到并删除 `@media (max-height:820px)` 里 `.snapshot-list{max-height:200px}`——该选择器只命中快照页列表，在 ≤820px 高的屏幕上把列表硬压到 200px（用户实测的直接成因之一）；② 页面改左右两栏：日历收成 336px 左侧日期栏（窄栏下格子变方更紧凑），列表占满右侧与全部剩余高度；③ 会话行内动作按钮收紧凑档（`.session-root-row .btn-sm` / `.session-head .btn-sm` 36→26px）、盘符色块 28→24px。实测 1366×768：列表内滚区 153px → 456px，会话均行高 160px → 137px。
17. 对比页多快照折线：悬浮最左/最右数据点时读数（含快照大小）超出屏幕显示不全。**已修**：读数落位改为按**读数实际宽度**做边界夹取（`viz/line.js` 的 `readoutLeft()` 纯函数：右侧放不下翻左侧、两侧都放不下则夹进宿主），并修掉 `.line-readout[hidden]` / SVG `.line-guide[cursor][hidden]` 被 CSS 覆盖导致常驻的旧问题。实测 1366×768：末点读数原越出卡片 123px、超出视口 94px → 现全部落在宿主内（-26px / -55px）。
18. 快照中的「跳过快照」要处理：里面到底有没有保存，没保存不该出现，保存了没内容也不该出现。**已改**：展示层过滤口径（`snapshot-view.js`：`savedRoots/sessionSavedBytes/isMeaningfulSession/splitSessions`）——① 全盘 skipped（自动保存被谓词拒绝）不显示；② 已保存但快照为 0 字节不显示；③ 大小字段缺失仍保留（不误判）。列表头给「已隐藏 N 个空会话 + 清理」提示，清理走既有会话删除 API（仅删清单，不动快照）。实测本地 38 个会话 → 列表显示 11 个，隐藏 27 个空会话，日历统计同步只数这 11 个。⚠️ 后端契约不变（`tests/test_stage_d.py` 要求全盘 skipped 仍写会话清单，作审计轨迹）。

## 2026-09-13 第四轮实测反馈（已处理，留痕）

19. 快照日历左侧下方空闲太多。**已改**：左栏改为「日历（贴内容高）+ 最近快照（吃掉剩余高度）」纵向两卡；「最近快照」列出最近 8 次保存（时间 · 盘 · 合计大小），点击=按该天筛选列表（与点日历格同语义）。实测 1366×768：左栏 580px = 日历 303 + 最近快照 265，空白消失。
20. `C:\SDK5` 这些是不是测试环境？如果是请和真实数据隔离。**确认是测试夹具数据**（历史探针留下的 24 份会话 + 5 份 219B 快照，根名 `C:\SDK1..6`，真实目录里已无这些文件夹）。**已隔离**：① 展示层识别——`FIXTURE_ROOT_RE` 只认明确测试命名（`SDK<n>`/`fixture<n>`/`test-data` 等），**所有**保存根都命中才算夹具，任一真实盘即不判夹具；夹具会话不进列表/日历/最近快照/对比基准，列表头给「已隐藏 M 个测试会话 + 清理」；② 物理隔离——把 24 份会话清单 + 5 份夹具快照移入 `%LOCALAPPDATA%\PythonDiskScanner\_quarantine\fixtures_20260913\`（可逆，不被 `session.list_sessions()` 扫描），并清掉 ledger 里的 `C:\SDK*` 键；③ 防复发——`fixture_snapshots.mjs` 拒绝写入真实数据目录（实测 exit=2 拒绝、隔离目录正常）。
21. 对比界面 C 盘和 D 盘可以混在一起多选，希望优化；是不是可以对比不按照快照来（每次保存 CD 快照是一起保存的）。**已改**：把「盘」提为显式对比范围（页头新增「对比盘 [C:/D:]」）——基准列表只列该盘的各次保存，且**按会话分组**（组头「2026-09-13 13:25 · 该次保存含 D: C:」），多选因此**恒为同一盘的不同时间点**；`enforceSameDrive()` 兜底丢弃跨盘选中项并提示；夹具/空会话不进基准列表。顺带修掉一个冷启动竞态：直接打开对比页时会话缓存为空导致永远停在空态（实测 90s 不出结果），现会话晚到后自动补跑对比。实测：同盘多选 2 份 → 折线 2 点「已选 2 份 · 趋势已开启」；切盘后列表只剩该盘且对比自动重跑（100 行结果）。**未做**（需后端配合）：把「一次保存」直接当作一个对比对象、一次出 C:+D: 合并表——`/api/compare` 契约是单盘，多盘合并要新增后端接口或前端并发合并（含异步 202/取消/下钻口径），建议作为后续独立工作项。

对于不明确的问题，尤其时ui方面，一定要借助视觉，短间隔截图多次确认，确保知道问题所在地点