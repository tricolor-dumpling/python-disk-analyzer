# 问题核查复现与修改定位清单

> 配套《问题逐项核查报告_待修复.md》使用。本文不提供业务修复代码，只规定后续复现、证据保存和修改前的验收口径。完整阶段流程见《[问题整改阶段开发计划](./问题整改阶段开发计划.md)》。

## 一、文档与图片目录约定

本资料包解压后的目录结构必须保持如下形式；两份核查文档均从 `docs/` 目录所在位置引用图片：

```text
问题核查资料_20260902/
├─ 问题说明.md
├─ 问题逐项核查报告_待修复.md
├─ 问题核查复现与修改定位清单.md
└─ img/
   ├─ image-20260902154144366.png
   ├─ image-20260902154603439.png
   ├─ image-20260902154846994.png
   ├─ image-20260902154955413.png
   ├─ image-20260902155747740.png
   ├─ image-20260902160946498.png
   └─ image-20260902161629792.png
```

Markdown 图片统一使用正斜杠相对路径，例如：`![说明](./img/image-20260902154144366.png)`。不要改为本机绝对路径、Windows 反斜杠路径或压缩包内路径。

## 二、证据记录格式

每次复现至少记录：日期/版本、服务端口、Everything 进程与 SessionId、是否已有旧实例、输入路径、操作顺序、Network 请求及状态码、`/api/fullscan/status` 和 `/api/health` 响应、UI 最终状态、截图/录屏路径。

## 二、优先复现矩阵

| 优先级 | 问题 | 隔离条件 | 通过标准 |
|---|---|---|---|
| P0 | 1、7、13、17、20 | 两份测试快照；必要时 mock SDK；第二次扫描前清除旧状态 | 每个请求有终态；状态与缓存来源一致；停止后不伪称缓存 |
| P1 | 3、5、8、14 | 临时数据目录/测试盘；不使用真实快照目录 | 删除粒度明确；自动流程可恢复；导出可下载；关系功能需求先定稿 |
| P2 | 2、6、9、10、11、12、15、18、19、21 | Edge 浏览器，桌面/窄屏，开启/关闭 reduced motion | 逐帧或截图能区分设计动画与残留渲染；每个按钮有可见反馈 |
| P3 | 4、16 | 构造时间窗口快照；采集 30 秒日志 | 趋势空态有原因；200 日志可解释且无异常堆栈 |

## 三、建议测试脚本/夹具（后续实施）

### A. 对比与趋势

构造同根快照时间点：当前、23 小时前、25 小时前、8 天前；另造跨盘基线。验证：

- 对比页 loading 必须转成功/错误/扫描中状态；
- 较昨日只命中 24 小时内；较上周只命中 `(24h, 7d]`；
- 跨盘选择要有明确提示；
- 无快照/损坏快照不能无限 loading。

### B. 二次扫描和停止

采样状态序列：`idle → running → completed/aborted/error`，分别测试完成后重扫、保存后重扫、第一次中止后重扫、连续点击启动。重点字段：`running`、`scan_version`、`roots_done`、`result_ready`、`save_ready`、`stop_requested`、`stop_reason`。

### C. 视图与动画

数据加载后在 0ms、100ms、500ms、动画结束四个时点截图：矩形图→排行→表格→矩形图；同时记录两个容器 hidden/class、canvas 数量、动画数量。主题按钮和设置选项分别从中心/边缘点击，桌面/窄屏/Edge/reduced-motion 各跑一遍。

### D. 快照安全操作

必须使用临时 `LOCALAPPDATA`/`DSA_SNAPSHOT_DIR` 或 mock 数据目录。删除测试应覆盖：单盘、整会话、已跳过根、路径越界、文件不存在、删除失败、台账恢复、清单原子写。

### E. 导出

覆盖无结果、全量完成、多盘指定 root、空结果、未知大小、CSV BOM/转义、JSON UTF-8、Content-Disposition 和命令面板入口。原错误 JSON 只应在无可导出结果等保护场景出现。

## 四、后续代码修改定位索引

- 对比 loading/错误：`web/static/js/app/pages/compare.js:201-265`；`app.py:604-684`。
- 视图切换残留：`web/static/js/app/pages/workspace.js:377-449`；`web/static/css/style.css:122-124`。
- 指定快照删除：`app.py:525-601`；`web/static/js/app/pages/snapshots.js:124-186,456-476`。
- 趋势卡：`web/static/js/app/pages/snapshots.js:203-385`。
- 自动扫描/保存：`web/static/js/app/main.js:228-255`；`components/scan.js:137-153,339-379`。
- 扫描状态/停止：`app.py:366-391`；`components/scan.js:155-329`；`fullscan.py`。
- 缓存来源：`app.py:227-319`；`workspace.js:609-620,244-253`。
- 设置/主题：`index.html:156-216`；`components/settings.js:63-77,174-187`；`theme.js:79-118`。
- 日志：`app.py:853-937`；前端轮询 `components/scan.js:169-195`。
- 导出：`app.py:753-813`；`components/scan.js:412-418`；`components/list.js:142-184`。

## 五、测试环境注意事项

- 使用正式 `run_server()` 与直接 `Flask test_client` 分开记录；后者可能绕过 stdout/stderr UTF-8 重配置并触发 GBK emoji 问题。
- 启动前记录并确认旧端口实例，禁止把旧实例响应当作新代码验证。
- Python 测试基线当前为 266；动效 Node 测试为 25；历史报告中的 419 仅作历史参考。
- 真实 Everything 扫描、停止、保存和删除必须在隔离数据目录中执行。

## 五-B、阶段 A（R0）实测值与复现证据（2026-09-02 追加）

> 本轮为只读核查 + 新增夹具/探针/文档，未修改任何生产代码。全部证据见 `%TEMP%\fix_verif\`。

### 门禁复跑结果（主代理执行）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端单元 | `unittest discover -s tests -t . -v` | **266/266 OK（skipped=1）**（1.659s） |
| 前端纯函数 | `node --test-isolation=none --test "scripts/dev/*.test.mjs"` | **25/25**（128.8ms） |
| 语法卫生 | `py_compile -W error::SyntaxWarning` 全量 16 文件 | **exit=0**；P-9 现状：cli.py 的 `exports\\` 非法转义在 Python 3.11.8 下**不告警**（3.14 告警面差异，P-9 修复前按手册如实记录，修后须零告警） |
| P13 基线 | `test_budget` 连续 50 轮（-W error::ResourceWarning） | **11/50 失败**：rounds 4/7/10/14/31/32/34/35/44/48/50，均为 `test_concurrent_saves_serialize_accounting`；round 31 出现**决定性直接证据** `PermissionError: [Errno 13] Permission denied: '...\\.snapshot.lock'`（锁文件 unlink 被拒 = P-1 根因的 FILE_SHARE_DELETE 缺失实证）。复现率 22%，P13 挂账有效 |

### 版本基线冻结（实际环境 vs 手册标注）

| 项 | 手册基线 | 实际检出 | 处置 |
|---|---|---|---|
| Python | 3.14.3 | **3.11.8**（.venv，`pyvenv.cfg` home=D:\dev\Python） | 记录为当前有效基线；不据此改动生产代码 |
| Flask | 3.1.3 | **3.0.2**（requirements `flask>=3.0.2`） | 同上 |
| Node | 25 | **v24.19.0** | 同上 |
| unittest | 266 | **266**（一致，skipped=1） | 一致 |
| node 测试 | 25 | **25**（一致） | 一致 |
| 服务端口 | 5999 旧实例（G9） | **当前无 5999 监听**（netstat 无 LISTENING；测试中 5999 仅 test_shutdown.py 模拟） | 记录：未复用旧实例状态做任何结论 |
| Everything | 运行中 | **PID 5096，Session 0**（已记录，未触碰） | 记录 |

### 真机复现（桩态确定性探针，浏览器渲染层）

探针 `scripts/dev/u50_repro_probe.mjs`（桩态 fetch，零写操作）在隔离数据目录 Flask（`%TEMP%` 重定向）上运行，Playwright chromium 151 headless。关键帧每 100ms，判读交 GPT-5.6 Luna（opentoken `gpt-5.6-luna`，见流程 §经 workflow agent provider/model 通道）。

**2-6 扫描环形动画回跳 —— 已复现（高置信）**
- 客观测量（getComputedStyle transform 角度，100ms 采样 × 3s）：
  `-90→-10°` 匀速（0-600ms）→ t=700ms **-87.5°**（瞬跳 -76°）；此后每 1200ms 重复一次（t=1300→1400、1900→2000、2600→2700 各 -76° 瞬跳）。
- 与手册候选根因 **完全吻合**：`donut-spin` keyframes 只定义 `to{transform:rotate(360deg)}` 而元素带 `rotate(-90)` 属性 → 每 1.2s 实际转 450°、循环结束回跳 90°（-76° = 90° 回跳减采样窗内继续前进的 ~14°）。
- Luna 目视（scan-ring-006/007、026/027 特写）：006→007 弧从 3 点明显移回 4 点方向（支持）；013→014 / 019→020 帧间弧位接近（部分支持）。综合判定已复现。

**2-11 主题扩散铺满 —— 已复现（高置信）**
- Luna 目视（theme-diffuse-000…006）：深色遮罩 000→003 逐帧扩大，约 003 触及视口底部，004 起页面整体深色铺满、圆形边界消失 → 「触底后迅速铺满」现象成立。
- 触发路径：顶栏主题按钮（vtSupported=true，450ms View Transitions clip-path 扩散）。

**2-2 视图残留 —— 未复现（当前桩态复现下）**
- 探针终态断言：终态 table 时 `#treemap-wrap` hidden=true，`residualDetected=false`；Luna 目视 view-000/100/500/800 均稳定表格，未见矩形图/排行残留（仅 view-000 过渡帧有矩形色块 = 120ms 交叉淡化设计行为）。
- 结论：手册贰章「需先复现」项 2-2，本轮桩态确定性复现**未能复现**；保留「真机高频切换/慢速设备」残余风险，阶段 E 以真机录屏再确认。**不据此关闭问题**（红线：unittest/确定性复现不绿≠关闭依据）。

**2-9 停止反馈时间线 —— 已复现（桩态）**
- 探针：`click-stop → 90ms 状态文案「已停止：已完成部分可浏览，可保存快照」+ toast`。当前实现停止反馈 <100ms 可见（POST 后轮询收敛 + 状态文案），但实测基于桩态即时返回；真机 Everything 停止延迟（G8 分钟级 busy）仍需阶段 B 真机验证。

**2-14 导出错误路径 —— 已复现（桩态）**
- 无全量结果时 `GET /api/export?format=csv&root=D:\` → popup 显示 `{"error":"暂无可导出的全量扫描结果，请先完成全量扫描","ok":false}`（= 手册记载的原始问题 14 错误 JSON）。
- 就绪态（exportReady=true）→ 下载正常（filename=report.csv / content-type text/csv; charset=utf-8-sig）。**入口与错误保护存在，导出「不可用感」来自无结果前置；阶段 B 按导出四态回归。**

### 夹具验证结果

`fixture_snapshots.mjs` 生成的 9 份 `.snap.gz` + 5 份 session 清单经项目自身模块验证：
- `snapshots.load_snapshot` 全部读通（CRC/文件名/机器标识校验通过）；
- `compare.compare_snapshots`（23h C 基线 vs current）：total_base=9000 / total_cur=10000 / delta=+1000 / rows=4；
- `session.list_sessions` 枚举 5 会话正常。
- 接入 `/api/snapshots`/`/api/compare`：以 `LOCALAPPDATA`/`DSA_SNAPSHOT_DIR` 重定向（W3.5 口径），阶段 C 实现期收口。

### 识图子代理（GPT-5.6 Luna）判读结论汇总

| 图/帧 | 结论 |
|---|---|
| 7 张既有截图（问题 1/3/4/6/10/14/15/17/19/21） | 全部可判读，与手册贰章现象描述一致（骨架屏拥挤/无删除按钮/双空态/弧 150°-210°/2×2 按钮/不对齐/状态栏文案） |
| u50 scan-ring 特写 | 006→007、026→027 弧位跳变（佐证 2-6 回跳） |
| u50 theme-diffuse | 扩散逐帧扩大、003 触底、004 铺满（2-11 已复现） |
| u50 view | 稳定表格、无残留（2-2 未复现） |

> ⚠️ 判读通道注记：DSH 普通 subagent 工具无模型参数且继承默认模型（无图像能力）；识图必须走 workflow `agent(..., {provider:"opentoken", model:"gpt-5.6-luna"})` 通道；判读需给绝对路径。

## 五-C、阶段 B（R1）实现与验收证据（2026-09-04 追加）

> 本轮为实现阶段：B-1…B-20 全部落地（阶段 B 专属纪律遵守：B-13/B-14 与 B-15 同批、B-7 与 B-1 同批、ETA 标「估算」、source 新字段不后门、W3.5 隔离数据目录、日志不静默、环境偏差如实记录）。分支 `stage-b`，变更集 1–7 独立可回退。

### 门禁结果（连续 3 轮全绿）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端单元 | `unittest discover -s tests -t .` ×3 | **289/289 OK**（新增 `tests/test_stage_b.py` 23 用例；P13 轮 1 复现一次按纪律复跑即绿，轮 2/3 全绿） |
| ResourceWarning | `-W error::ResourceWarning unittest discover` ×3 | **289/289 OK**（看门狗用例日志为按需输出） |
| 前端纯函数 | `node --test-isolation=none --test "scripts/dev/*.test.mjs"` | **25/25** |
| 语法卫生 | `py_compile` 16 文件 + test_stage_b | **exit=0**（cli.py `exports\\` 转义 3.11 不告警，P-9 面差异如实记录） |
| smoke | `python -m http.server 8771 -d .` + `node scripts/dev/u22_smoke_probe.mjs v2` | **21/21**（A16/A17 适配 B-17 轮询节奏；A8 busy 不降级回归） |
| 轮询频率 | `u51_poll_probe` | **PASS**：空闲 30s status 5 次（≤6）、运行中 30s 14 次（≤15） |
| 视觉探针 | `u52_viewport_probe`（9 截图）/ `u53_visual_probe`（7 场景） | console errors **0**；u53 客观采样全符合（停止文案/徽章三态/导出 toast） |
| 复现回归 | `u50_repro_probe --steps all` | 五段（view/theme/scan/stop/export）完成，stop 段 `click→90ms 反馈`，export 段适配 B-15（无结果按钮禁用 + Blob 下载） |

### 状态机四序列（test_stage_b + 集成链路，fake SDK）

| 序列 | 证据 |
|---|---|
| 1. 首次扫描 | start→scanning→idle，`result_ready=True save_ready=True`（test_stage_b test_first_scan_phase_sequence） |
| 2. 完成后重扫 | `scan_version` 递增且收敛（test_second_scan_version_increments） |
| 3. 停止后重扫 | stop_ack_at 记录、start 复位；中止部分根导出 X-Partial=true（集成 ⑧） |
| 4. 保存后重扫 | save 后 `save_ready=False`（已消费）→ 可重扫（集成 ④） |
| 锁占用 | 锁被占 → start 返回 `{started:true, queued:true, phase:"queued"}`（test_queued_when_lock_held） |
| 看门狗 | 打桩阈值 → CANCEL_EVENT 置位 + error="SDK 无响应" + 已完成根保留（test_watchdog_stalls_then_cancels） |

### source 三场景一致性（Luna 判读 + DOM 采样）

| 场景 | 响应 source | 前端徽章 | 判定 |
|---|---|---|---|
| 索引命中 | `index` + source_at | 「来自全量扫描索引（完成于 …）」 | PASS（Luna 目视 + DOM badgeText 采样） |
| 真实 SDK 直扫 | `sdk` + source_at | 徽章隐藏（class hidden） | PASS（DOM 采样 badgeHidden=true；Luna 复核静态图无徽章即正确语义） |
| 全量扫描中 | `scanning` + scanning:true | 「扫描中」 | PASS |

### 视觉验收（gpt-5.6-luna 三轮判读，workflow agent provider=opentoken model=gpt-5.6-luna）

轮 1：1366/1920 常规视口发现引导弹窗遮挡扫描卡（探针缺陷）→ 修复 u52 预置 onboarding 关闭。轮 2：1366/1920 六张全 PASS（三按钮同行无重叠、进度行整合文案、queued 无「0/2」误导、完成态无概览残留）。轮 3：357×651 窄屏滚动聚焦后三张全 PASS（卡内无重叠/溢出）。B-9 停止反馈（「正在停止…（等待扫描引擎响应）」+ 按钮禁用激活）、B-14 徽章两场景、B-15/B-16 导出三 toast 均 PASS。**结论：B-12 扫描卡三视口布局 + B-9/B-14/B-15/B-16 视觉语义全部通过 Luna 判读 + 主代理复核。**

截图证据：`%TEMP%\u52_viewport\scan-{running,queued,done}-{1366x768,1920x1080,357x651}.png` ×9、`%TEMP%\u53_visual\{stopping,badge-index,badge-sdk,badge-scanning,export-ok,export-404,export-409}-1366x768.png` ×7。

### 集成链路（隔离目录，W3.5：LOCALAPPDATA 重定向）

`首次扫描→browse(source=index)→保存(session+1)→save_ready 消费→对比异步(202→轮询 done)→导出 CSV(BOM+Content-Disposition)→撤销(session=0)→中止部分根导出(X-Partial=true)` **8 步全通**。

### 环境与遗留

- 环境基线不变（Python 3.11.8 / Flask 3.0.2 / Node v24.19.0 / Everything PID 5096 Session 0）；隔离服务端口 5000（已核对 PID），静态 8771；Everything 进程未触碰。
- 真机 Everything 分钟级 busy（G8）按「请求发起态」口径记录——二次扫描/停止时间线以桩态确定性验证（test_stage_b + u50 stop 段），真机全量 API 以隔离 test_client + 夹具验证。
- P13（snapshots.py 锁竞态）挂账不变：**禁碰纪律**，未做任何 snapshots.py 改动；本轮 3 轮门禁中轮 1 复现一次（复跑即绿），按「复现允许复跑」纪律记录。
- P13 专项复跑（2026-09-04，`-W error::ResourceWarning unittest tests.test_budget`）：`test_concurrent_saves_serialize_accounting` **复现失败**（AssertionError: 1 != 8，重试窗耗尽）——与阶段 A 11/50 基线一致，挂账有效（P13-FIX 为独立变更集，本阶段不触碰）。
- 2-6 donut 动画回跳：u50 scan 段仍测得 450° 循环（-76° 回跳），属阶段 E（E-1）任务，阶段 B 不触碰（动画红线 P7 与本阶段无关）。

## 五-D、阶段 C（R2）实现与验收证据（2026-09-05 追加）

> 本轮为实现阶段：C-1…C-8 全部落地（阶段 C 专属纪律遵守：删除 API additive + 边界校验复用 api_save_undo 模式、破坏性流程隔离目录、趋势空态带原因不误导、视图互斥终态防 2-2 残留、Git 版本管理 G-1…G-6 全程执行）。分支 `stage-c`，变更集 1–9 独立可回退（c9c0ef3→0afb45e），已推送 origin/stage-c。

### 门禁结果（阶段 C 专属 + 既有回归）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端单元 | `unittest discover -s tests -t .` | **300/300 OK**（阶段B 289 + `tests/test_stage_c.py` 11 用例：删除单盘/整会话/越界/幂等/409/台账/逐目标清单） |
| 前端纯函数 | `node --test-isolation=none --test "scripts/dev/*.test.mjs"` | **35/35**（25 + trend-window.test.mjs 10 增量：23h/24h 整/25h/7d 整/8d/同刻/跨盘/损坏快照窗口边界） |
| smoke | `python -m http.server 8771 -d .` + `u22_smoke_probe v2` | **21/21**（C-7 新增按钮/容器未破坏断言面） |
| 复现回归 | `u50_repro_probe`（五段） | view/theme/scan/stop/export 全部完成无 console 错误（scan 段 4 跳仍为已知 2-6，阶段 E 任务） |
| 阶段 C 探针 | `u55_trend_probe` / `u56_relate_probe` / `u57_list_diff_probe` | **u55 11/11**（趋势空态原因行/两卡口径 tooltip/差值卡/两地同基线一致性/无全量提示）· **u56 16/16**（关系树互斥终态/懒展开 1 次请求/虚拟化/键盘/窄屏/首行不被遮挡）· **u57 21/21**（排行 vs 表格 4 vs 5 列/cozy 完整 vs compact 仅数值/虚拟行高 36/26） |

### C-1 视图唯一性（2-2）结论：**未复现（登记不关闭）**

- u50 §1 增强终态采样：任意 8 连击（三视图来回）后终态——全局可见 canvas 数（getClientRects+computed display 跨容器判定）+ 非活动容器 computed display + 0/100/500/800ms 关键帧 + 切回 treemap 稳定态补采。
- 实测（桩态确定性）：终态 table 激活 `visibleCanvas=0` / `treemapDisplay=none` → `uniqueOk=True`、`residualDetected=False`；treemapStable `tableDisplay=none` / `visibleCanvas=2`（双层画布渲染器 staticCanvas+fxCanvas 设计，treemap.js:218-227）→ `treemapUniqueOk=True`；console 0。
- 按手册「先复现后实现」纪律：**终态断言全过 → 登记「未复现」不关闭问题**（阶段 A 桩态未复现、阶段 C 探针复核仍未复现；2-2 待阶段 E 真机复核）。

### 视觉验收（gpt-5.6-luna 判读，workflow agent provider=opentoken model=gpt-5.6-luna）

| 组 | 截图 | Luna 结论 | 主代理复核 |
|---|---|---|---|
| C-1 三视图连点 | u50 view-000/100/500/800.png ×4 | **全 PASS**（交叉淡化期叠加正常、500/800 收敛单一容器、无残留） | 一致：DOM 采样 visibleCanvas/display 佐证 |
| C-7 关系树 | u56 relate 四张 | **终局 PASS**（修复后 data\\/deep\\ 可见、互斥终态、虚拟化、窄屏无破版） | 一致：elementFromPoint 命中测试 + 像素统计佐证 |
| C-8 列表差异 | u57 五张 | **终局 PASS**（排行/表格列结构差异、cozy 完整 vs compact 仅数值、虚拟 26px 整齐） | 一致：u57 DOM 断言 + 行高实测佐证 |

**Luna 判读抓出的真实缺陷（重要）**：①`.list-footer` 的 `display:flex` 覆盖 `[hidden]` 属性 UA 默认 → relate 激活时页脚 sticky+z2 悬浮遮挡树首行（elementFromPoint 首行返回 list-footer、首行像素 dark=0）→ 补 `.list-footer[hidden]{display:none!important}`；②`.relate-tree` 同样覆盖 `[hidden]` → 非 relate 模式下透明 flex 容器以 absolute inset:0 铺满 table-wrap，**遮挡整个排行/表格视图**（elementFromPoint 表格行位置命中 relate-tree、截图全空白、Luna 判空）→ 补 `.relate-tree[hidden]{display:none!important}`。两处均经红色注入实验（RED 0%→16.06%）+ 命中测试双证修复。

### 删除闭环集成（隔离目录，W3.5：LOCALAPPDATA 重定向）

test_stage_c.py 11 用例覆盖：单盘删除（文件删除 + session roots 条目移除 + 其他盘保留）/ 整会话删除（无剩余条目删 session 文件）/ 已跳过根不动 / 越界路径 400 / 文件缺失幂等 `{deleted:true,already:true}` / 删除失败成败清单 / 台账 ledger_backup 恢复一致 / 并发 / 幂等。前端链：删除按钮 → confirmDialog → POST → 清 trendCache 涉删基线条目 → refreshSnapshots 已删项不出现（u20 纪律保持）。

### 环境与遗留

- 环境基线不变（Python 3.11.8 / Flask 3.0.2 / Node v24.19.0）；隔离服务端口 5000（PID 21188）/ 静态 8771（PID 28952），收尾时已核对清理；Everything 进程未触碰。
- C-1 未复现登记（2-2 待阶段 E 真机复核）；P13 挂账不变（禁碰 snapshots.py）；真机 Everything 分钟级 busy（G8）按「请求发起态」口径记录。
- 趋势窗口边界：trend-window.test.mjs 35 用例全过（23h 命中较昨日 / 24h 整边界 / 25h 较上周 / 7d 整边界 / 8d 双窗口外空态带原因 / 同刻仅一份空态 / 跨盘无同盘基线空态 / 无快照原因文案 / 多盘确定性取首个合格盘）。

## 五-E、阶段 D（R3）实现与验收证据（2026-09-05 追加）

> 本轮为实现阶段：D-1…D-3 全部落地（阶段 D 专属纪律遵守：自动扫描恰一次 Network 硬断言、一次性触发保护键、保存失败/停止恢复入口保留、ETA 标注「估算」且不闪跳、三视口基线截图先行 + Luna 判读、u20 网络序列纪律、console 0、Git G-1…G-6 全程执行）。分支 `stage-d`，变更集 1–4 独立可回退（a9c7610→2a6c49e），已推送 origin/stage-d。

### 门禁结果（阶段 D 专属 + 既有回归）

| 门禁 | 命令 | 结果 |
|---|---|---|
| 后端单元 | `unittest discover -s tests -t .` | **307/307 OK**（阶段C 300 + `tests/test_stage_d.py` 7 用例：自动保存恰一次/session.auto 标记/save_ready 消费/谓词拒绝 skipped/全失败 409 failed 清单可见/无结果 409/扫描中 409/部分失败逐盘清单/created_at 数据事实） |
| ResourceWarning | `-W error::ResourceWarning unittest discover` | **307 OK**（P13 轮 1/2 复现按纪律复跑即绿，轮 3 全绿；P13 专项 50 轮 18/50 失败挂账不变） |
| 前端纯函数 | `node --test-isolation=none --test "scripts/dev/*.test.mjs"` | **38/38**（35 + motion-core ETA 纯函数 3 用例：estimateRemainingSec 均速口径/边界 null/etaShouldUpdate >10% 阈值与 soon 档跨越） |
| 语法卫生 | `py_compile` 17 文件（含 test_stage_d） | **exit=0** |
| smoke | `python -m http.server 8771 -d .` + `u22_smoke_probe v2` | **21/21**（smoke 预置 pds_auto_started_v1 隔离自动扫描，断言面零破坏） |
| 复现回归 | `u50_repro_probe`（五段） | view/theme/scan/stop/export 全部完成无 console 错误 |
| 阶段 B/C 回归 | `u51_poll_probe` / `u52_viewport_probe` / `u53_visual_probe` / `u55_trend_probe` / `u56_relate_probe` / `u57_list_diff_probe` | **全绿**：u51 空闲 5≤6 运行 14≤15；u52/u53/u55/u56/u57 断言全过、console 0 |
| 阶段 D 探针 | `u58_auto_scan_probe` / `u59_progress_probe` / `u60_scan_layout_probe` | **u58 11/11**（冷启动恰一次/刷新不重复/扫描中刷新恢复运行态/未就绪不发起/当日会话不发起/自动保存恰一次/保存失败恢复入口）· **u59 11/11**（四要素/ETA 估算标注/不闪跳/queued 无 0/2 误导/reduced 直显）· **u60**（三视口×三态 9 张截图 rect 采样无重叠 console 0） |

### D-1 自动扫描恰一次（u58 + 真机）

| 场景 | 断言 | 结果 |
|---|---|---|
| 冷启动（无会话、无保护键、health ready） | `/api/fullscan/start` POST 计数 = 1 + 保护键写入 | PASS（startCount=1） |
| 刷新（sessionStorage 保持） | 不重复触发（start=0，恢复轮询态） | PASS |
| 扫描中刷新 | 恢复运行态（progress.running）+ 不重发 | PASS |
| Everything 未就绪 | 门控不进入 ready 分支 → 不自动发起 | PASS |
| 当日已有快照会话 | 不自动发起（防同日重复全扫） | PASS |
| 自动保存 | `/api/save` POST = 1（完成边沿恰一次） | PASS |
| 保存失败 | 错误 toast 可见 + 手动「立即保存」入口恢复 | PASS |
| 真机（隔离目录 + 真实 SDK，1366×768） | start POST=1 / browse=0 / 409=0 / console=0 | PASS |

### D-1 冷启动竞态修复（真机发现并修复的真实缺陷）

- **现象**：真机冷启动自动扫描与启动浏览同 tick 竞争 SDK 锁 → 5×`POST /api/browse` 409 console 噪声（violate console 0 验收）；扫描 queued/首盘期 live-refresh 反复 poke browse 撞 409。
- **修复（变更集 4）**：①topbar.js `evaluateEnvGate` ready 分支——自动扫描可评估时先派发扫描（拿锁），启动浏览延后到扫描完成（`pds:browse-after-scan` 一次性监听，browse 命中全量索引零 409 零重复 SDK 直扫）；不可评估时保持既有立即浏览（u20 网络时序不变）；②scan.js 完成边沿（result_ready）派发 `pds:browse-after-scan`；③workspace.js `doLiveRefresh` 前置守卫——`lastBrowseData` 为空（冷启动自动扫描延后浏览期无数据可生长）→ 跳过实时刷新；重新扫描（已有数据）路径 L3-2 实时生长保持。
- **复验**：真机冷启动 browse=0 / 409=0 / console=0；桩态全链 start=1→save=1→browse=1 console=0；u58 11/11 保持。

### D-2 自动保存闭环（test_stage_d 7 用例 + u58）

- `autoSaveSetting` 前端默认 true（缺键视为 ON，设置弹窗同步；显式 false 保持 OFF——既有手动路径不破坏）。
- 自动保存失败不吞错误：`saveSnapshot` 返回成功/失败（additive），`maybePromptSave` 失败时恢复「立即保存/暂不保存」提示条（修订清单 #5 恢复入口纪律）。
- 后端契约（test_stage_d）：自动保存会话 `auto=true` + save_ready 消费恰一次；谓词拒绝 → 逐盘 skipped（skip_reason）；全失败 → 409「本次没有生成任何快照」错误可见；部分失败 → failed 清单逐盘可见（P12·W2.11 语义保持）；扫描中/无结果 409 前置保护。

### D-3 按钮迁移 + 进度四要素 + ETA 不闪跳（u59/u60 + Luna）

| 项 | 实现 | 验收 |
|---|---|---|
| 按钮语义 | 扫描卡/顶栏按钮：空闲+有结果 →「重新扫描」；失败 →「重试扫描」；空闲无结果 →「开始全量扫描/开始扫描」；「保存快照」「创建快照」保留 | u60 label 采样：done 态=重新扫描、running/queued=开始全量扫描 |
| 进度四要素 | 状态行「总进度 % · 已完成 x/y 盘 · 当前盘」+ 行尾「已用时」+「预计剩余 ~T（估算）」 | u59 11/11（含 reduced-motion 直显） |
| ETA 不闪跳 | motion-core 纯函数 `estimateRemainingSec`（已完成根均耗时×剩余根数，向上取整）与 `etaShouldUpdate`（变化 >10% 才更新；首采样/「即将完成」阈值跨越必更新）；scan.js 按扫描代次隔离缓存 | motion-core.test +3 用例 38/38；u59 两次采样 <10% 文案不变 |
| 三视口布局 | 1366×768 / 1920×1080 / 357×651 × running/queued/done | u60 9 张截图 rect 采样无重叠（父子嵌套排除）+ console 0；Luna 判读 9 张全 PASS |

### 视觉验收（gpt-5.6-luna 判读，workflow agent provider=opentoken model=gpt-5.6-luna）

| 组 | 截图 | Luna 结论 | 主代理复核 |
|---|---|---|---|
| 设计基线（改造前） | u52 9 张（三视口×三态） | **全 PASS**（无重叠/溢出） | 一致：rect 采样佐证 |
| D-3b 三视口三态 | u60 9 张（含「重新扫描」标签） | **全 PASS**（按钮/进度/状态行/chips/保存提示均卡内无重叠；窄屏仅正常换行） | 一致：rect 采样 + 父子嵌套排除 |
| D-3b 进度四要素 | u59 running 四要素 + reduced 2 张 | **全 PASS**（总进度 70%/已完成 1/2 盘/当前盘/已用时/预计剩余「即将完成」直显） | 一致：DOM textContent 采样佐证 |

### 环境与遗留

- 环境基线不变（Python 3.11.8 / Flask 3.0.2 / Node v24.19.0）；隔离服务端口 5000 / 静态 8771，收尾时已核对清理；Everything 进程未触碰。
- P13（snapshots.py 锁竞态）挂账不变：**禁碰纪律**，未做任何 snapshots.py 改动；本阶段门禁轮 1/2 复现（复跑即绿）+ 专项 50 轮 18/50 失败（`test_concurrent_saves_serialize_accounting` 1!=8），与阶段 A/B/C 基线一致，挂账有效。
- 自动扫描的「当日快照会话」判定为前端聚合口径（session.created_at / roots[].notice.date）；后端零新增字段（13 接口语义零变更）。
- 2-6 donut 动画回跳仍属阶段 E（E-1）任务，本阶段不触碰。

## 六、修改前禁止事项

1. 不要把“已有 API/按钮”直接当成“用户问题已解决”，必须完成对应场景回归。
2. 不要在真实快照目录直接测试删除/清空/撤销。
3. 不要仅通过删除 loading 文案掩盖对比、扫描或网络未收敛。
4. 不要通过字段缺失推断缓存来源；来源应由后端明确返回。
5. 不要为满足自动扫描而删除所有手动恢复入口，除非失败与停止流程已有可用替代方案。
