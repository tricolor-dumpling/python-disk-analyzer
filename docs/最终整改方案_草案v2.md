# 下一迭代整改方案（草案 v2）

> 状态：**修订版**（v1 保留于 `docs/最终整改方案_草案v1.md` 供对照）。本版解决红队评审 RT-01～RT-07 全部阻塞项，RT-N01～N10 逐条给出归属；五项待拍板事项按决策者初步意向移入 §九「已决事项」，以最终定稿文档为准。
> 输入：第一轮输入汇总、架构评审报告_20260824、开发方案.md、test-report/ 三份报告、`docs/整改方案_红队评审.md`。
> 编制：架构师 / Tech Lead · 2026-08-24。编号体系沿用：DEF-xxx/N-x/D-x/P0-x/§三A…/§四#n/RT-xx。

---

## 一、跨角色冲突裁决（11 项，含红队修订）

| # | 冲突 | 裁决 | 理由 | 状态 |
|---|------|------|------|------|
| 1 | K4 白名单 vs SEC-1 | 白名单剔除 everything_* 三键，仅留 auto_save/last_roots(/theme)；API 写入三键一律 400 | 配置投毒链封堵成本为零 | 已决(TL) |
| 2 | 日配额语义(N1) | **接线**：auto 超限=硬门槛 return None（复用跳过契约）；explicit 超限=软警告继续写，回传走 **additive 通道（模块级 last_save_notice，读后即清），save_snapshot 返回 Path\|None 契约冻结**（RT-07）；同步改 snapshots.py:515 docstring、README:233/256、app.py:277；拒绝提示 info/warn（N-6） | 用户主动动作不宜硬拦；三方文本一次对齐 | 已决(签名意向) |
| 3 | 一键启动批次 | P0 只做冷启动后台自动拉起（bind 前 daemon 线程，不阻塞起服）＋引导态「重试环境检测」按钮；显式一键启动按钮拆批次二（W2.0） | 冷启动自动拉起覆盖绝大多数未就绪场景，免前端 20s 轮询状态机 | 已决(签名意向) |
| 4 | Web 导出归属 | 提前至批次二 W2.7（先抽流式参数再复用 cli.export_report_*），Non-goals 划掉该项 | 边际成本低、补齐行动闭环最后一公里（G-1=P1） | 已决(签名意向) |
| 5 | DEF-018 多盘原子性 | 维持存疑不排期；B-2 undo 回滚台账必做（W2.11），B-1 以逐盘 try＋响应列出各盘成败缓解，不做全事务改造 | B-2 实证且 S 级；全事务 L 级不值 | 已决(TL) |
| 6 | 空目录口径 | 改文档不改行为（W3.3），理由注明 MAX_ROWS=50 万上限；开关版 backlog | 三方一致；行为变更收益低风险高 | 已决(TL) |
| 7 | D2 机器校验 | 改实现：生产对比路径接线 machine_guid 强校验＋逃生口；**逃生口三端对齐（RT-N01）：CLI `--allow-other-machine` 参数／Web 确认弹窗二次提交 allow 参数／TUI 红字确认键** | 跨机 diff 数字误导；逃生口保住合法场景 | 已决(签名意向) |
| 8 | dispatcher 去留 | 删除（六项联动清单见 v1，维持不变：main.py:180/README×6/spec 注释/py_compile/grep 门禁） | 零产出＋spec 地雷；统一锁路线(a)后存在价值为负 | 已决(TL) |
| 9 | **禁改约束(最高优先)** | 解除「禁止修改 scan/compare/snapshots 核心行为」，换受控修改护栏（见下） | 四个 P0/P1 修复必须触碰这三文件 | 已决(签名意向) |
| 10 | N3 health 吞异常 | 归批次一 W1.3 同工作流：health 区分 degraded 类型与真未就绪 | 引导态正确指引的前提 | 已决(TL) |
| 11 | TUI DEF-003 归属 | 进批次一发版前必修；发布门槛以 PM 四项 P0 验收为准 | 崩溃即信任灾难且修法 S 级 | 已决(TL) |

### 冲突9 受控修改护栏（v2 增补实施细则）

1. **兼容性回归先行**：动手前合入三类回归——快照格式 golden 夹具／API 字段契约断言／聚合口径夹具（哨兵值＋合计＋大小写混合根行，RT-N02）。
2. **两条红线不变**：快照格式 format v1 不变；已有 API 字段语义只增不改不删。
3. **零破坏传出通道实施细则**（RT-04/RT-07 固化，禁止临时改签名）：扫描层统计经 sizes dict 附加属性 `unknown_size_count` 传出，`(sizes, contents)` 二元组解包契约冻结；保存层警告经模块级 `last_save_notice`（读后即清）传出，save_snapshot 返回 Path|None 契约冻结。
4. **变更面收敛**：scan.py 仅限尺寸读取收口；compare.py 仅限合计助手下沉与 leaf_only 参数；snapshots.py 仅限配额接线、陈旧锁、docstring、load 层 legacy 标记；每项独立提交可回滚。
5. **合并闸门**：每项合并前 unittest 全绿（基数见 §五.1）；护栏变红即冻结该文件变更。

---

## 二、迭代目标与范围

**目标句**（不变）：让每个新用户在本版本上跑通「就绪 → 可信数字 → 定位大头 → 打开处理 → 存快照 → 次日一键对比」完整闭环，全程看不到一个错误或看不懂的数字。

**In**：批次一＝发布门槛 P0（哨兵值、合计口径、Everything 引导包、行动闭环最小集、TUI 崩溃修复、护栏回归先行）；批次二＝稳定与闭环十四项；批次三＝打磨还账五项（明细见 §三）。

**Out（对齐 Non-goals，注明去向）**

| 不做项 | 去向 |
|---|---|
| 删除/移动文件 | 永久 Non-goal |
| MFT 备用引擎；定时快照/自启/托盘；设置扩展(G-4 等) | backlog |
| dispatcher 启用 | 本轮删除（冲突8），不再回来 |
| vitest/jsdom 测试框架建设 | 零依赖冒烟页＋node --test 替代 |
| 空目录口径变更 | 只改文档（W3.3），开关版 backlog |
| API/快照格式变更 | 红线不变 |
| G-6 单根重扫、DEF-016 盘内刻度、DEF-031 余项、R-1 字符串共享 | backlog P13 候选 |
| **PM 指标集与 JSONL 事件日志**（RT-N07） | backlog P13 候选；红线「天文数字输出率=0」仍由 W1.1 验收承载 |

台账覆盖度（RT-03 修订）：31 项中 **28 项本迭代落地**（含 DEF-020 以改文档落地）、DEF-018 关闭（存疑）、**DEF-030 关闭缺陷形态转为 web 契约测试编码规约（对象 tests/test_web.py 在当前树不存在）**、DEF-016 与 DEF-031 余项入 backlog；D1-D5、N1-N3 全部落地。

---

## 三、分批计划

> 量：S≤0.5 人日、M≈0.5-1.5、L>1.5。★ 标记为 v2 因红队意见修改的条目。

### 批次一 · 发布门槛 P0（≈5.5-6 人日）

| ID | 对应 | 工作项 | 触碰文件 | 修法要点 | 量 | 依赖 |
|----|------|--------|----------|----------|----|----|
| W1.0 | 冲突9、RT-03 | ★护栏回归先行＋基线核定（硬性步骤） | tests/*、README(D3)、docs/开发方案.md(措辞) | 交付物：①三类护栏回归合入；②**记录 `python -m unittest discover` 实际用例数作为新基线，回填 README D3**；③《开发方案》「419 全绿底线」措辞改为「unittest 全绿底线（基数以 W1.0 记录为准）」；④web 契约测试编码规约随首批测试生效：一律 with-resp/close＋discover 加 `-W error::ResourceWarning` 卫生门禁 | S | 无 |
| W1.1 | DEF-001、N3、RT-04、RT-05 | ★哨兵值过滤收口＋未知数传出＋legacy 标记 | scan.py(:232/:455)、read_result_size、snapshots.py(load 层)、tests/test_scan.py | 封装 read_result_size（BOOL FALSE→None、2^64-1 恒滤、上限=min(卷容量,16TB)，取卷失败回退 16TB 常量记日志）；None 不聚合；未知计数按护栏细则经 `unknown_size_count` 传出（二元组契约不动），主扫描与轻刷共用，轻刷调用方（tui.py:492）同口径展示「N 条大小未知」；**load_snapshot/_rows_to_map 对 s≥16TB 行打 legacy 标记并计数透出（不做改名/quarantine 清洗工具）** | M | W1.0 |
| W1.2 | DEF-002/022、RT-01、RT-N02、RT-05 | ★合计口径下沉公共助手 | compare.py(:148-149/:187-188 两处)、app.py、cli.py、tui.py(H)、tests | **合计计算下沉公共助手，diff_from_current 与 compare_snapshots 两处同步替换**（生产三条链路 CLI --baseline/app.py:413/TUI H:571 全走 diff_from_current，漏一处即 P0-2 验收失效）；根行查找先 normcase 再匹配，防 'C:\\' vs 'c:\\' 静默跌入顶层求和回退；_merge 加 leaf_only 参数（Web 默认 leaf）；对比摘要卡消费 legacy 标记提示「基线含 N 条已知异常大小数据，建议重扫重建基线」 | S+M | W1.0,W1.1 |
| W1.3 | DEF-004/008/010/011、N3、冲突10、RT-06、RT-N04、RT-N09 | ★Everything 引导包 | env.py、app.py(run_server/health)、scan.py:181、exceptions/sdk/messages、app.js、index.html、tests | 类型化 EverythingQueryError(code)＋SDK code 0-7 中文映射＋{ok,error,detail,code}；Session 0 专属文案＋service_only 字段；ready=true 才自动浏览否则引导态；health except 区分 degraded（配置损坏/DLL 失败/**Everything 未安装**三种病因，RT-N09）；bind 前 daemon 自动拉起；统一渲染器容忍新旧两类错误响应并存（无 code/detail 不崩，RT-N04）；引导态文案标注等待上限「最长约 20 秒」防重复点击 | M-L | 无 |
| W1.4 | DEF-005、N2、DEF-009(P0-4) | 行动闭环最小集＋文件行修复 | app.js、app.py(open-path)、index.html、style.css、smoke.html | 渲染层区分（目录行才有 role=button/data-path/tabindex，键盘一并解决）；explorer /select list 形参＋校验；复制 clipboard+execCommand 兜底；B/I 同批互验 | M | 无 |
| W1.5 | DEF-003(冲突11) | TUI selected_idx 收敛 | tui.py、tests/test_tui.py | 收敛＋渲染前钳制双保险 | S | 无 |

**P0 可度量验收（逐条；★ 为 v2 修订）**
- W1.1：①假 SDK 注入 0xFFFFFFFFFFFFFFFF/BOOL FALSE/超上限值夹具单测入常驻回归；②真实脏索引机器上任一出口无超卷容量条目（天文数字输出率=0）；③C 盘概览总占用 vs 资源管理器偏差 ≤5%；④主扫描与轻刷同索引数值一致；⑤★golden 夹具断言「含未知条目→unknown_size_count 正确、聚合不含、六处 `(sizes, contents)` 解包不变」（fullscan/tui/app/cli）。
- W1.2：①夹具 7050B→6050B 场景合计=−1000B 且等于根行 delta；②多根嵌套合计≠明细行累加；③Web 三卡一致；④CLI 与 Web 同输入一致；⑤★TUI H 视图合计与 CLI/Web 同输入一致（三条链路同口径证明）；⑥★大小写混合根行快照对（'C:\\' vs 'c:\\'）合计不跌入回退分支。
- W1.3：①★三种未就绪环境（无 IPC／Session 0／Everything 未安装 degraded）冷启动首屏 0 裸错误码；②★自动拉起后 ≤15s 内徽章进入**确定态**：就绪变绿，或进入「Everything 正在加载索引（最长约 20 秒）」进行时提示；IPC 就绪即刻变绿（对齐 env.py DEFAULT_EVERYTHING_STARTUP_TIMEOUT_SECONDS=20）；③拉起失败兜底：引导态「重试环境检测」≤2 次点击可达且不出裸码；④错误映射全覆盖＋管理员权限错位专述文案；⑤health 配置损坏返回 degraded 区分于未就绪；⑥★渲染器对无 code/detail 的旧形态错误响应正常降级。
- W1.4：①任意行 ≤2 次点击完成打开定位或复制路径（剪贴板==绝对路径）；②文件行点击/键盘 0 个额外 /api/browse（冒烟断言①）；③open-path 非法输入 400。
- W1.5：深刷收缩夹具下 ↑/Enter 不抛 IndexError；test_tui 全绿。

### 批次二 · P1 稳定与闭环（≈8-9 人日）

| ID | 对应 | 工作项 | 触碰文件 | 修法要点 | 量 | 依赖 |
|----|------|--------|----------|----------|----|----|
| W2.0 | 冲突3 | 一键启动按钮完整版 | app.js、app.py(/api/env/start) | 异步启动+轮询 ≤20s，按钮态与 409 处理 | M | W1.3 |
| W2.1 | DEF-006、**RT-02** | ★SDK 统一持锁＋busy 前端契约 | app.py、scan.SCAN_LOCK、tui worker、main、app.js(徽章:146-157/设置弹窗:813-821/init 门控:1019-1027) | health 非阻塞 acquire，拿不到立即返回 `{ready:false, busy:true, reason:"scanning"}`；**硬性契约「busy ≠ 未就绪」三处消费点**：①徽章轮询遇 busy 保持现有状态并显示「扫描中」中性提示（不得打回 warn 未就绪）；②设置弹窗健康栏透出 busy 原因；③引导态/自动浏览门控**只在首次加载与用户点击「重试环境检测」时评估 ready**，不随 15s 轮询周期重求值（防止一次全量扫描把整页打回引导态）；顺带 compare 拿锁失败 409（C-1） | S-M | W1.x |
| W2.2 | DEF-007、N1、N-6、RT-N03、RT-07 | ★日配额接线 | snapshots.py(:368-379,:515)、app.py(:277/:295)、messages、README、tests | auto 硬门槛/explicit 软警告（警告经 last_save_notice 回传，app/README 消费；返回值不变）；check＋record 都放 .snapshot.lock 锁内；README 声明日配额为**近似语义**（跨进程 day_writes.json 读改写存在有界 TOCTOU，略超预算可接受）并以并发夹具锁定现状；拒绝提示 info/warn | S-M | W2.1 |
| W2.3 | DEF-021 | 陈旧锁检测 | snapshots.py(:444-459)、tests | PID 存活+mtime TTL 600s 兜底，偷锁仅一次 | S-M | — |
| W2.4 | DEF-019/009(J) | 快照一键对比 | app.js、index.html、smoke.html | root 归一化预检＋切换单按钮＋409 按钮态；指引第 4 步改次日对比 | M | W1.2 |
| W2.5 | DEF-012/013/014/015 | 前端毛刺批 E/D/G/H | app.js、index.html、app.py、style.css | is_dir 400；空会话撤销 disabled；toast 中性文案；筛选空态+清除按钮 | M(4×S) | — |
| W2.6 | DEF-025/026/028/029、冲突1、**RT-N06** | ★弹窗与设置卫生 K1-K7 | app.js、index.html、app.py(:447) | K1-K7 按 v1；confirmResolver 语义冻结；**wipeData 清键枚举**：handledScanVersion 新键名定为 `pds_handled_scan_version_v1`；wipe 清除键集合={handledScanVersion 新键, lastBrowse 相关 state 键}；`pds_onboarding_dismissed_v1` **默认随 wipe 清除（恢复出厂语义，引导页重现）**——产品侧如需保留引导关闭状态再复议此默认值 | M | W2.5 |
| W2.7 | G-1、DEF-009、冲突4 | Web 导出 | app.py(GET /api/export)、cli.export_report_*、app.js | **先抽流式参数（现实现写文件，需 StringIO 内存生成）**再复用；csv(utf-8-sig)/json 双格式；导出头部消费 legacy 标记提示（RT-05） | S-M | W1.1 |
| W2.8 | N-1 | 浏览竞态防护 | app.js(browsePath) | browseSeq 代际 token ~10 行 | S | — |
| W2.9 | SEC-1/2 | 安全两项 | app.py、tests | settings 白名单落地+投毒链单测；Host 白名单中间件 | S | W2.6 |
| W2.10 | DEP-1、R-2 | 部署与停服健壮 | app.py(run_server)、fullscan.py(cancel_event) | bind 前探 /api/health 防双实例；atexit cancel_event+join(timeout) | S | — |
| W2.11 | B-2/B-1缓/B-3、冲突5、**RT-N05** | ★保存/撤销语义补全（含搭车） | app.py、snapshots.py(ledger)、session.py、app.js | undo 回滚台账；逐盘 try+成败清单；completed_at 透传；**搭车项：SEC-4 undo unlink 前 relative_to(get_snapshots_dir()) 校验（~3 行）＋B-5 session.save_session 改 tmp+os.replace（模式照抄 save_ledger）**——同域同文件顺车不留 backlog | S-M | W2.1 |
| W2.12 | 冲突8、D5、DEF-023/024、**RT-03** | dispatcher 删除＋CLI 边角 | dispatcher.py 删、main.py、README×6、spec 注释、messages.py(E_BUSY)、cli.py(:378/:79) | 联动清单同 v1；E_BUSY 文案去键位字母；空路径 fatal exit 1；prompt 捕获 EOF；~~test_web.py ResourceWarning~~ **DEF-030 已转规约（W1.0），本项不再引用该文件** | S-M | — |
| W2.13 | D1-D4、冲突7、**RT-N01** | ★文档漂移＋机器校验接线 | README、cli.py/api_compare/tui.py(H)、compare.py、tests | 强校验接线+逃生口**三端对齐**：CLI --allow-other-machine 参数；Web 对比被拒时出确认弹窗、二次提交带 allow 参数重发；TUI 红字提示+确认键；D1/D3/D4 改文档 | S-M | W1.0 |

**批次二验收要点**：并发脚本无挂起无交错；★busy 场景冒烟断言④（stub 返回 busy health：徽章不打回未就绪、整页不进引导态、设置健康栏显示扫描中）；孤儿锁可恢复；伪造 Host 403、settings 写 everything_* 400；二次启动单 LISTENING；undo 后指纹谓词不抑制下次自动保存、undo 仅删快照目录内路径；跨机基线三端均可显式放行且默认拦截；grep dispatcher 生产代码零残留；explicit 超限→保存成功且有警告（test_tui 断言，RT-07）。

### 批次三 · P3 打磨/还账（≈3 人日）

| ID | 对应 | 工作项 | 触碰文件 | 修法要点 | 量 | 依赖 |
|----|------|--------|----------|----------|----|----|
| W3.1 | R-1 | 内存瘦身一期 | fullscan.py(:54-55)、snapshots.py(:524-527)、app.py(409 文案) | 砍 display_paths；超限文案改真因；字符串共享延后 | S-M | — |
| W3.2 | L-5/DEF-017、N-8 | 概览自动刷新 | app.js(pollFullscan) | 完成分支触发 refreshOverview；定时器收敛 | S | — |
| W3.3 | DEF-020、DOC-4/5、**RT-N08、RT-N10** | ★口径文档＋API 卫生碎项 | README、requirements.txt、app.py(404/405)、app.js(:780) | 空目录口径声明；结构清单更新（dispatcher 删行）；requirements 声明 Flask；**碎项归堆：compare truncated 前端文案改为如实描述（底层 50 万行截断 vs 前 100 条切片）、404/405 改返 JSON 契约、scan_snapshot_dir/INFO_SNAPSHOT_AUTO 闲置资产在文档中声明「保留待接或删除」的归宿；README 已知限制补一句 explorer /select 定位失败的残余边界** | S | W2.12 |
| W3.4 | DEF-027、DEF-031(部分) | 徽章反馈与版本铺垫 | style.css、index.html | hover brightness+呼吸动画(reduced-motion 豁免)；页脚版本号 | S | — |
| W3.5 | 盲区三、P11 未竟、**RT-N09** | ★隔离目录破坏性流程验收 | scripts、docs | 重定向数据目录脚本化 save/undo/wipe（含 confirmResolver 双确认、undo 台账回滚、wipe 清键集合核验）；**增列 PyInstaller windowed 打包版 daemon 自动拉起冒烟**；承接核销 P10/P11 悬置勾选项 | S-M | W2.x |

---

## 四、架构评审遗留项取舍（v2 微调）

吸收不变：N-1→W2.8、DEP-1/R-2→W2.10、SEC-1/2→W2.9、B-3→W2.11、N-6→W2.2、L-5→W3.2、R-1→W3.1 一期、C-1 部分吸收（W2.1）。
v2 调整：**B-5 与 SEC-4 从 backlog 提升为 W2.11 搭车项（RT-N05）**；backlog 余下：G-6、SEC-3 CSRF、B-4、B-6、N-2~N-5/N-7~N-13、ARCH-1 快照视图模型统一、ARCH-2 main 兼容层拆除（建议 P13 首议）。

---

## 五、测试与质量策略

1. **unittest 全绿底线（基数以 W1.0 实测记录为准）**：当前树 tests/ 仅 8 个文件（无 test_web.py/test_dispatcher.py），`def test_` 约 165 项；「419」系历史机器数字不再作为承诺基数。每工作项合并前 `python -m unittest discover -s tests` 全绿＋`-W error::ResourceWarning` 卫生门禁；W1.0 首日核定新基线并回填 README D3 与《开发方案》措辞（**硬性步骤，不完成不得动 scan/compare/snapshots**）。
2. **哨兵值失败注入回归**：假 SDK 夹具喂 0xFFFFFFFFFFFFFFFF/BOOL FALSE/超上限值 → unknown_size_count 正确、聚合不含、二元组解包不变（W1.1 验收⑤，golden 夹具常驻）。
3. **合计口径夹具**：7050B→6050B 多根/祖先嵌套＋大小写混合根行对；断言合计==根行 delta、Web 三卡一致、CLI==Web==TUI H（W1.2）。
4. **前端零依赖冒烟页四断言**：①data-path="" 行不发请求；②筛选空态出「清除筛选」；③双弹窗 Esc 逆序关闭；④★busy 契约（stub 返回 busy health 后徽章保持、整页不进引导态、设置健康栏显示扫描中）。约定改 app.js 必跑；纯函数拆 app-core.js 用 node --test 直测。stub 同时照抄新旧两种真实错误响应形态（RT-N04）。
5. **隔离数据目录破坏性流程脚本化验收**（W3.5）：save/undo/wipe 全流程＋windowed 打包版拉起冒烟；产出留档 docs/，核销 P10/P11 悬置项。
6. 沿用「设计→实现→测试→验收→更新文档」流程；不伪造验收。

---

## 六、总工作量与关键路径

| 批次 | 内容 | 人日 |
|---|---|---|
| 批次一 | W1.0-W1.5（含护栏、legacy 标记、busy 前端契约前置评估） | ≈5.5-6 |
| 批次二 | W2.0-W2.13 | ≈8-9 |
| 批次三 | W3.1-W3.5 | ≈3 |
| **合计** | | **≈16.5-18 人日（±20%）** |

关键路径不变：W1.0（含基线核定，硬门槛）→ W1.1 → W1.2 → 批次一验收闸门 → 发版判定（批次一验收全过＋unittest 全绿）。次关键：W2.1→W2.2→W2.3 串行；前端线 W1.4→W2.5→W2.6。并行两人约 2 周完成批次一二，批次三 0.5-1 周。

---

## 七、风险登记表

| 风险 | 触发条件 → 缓解措施 |
|---|---|
| TUI/CLI 受新异常类型影响 | scan.py:181 抛新类型后捕获失配 → 继承既有基类、messages 集中映射、test_cli/test_tui 补断言、CLI 真机冒烟 |
| 测试断言与实现漂移 | 错误文案/响应结构改动打爆现有测试 → 同 PR 先改期望再改实现；additive 不删旧键；渲染器容忍新旧形态（RT-N04）；W1.0 基线核定消除幻影文件引用 |
| 弹窗重构破坏 confirmResolver | K1/K3 抽 util 时 Promise 链断裂 → confirmResolver 语义红线；先落冒烟断言③；wipe 双确认入 W3.5 脚本 |
| 删除 dispatcher 联动遗漏 | main.py 兼容层/外部脚本/README/spec 残留 → 六项清单勾选＋grep 零残留门禁＋excludes 注释更新 |
| 快照相关变更伤及老用户数据 | D2 拦截存量跨机对比；配额致 auto 静默跳过；**存量污染快照在新合计下算出天文 delta** → 全部带逃生口或显式原因（--allow-other-machine、REASON_DAY_BUDGET_EXCEEDED）；**存量污染走 load 层 legacy 标记＋「建议重扫重建基线」提示（RT-05 决议，S 级；不引入改名/quarantine 类清洗工具，避免 session 清单 snapshot_path 失配联动）**；破坏性验证仅在隔离目录 |
| explorer /select 残余静默 | launched:false 仅捕获 spawn 失败，定位失败仍静默 → 保留「已复制路径」info 降级＋README 已知限制一句（W3.3）；验收口径按「请求已发出＋路径可复制」 |
| busy 语义被误读为故障 | W2.1 上线后扫描期 health 忙碌被当成未就绪 → RT-02 三处消费点契约＋冒烟断言④＋徽章中性文案「扫描中」 |
| 哨兵值上限误伤极端大容量盘 | GetDiskFreeSpaceExW 失败 → 回退 16TB 常量记日志＋夹具覆盖该分支 |
| 前端净增 250~320 行缺回归网 | 冒烟页仅 4 断言 → 改 app.js 必跑纪律＋纯函数尽早拆 node --test |

---

## 八、与《docs/开发方案.md》的衔接

1. 本方案作为 **P12（a/b/c 三批）**挂入，沿用五步流程；P10/P11 悬置项由 W3.5 承接核销。
2. 全局约束条款修订文字（v1 版维持，补充细则指引）：「scan.py、compare.py、snapshots.py 自 P12 起允许受控修改，前提①先合入兼容性回归；②快照格式(format v1)与已有 API 字段语义红线不变；③每项独立提交、unittest 全绿方可合并；④零破坏传出通道实施细则见整改方案 §一护栏第 3 条（返回值契约冻结）」。
3. 「允许修改」清单补：exceptions/messages/session/env/sdk/cli/tui/main.py、PyInstaller spec（注释级）；dispatcher.py 删除登记一次性例外。
4. 进度日志建议：`2026-08-24：整改方案 v2（吸收红队评审 RT-01~07）编制完成；五项拍板事项决策者已签初步意向，待最终定稿。`

---

## 九、已决事项（决策者已签·初步意向批准，2026-08-24；以最终定稿文档为准）

1. **冲突9**：解除禁改约束＋受控修改护栏生效（含 RT-04/07 零破坏传出通道实施细则）。
2. **冲突2**：日配额语义＝auto 硬门槛/explicit 软警告，授权修订 README 承诺（警告回传按 RT-07 additive 方案，不改语义）。
3. **冲突3**：P0 只做冷启动自动拉起＋重试检测按钮，一键启动按钮移批次二；**P0-3 验收②按 RT-06 修订为「≤15s 进入确定态（绿或『加载中』进行时提示，进行时上限约 20s）」——该修订使指标可度量，纳入批准范围**。
4. **冲突4**：Web 导出提前至批次二 W2.7（红队核实需先抽流式参数，S-M 估算维持）。
5. **冲突7**：D2 改实现带逃生口（RT-N01 三端口径为补充实施细节）。
6. **RT-05 处置**：决策者意向采用 load 层 legacy 标记提示的 S 级方案，不恢复 --scrub-snapshots 工作项（已落入 W1.1/W1.2/W2.7 与 §七）。

## 十、复议事项

**无。** 五项签批意向与红队新证据逐一核对后均不冲突：RT-06 仅使验收指标可度量（不改批次划分），RT-07/RT-N01 仅定义回传通道与三端口径（不改语义方向）。另有两个实现默认值请产品侧知悉（非复议项）：wipe 清键含 onboarding 键（恢复出厂语义，W2.6）、busy 时徽章中性文案「扫描中」（W2.1）。

---

## v1→v2 变更记录

- RT-01：W1.2 修法落点由泛指改为「合计下沉公共助手，diff_from_current 与 compare_snapshots 两处同步替换」，验收追加⑤ TUI H 一致性断言（生产三链路全走 diff_from_current，漏改即 P0-2 全灭）。
- RT-02：W2.1 新增硬性契约「busy≠未就绪」，徽章/设置弹窗健康栏/引导态门控三处消费点逐一交代；冒烟页增第④断言；§七新增对应风险行。
- RT-03：测试底线改述为「unittest 全绿（基数以 W1.0 实测记录为准）」；DEF-030 关闭缺陷形态转 web 契约测试编码规约（with-resp＋-W error::ResourceWarning 门禁，随 W1.0 生效），W2.12 不再引用不存在的 test_web.py；台账改「28 项落地＋1 项转规约」；W1.0 基线核对升级为硬性步骤（不完成不得触碰三核心文件）。
- RT-04：W1.1 写死零破坏传出通道＝sizes dict 附加 unknown_size_count 属性（六处二元组解包不动），golden 夹具覆盖「计数正确/聚合不含/解包不变」，补轻刷调用方展示口径；上升为 §一护栏第 3 条实施细则。
- RT-05：按决策者意向落实 load 层 legacy 标记 S 级方案（W1.1 检测、W1.2 摘要卡提示、W2.7 导出头部提示），不恢复 scrub 工作项；§七风险表原「quarantine/scrub」虚空引用已替换为与工作项一一对应的 legacy 措辞。
- RT-06：P0-3/W1.3 验收②改为「≤15s 进入确定态（绿或『Everything 正在加载索引（最长约 20 秒）』进行时提示）」，对齐 env.py 20s 超时常量；引导态文案标注等待上限。
- RT-07：explicit 软警告回传指定 additive 方案＝模块级 last_save_notice（读后即清），save_snapshot 返回 Path|None 契约冻结入护栏细则；W2.2 补消费方与 test_tui「超限仍成功且有警告」断言。
- RT-N01→W2.13（逃生口三端对齐）；RT-N02→W1.2（normcase＋大小写夹具）；RT-N03→W2.2（锁内 check+record＋近似语义声明＋并发夹具）；RT-N04→W1.3（渲染器容旧形态＋stub 双形态）；RT-N05→W2.11 搭车（SEC-4＋B-5）；RT-N06→W2.6（清键枚举，onboarding 键默认清除）；RT-N07→backlog（指标集/JSONL 去向已列入 §二 Out 表）；RT-N08→W3.3（truncated 文案/404-405 JSON 化/闲置资产归宿）；RT-N09→W1.3 验收①第三环境＋W3.5 windowed 冒烟；RT-N10→§七风险行＋W3.3 README 已知限制。
- 拍板事项：五项自「待拍板」移入 §九「已决事项（决策者已签·初步意向批准）」，注明以最终定稿文档为准；§十新增「复议事项：无」及两项产品侧知悉默认值。
