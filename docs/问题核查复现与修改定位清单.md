# 问题核查复现与修改定位清单

> 配套《问题逐项核查报告_待修复.md》使用。本文不提供业务修复代码，只规定后续复现、证据保存和修改前的验收口径。完整阶段流程见《[问题整改阶段开发计划](./问题整改阶段开发计划.md)》。

## 一、文档与图片目录约定

本资料包解压后的目录结构必须保持如下形式；两份核查文档均从 `docs/` 目录所在位置引用图片：

```text
问题核查资料_20260902/
├─ 问题说明.md
├─ 问题逐项核查报告_待修复.md
├─ 问题核查复现与修改定位清单.md
└─ img/
   ├─ image-20260902154144366.png
   ├─ image-20260902154603439.png
   ├─ image-20260902154846994.png
   ├─ image-20260902154955413.png
   ├─ image-20260902155747740.png
   ├─ image-20260902160946498.png
   └─ image-20260902161629792.png
```

Markdown 图片统一使用正斜杠相对路径，例如：`![说明](./img/image-20260902154144366.png)`。不要改为本机绝对路径、Windows 反斜杠路径或压缩包内路径。

## 二、证据记录格式

每次复现至少记录：日期/版本、服务端口、Everything 进程与 SessionId、是否已有旧实例、输入路径、操作顺序、Network 请求及状态码、`/api/fullscan/status` 和 `/api/health` 响应、UI 最终状态、截图/录屏路径。

## 二、优先复现矩阵

| 优先级 | 问题 | 隔离条件 | 通过标准 |
|---|---|---|---|
| P0 | 1、7、13、17、20 | 两份测试快照；必要时 mock SDK；第二次扫描前清除旧状态 | 每个请求有终态；状态与缓存来源一致；停止后不伪称缓存 |
| P1 | 3、5、8、14 | 临时数据目录/测试盘；不使用真实快照目录 | 删除粒度明确；自动流程可恢复；导出可下载；关系功能需求先定稿 |
| P2 | 2、6、9、10、11、12、15、18、19、21 | Edge 浏览器，桌面/窄屏，开启/关闭 reduced motion | 逐帧或截图能区分设计动画与残留渲染；每个按钮有可见反馈 |
| P3 | 4、16 | 构造时间窗口快照；采集 30 秒日志 | 趋势空态有原因；200 日志可解释且无异常堆栈 |

## 三、建议测试脚本/夹具（后续实施）

### A. 对比与趋势

构造同根快照时间点：当前、23 小时前、25 小时前、8 天前；另造跨盘基线。验证：

- 对比页 loading 必须转成功/错误/扫描中状态；
- 较昨日只命中 24 小时内；较上周只命中 `(24h, 7d]`；
- 跨盘选择要有明确提示；
- 无快照/损坏快照不能无限 loading。

### B. 二次扫描和停止

采样状态序列：`idle → running → completed/aborted/error`，分别测试完成后重扫、保存后重扫、第一次中止后重扫、连续点击启动。重点字段：`running`、`scan_version`、`roots_done`、`result_ready`、`save_ready`、`stop_requested`、`stop_reason`。

### C. 视图与动画

数据加载后在 0ms、100ms、500ms、动画结束四个时点截图：矩形图→排行→表格→矩形图；同时记录两个容器 hidden/class、canvas 数量、动画数量。主题按钮和设置选项分别从中心/边缘点击，桌面/窄屏/Edge/reduced-motion 各跑一遍。

### D. 快照安全操作

必须使用临时 `LOCALAPPDATA`/`DSA_SNAPSHOT_DIR` 或 mock 数据目录。删除测试应覆盖：单盘、整会话、已跳过根、路径越界、文件不存在、删除失败、台账恢复、清单原子写。

### E. 导出

覆盖无结果、全量完成、多盘指定 root、空结果、未知大小、CSV BOM/转义、JSON UTF-8、Content-Disposition 和命令面板入口。原错误 JSON 只应在无可导出结果等保护场景出现。

## 四、后续代码修改定位索引

- 对比 loading/错误：`web/static/js/app/pages/compare.js:201-265`；`app.py:604-684`。
- 视图切换残留：`web/static/js/app/pages/workspace.js:377-449`；`web/static/css/style.css:122-124`。
- 指定快照删除：`app.py:525-601`；`web/static/js/app/pages/snapshots.js:124-186,456-476`。
- 趋势卡：`web/static/js/app/pages/snapshots.js:203-385`。
- 自动扫描/保存：`web/static/js/app/main.js:228-255`；`components/scan.js:137-153,339-379`。
- 扫描状态/停止：`app.py:366-391`；`components/scan.js:155-329`；`fullscan.py`。
- 缓存来源：`app.py:227-319`；`workspace.js:609-620,244-253`。
- 设置/主题：`index.html:156-216`；`components/settings.js:63-77,174-187`；`theme.js:79-118`。
- 日志：`app.py:853-937`；前端轮询 `components/scan.js:169-195`。
- 导出：`app.py:753-813`；`components/scan.js:412-418`；`components/list.js:142-184`。

## 五、测试环境注意事项

- 使用正式 `run_server()` 与直接 `Flask test_client` 分开记录；后者可能绕过 stdout/stderr UTF-8 重配置并触发 GBK emoji 问题。
- 启动前记录并确认旧端口实例，禁止把旧实例响应当作新代码验证。
- Python 测试基线当前为 266；动效 Node 测试为 25；历史报告中的 419 仅作历史参考。
- 真实 Everything 扫描、停止、保存和删除必须在隔离数据目录中执行。

## 五-J、2026-09-08 批次（用户实测问题 10 项 · 复现与定位清单）

> 依据：`问题/问题清单.md`（用户原文）、`docs/问题核查资料_20260908/复现记录.md`（2026-09-08 现场取证）。
> 本阶段 P0 只做**基线冻结与工具链**，10 项问题的修复属于 P1–P8；本条登记每项的**最小复现命令 / 证据 / 结论 / 归属阶段**，供后续阶段直接取用。
> 环境速查：`.venv\Scripts\python.exe`（3.14.3）；服务 `python app.py --no-browser`（5000）；探针统一 `import { chromium, launch } from "./_harness.mjs"`；三支取证工具 `scripts/dev/p00_*.mjs`。

| # | 问题（用户要点） | 最小复现命令 | 证据文件 | 结论 | 归属阶段 |
|---|---|---|---|---|---|
| 1 | 扫描完成自动保存没生效、仍需手动保存；多次扫描怎么办 | `python app.py --no-browser` → 完成一次全量扫描 → 观察 toast「可保存快照」+ `保存快照` 按钮可用（实测 2026-09-08 20:08 复现） | `docs/问题核查资料_20260908/复现记录.md` §六；**P1 修复证据**：`docs/问题核查资料_20260908/p1/manifest.json`（三态截图 + A 类 rAF + 真实服务链路） | **已修复（P1·2026-09-09）**：根因=前端跨进程代次闸门 `pds_handled_scan_version_v1` 服务重启后残留静默 + 后端无自动保存归口。修复=**后端归口**（结果就绪回调触发一次自动保存，`config.auto_save`+`is_snapshot_disabled()` 双闸门）+ 前端删代次闸门、三态可见可补救（跳过精确原因 + 「仍要保存（强制）」）；多次扫描=每次完成尝试一次由四原子谓词裁决 | **P1 已交付** |
| 2 | 切到排行/关系后矩形图闪透 | `node scripts/dev/p02_view_frame_probe.mjs --base http://127.0.0.1:5000/ --out <证据目录>`（默认桩态；`--label prefix`/`postfix` 区分前后；先跑 `prefix` 于 `p1-autosave@750176a` 代码自证探针有效） | `docs/问题核查资料_20260908/p2/`（`manifest.json`：5 证据项 / 11 张图片——A 类 `baseline_prefix`+`postfix` 的 frames+summary、B 类 `screencast/` 122 原始帧/37 唯一帧+3 参照+5 关键帧、C 类三视口截图） | **已修复（P2·2026-09-10）**：根因 R1=方向判定为「矩形图 vs 表格」二元（非矩形图→非矩形图也走交叉淡化 → `removeAttribute("hidden")`+`opacity=1` 整幅重现 120ms）；R2/R3=两容器均无 z-index 且 `.v-crossfade` 规则在 CSS 中根本不存在；R4=`pause()` 只 `cancelAnim()`（不断重绘、不收束终帧、隐藏态仍响应 hover），列表 hover→矩形图联动的委托缺可见性守卫（实测 6 行 hover 产生 70 次隐藏期 canvas 绘制调用）。修复=①方向判定改「上一视图 vs 下一视图」（`treemapInvolved`）②补 `.v-crossfade-in/out` 层序(3/2)+`pointer-events:none` ③`setPaused()` 真暂停与 `highlightKey`/指针链守卫。**帧级前后对照（同一支探针、同一判据）：128/488 → 0/486 违规帧；30ms 连点 24 → 0** | **P2 已交付** |
| 3 | 删除舒适/紧凑列表开关 | `node scripts/dev/p03_rail_layout_probe.mjs --base http://127.0.0.1:5000/ --out <证据目录> --label prefix\|postfix`（工具栏裁剪图带 `--label`，即密度开关删除前后对照）；旁证 `node scripts/dev/u43_doc_probe.mjs --base http://127.0.0.1:5000/`（断言 `densityRemoved`） | 代码定位：`workspace.js:1154`（入口）、`style.css:637,641-645,653,661-662,1120,1132`；**P3 修复证据**：`docs/问题核查资料_20260908/p3/manifest.json` → `P3-density-AB`（`baseline_prefix/toolbar-prefix-1366x768.png` 含「紧凑列表」按钮 vs `postfix/toolbar-postfix-1366x768.png` 已消失） | **已修复（P3·2026-09-11，D3-6/D3-1）**：密度开关**整体删除**——DOM（`#btn-density` 从 workspace 模板移除）、状态（`state.js` 删 `view.density`）、读取点（`list.js`/`relate.js` 行高改常量 36px、删全部 density 分支；`workspace.js` 不再传 density）、CSS（9 条 `.compact-list` 规则全删）、探针（u25 密度段 / u43 `#btn-density` 断言 / u57 场景② 退役，u43/u57 改为正向断言「入口已删除」）、文档（README 三小节）。裁定依据：compact 会 `display:none` 掉占比条并把占比列压成 `width:0`（信息损失），收益仅 1.38× | **P3 已交付** |
| 4 | 右栏排版随意、扫描时拥挤、不随尺寸自适应 | `node scripts/dev/p03_rail_layout_probe.mjs --base http://127.0.0.1:5000/ --out <证据目录> --label prefix\|postfix`（三视口 × 三态，扫描态注入真实长盘名/长 ETA） | `docs/问题核查资料_20260908/p3/manifest.json`（20 图 / 哈希全唯一：`postfix/` 9 张三视口三态 + 2 张密度前后对照 + 量化 `layout.json`/`summary.json`；`baseline_prefix/` 为红线 A 基线）；旧基线 `p00/viewport_shots/` + `复现记录.md` §二 | **已修复（P3·2026-09-11，D3-2…D3-5）**：① `.side-rail` 由固定 `300px` 改区间自适应 `clamp(280px,22vw,380px)`（1024→280 / 1366→300.5 / 1440→316.8 / 1920→380）；② 扫描态状态行分层（`flex-wrap` + `.scan-meta` 独立第二行），基线实测 `#fullscan-status-text` 306.8px 高 / 18 行竖排 → 修复后 0 违规；③ 图例 `.donut-legend-value/label` 显式换行 + `.storage-row` 改 grid（基线 1920 下 `scrollWidth 111 vs clientWidth 92` 硬切 19px → 0）；④ `.overview-head` 标题 `word-break:keep-all` + 动作组 `flex:none`（基线「存储概 / 览」竖排 2 行 → 单行）；⑤ 右栏文本截断策略统一（禁止裸 nowrap）。**同阶段修掉根因缺陷 DEF-P3-01**（`style.css` 头注释 `--fs-*/--radius-*/--space-*` 的 `*`+`/` 提前闭合注释 → 度量 `:root` 整条被丢弃，87 处 `var(--fs-*)`/36 处 `var(--radius-*)` 失效）；**同一支探针 A/B：1366×768 右栏内滚 idle 138→0px、done 228→4px、scanning 484→58px；硬违规 clipped 8→0、escape 12→0、titleWrap 3→0** | **P3 已交付** |
| 5 | 对比直接显示最深目录、想按深度聚合/下钻 | `node scripts/dev/p04_compare_depth_probe.mjs --label prefix\|postfix --out <证据目录>`（探针自拉夹具 harness；`--label prefix` 在 stage-p3 @ `d37627e` 上现采自证探针有效） | `docs/问题核查资料_20260908/p4/`（`manifest.json`：41 项 / 24 图——`postfix/` 12 张 C 类关键帧 + `baseline_prefix/` 12 张红线 A 基线 + `api-*.json` 逐请求量化 + `layout-*.json`/`summary-*.json`）；夹具旁证 `p00/fixtures/fixture_validation.json` | **已修复（P4·2026-09-10，D4-1/D4-2/D4-6）**：根因=`app.py` 两条路径写死 `leaf_only=True` + `top_growth(report,100)`，`compare.py` 无 depth/rollup。修复=① `compare._rollup` 在排序/截断**之前**把键集合折叠到「相对对比根的第 N 层」（目标键在则取自身值，层级缺口取组内顶层成员之和；更浅的被覆盖目标与根行不入行集）；② `compare_snapshots`/`diff_from_current` 透传 `depth`（`app.py` 同步与异步两条路径同源，job 去重键并入新参）；③ 前端深度选择器（叶子（默认）/ 1..5 层）+ 行点击页内下钻 + 面包屑返回。**探针 A/B（同一支/同判据）：违规 80（API 20 + UI 60）→ 0**；`depth=1` 返回叶子 4 行 → 顶层 3 行且 `Σ(delta) == delta_total == +50`；下钻 `D:\apps\framework\core` 由 4 行假行（Σ=−220）→ 1 行 `engine +80 == delta_total` | **P4 已交付** |
| 6 | 无增量时列出 0 增量目录 | 同上 `--fixture flat` → 对比 flat-a/flat-b（两侧完全一致） | 同上 `p4/manifest.json` 的 `postfix/compare-flat-empty-*.png` 与 `baseline_prefix/compare-flat-empty-*.png`（问题 6 前后对照） | **已修复（P4·2026-09-10，D4-3/D4-4/D4-5）**：三个独立成因逐一处置——① `compare.py` 逐键产行：新增 `drop_zero`（排序与截断**之前**过滤 `delta == 0`）；② `top_growth` 按有符号 delta 降序导致负增量被挤出 Top-N：新增 `order_by="abs"`（默认值一字不改，cli.py:518 零回归）；③ 前端全量入表/空态判据错/摘要只统计切片内 rows：空态改判**过滤后**行集（文案区分「确实无变化」与「已隐藏 N 条零变化」），摘要取后端 additive 全量聚合行口径（`rows_total`/`zero_total`/`max_growth`/`max_release`/`depth`，与 `delta_total` 自洽）。**flat 夹具：修复前 1 行 `± 0.00 B`（`D:\flat\a\b\c\file.dat`）→ 修复后 0 数据行 + 双空态「无差异」**；growth 叶子口径：4 行（含 1 条零行、负增量被挤到末位）→ 3 行且 `+70 / +10 / −50` 正负同榜 | **P4 已交付** |
| 7 | 基线/目标意义不明、首开没有显性选盘 | `node scripts/dev/p05_semantics_roots_probe.mjs --label prefix\|postfix --out <证据目录>`（探针自建 subst 虚拟盘 + 自拉夹具 harness；`--label prefix` 在 CS2 @ `328ce70` 的 web/ 源码上现采自证探针有效） | `docs/问题核查资料_20260908/p5/`（`manifest.json`：51 项 / 截图 27 张——`postfix/` 9 张三视口×三态 + `baseline_prefix_final/` 9 张红线 A 基线 + `_baseline_prefix_cs1/` 探针首版留痕；`semantics-/onboarding-/roots-/layout-/summary-*.json` 逐项量化）；量化旁证 `docs/问题核查资料_20260908/p5/redline_b_datadir.txt` | **已修复（P5·2026-09-10，D5-1…D5-5）**：三个独立成因逐一处置——① 术语裸奔（「基线/目标」只在原生 `title=` 里解释）：改「对比基准（历史快照）」/「当前磁盘状态（实时）」，**删除只读「目标」输入框**改只读文本行 `#compare-current`；② 语义自相矛盾（代码自述「目标=同盘符最新快照」是标识性文案，实为当前磁盘状态）：界面如实表述，`compare.py` 零改动（`BACKCOMPAT-VERDICT: PASS`）；③ 首开无选盘 + 盘符硬编码 + 默认根魔法值：新增 additive `GET /api/roots`（只读复用 `fullscan._enumerate_roots`，`ready` 表达未就绪）、引导弹层第 2 步「选择要分析的盘」（可跳过、可逆）、默认根统一「上次浏览 → 枚举盘首个可用盘 → 请选择盘符」并清空全部 `"D:\\"` 兜底（9 处）。**探针 A/B（同一支/同判据/同夹具）：违规 64（rootsApi 22 + onboarding 24 + samples 18，术语裸词 18）→ 0**；1366×768 零页面滚动保持（`bodyScrollHeight 768 == innerHeight 768`） | **P5 已交付** |
| 8 | 想多选快照做折线图看趋势 | `node scripts/dev/p06_pages_visual_probe.mjs --label prefix\|postfix --out <证据目录>`（探针自拉夹具 harness；`--label prefix` 在 `stage-p5 @300b527` 的 web/ 源码 worktree 上现采自证探针有效）；数据源旁证 `node scripts/dev/u55_trend_probe.mjs`（16/16）、`node scripts/dev/u67_compare_leaf_probe.mjs --fixture`（8/8，夹具化真跑） | `docs/问题核查资料_20260908/p6/`（`manifest.json`：43 项 / 截图 30 张——`postfix/` 15 张（三视口 × 5 态）+ `baseline_prefix/` 15 张红线 A 基线；`pages-/layout-/trend-/summary-*.json` 逐项量化；`perf/series_perf.json` 性能实测；`probes/u55·u67·u68b·u33` 结果 JSON） | **已修复（P6·2026-09-10，D6-3/D6-4/D6-5/D6-6）**：① 后端新增 additive `GET /api/series`（`root`/`snapshots[]`/`path`/`depth`/`limit`）→ `points[{snapshot,created_at,bytes,present,rows}]`，口径 = 快照自身行数据（与 `/api/snapshots` 的 `total_by_root` 共用 `_snapshot_root_stats` 缓存，`/api/snapshots` 只快不慢：冷 853ms → 热 0.9ms）；② 前端 `viz/line.js`（零依赖 SVG：坐标轴 + 数据点 + 悬浮读数 + 键盘 ←/→ 游标 + aria 概述），路径计算复用 `motion-core.sparklinePath`；③ 对比基准升级为 `<select multiple>`（id 与默认单选语义不变），选中 ≥2 份即出折线，<2 份给**原因**（非空白）；④ 上限：份数 12（`SERIES_MAX_POINTS`）、累计解析行数 200 万，触顶在响应里如实回显 `truncated/dropped`；性能实测 5 份 × 13 万行（65 万行）冷启动 **4152ms** / 缓存命中 **1.6ms**。**探针 A/B（同一支/同判据/同夹具）：违规 8 → 0**（其中「无折线/无多选/无读数」4 项、其余 4 项见问题 9 行） | **P6 已交付** |
| 9 | 对比/快照页 UI 像 demo | 同问题 8 的 `p06_pages_visual_probe.mjs`（同一支探针同时判两页；量化判据：可见文本原始文件名 / 五类卡片计算样式元组 / 字号与间距刻度 / 内联 magic px） | `docs/问题核查资料_20260908/p6/baseline_prefix/`（修复前 15 张）vs `postfix/`（修复后 15 张）+ `pages-*.json`（逐样本量化） | **已修复（P6·2026-09-10，D6-1/D6-2）**：三个独立成因逐一处置——① **原始文件名外露**（`session_20260908_200846_…json`、`C_…_auto_….snap.gz`）：列表正文改「时间 · 自动/手动 · N 个盘 · 合计大小」/「盘符 · 大小」，文件名/会话 ID/清单名收进行尾「详情」展开区（`<details>`，折叠态不绘制 → 探针按 `checkVisibility({contentVisibilityAuto:true})` 判「绘制可见文本」）；② **卡片语言不一**：五类卡片（`.compare-stat`/`.compare-diverge`/`.trend-card`/`.session-item`/`.snapshots-list-wrap`）统一到卡片原语（`--card` + `--border` + **单一 `--radius-card`** + `--shadow-sm`）——修复前实测圆角 8px/16px/0px 三种混用，修复后计算样式元组**全等**；③ **内联尺寸与令牌缺口**：表格列宽 `style="width:120px/90px/80px"` → 列类 `.col-*`，条宽改 `--diverge-w`，`snapshots.js` 的 `const W=120,H=28` → 读 tokens `--spark-w/--spark-h` 单一来源；`tokens.css` 补 `--space-7`/`--fs-2xs`，两页字号全部落在 11/12/13/14/16/20/24、间距落在 4..32 刻度（实测 0 越界） | **P6 已交付** |
| 10 | 主题扩散不从鼠标位置开始；底边触底瞬间铺满 | `node scripts/dev/p07_theme_pixel_probe.mjs --label prefix\|postfix --out <证据目录> [--base http://127.0.0.1:5001/]`（CDP screencast 16ms 采帧 + 页内 canvas 解码 + RANSAC 圆拟合 + 覆盖曲线；`--label prefix` 在 `p6-pages-redesign` worktree 上现采自证探针有效）；旁证 `u62_theme_vt_probe.mjs`（46/46）与 `u63_theme_coord_probe.mjs --browsers chromium`（7/7，含像素层圆心断言） | `docs/问题核查资料_20260908/p7/`（`postfix/`+`baseline_prefix/` 的 `pixel-*.json` 逐例量化：面积曲线 / 拟合圆 / 伪元素 clip / VT 动画数；`probes/{u62,u63}-result.json`）；584 帧序列与判读见 P7 交付报告 §四 | **已修复（P7·2026-09-10，D7-1…D7-5）**：五个独立成因逐一处置——① 坐标空间错位（clip 参考盒是 `documentElement`，而 clientX/Y 是视口坐标，原代码无换算）→ 改 `getBoundingClientRect()` 换算 + 半径 `hypot(max(x,W-x),max(y,H-y))*1.02+16`；② 一帧铺满（WAAPI 缺 `fill` → 450ms 结束时 clip 回到 `none`，未覆盖像素一帧内全变）→ 补 `fill:"forwards"` 并在转场结束后 `cancel()` 防填充态堆积；③ 伪坐标（键盘 Enter/Space 与 `el.click()` 的 `clientX=clientY=0`、`detail===0` 被当合法坐标 → 圆从视口左上角扩散）→ 识别为「无坐标」并取触发元素中心；④ 命令面板「切换主题」传 `null` → 直切、完全没有扩散 → 传「刚刚的指针坐标 / 面板项中心」；⑤ 设置弹窗 300ms TTL 与 `pointFrom` 回退（慢点击/点在外边距时圆心退回控件中心）→ 改「下一次 change 消费 / 组外 pointerdown 清空」。**探针 A/B（同一支探针、同一判据）：违规 4（键盘圆心偏 921px、慢点击偏 14px、命令面板直切无扩散）→ 0**；u62 修掉恒真断言与死分支后 46/46，u63 新增像素层圆心断言后 7/7 | **P7 已交付** |

**备注（P0 阶段登记，不扩大范围）**：
- 以上证据图片总量 52 张（frame_recorder 4 + theme_screencast 39 + viewport_shots 9），清单见 `docs/问题核查资料_20260908/p00/manifest.json`。
- 视觉结论一律待 gpt-5.6-luna（opentoken，`workflow agent(provider:"opentoken", model:"gpt-5.6-luna")`）判读；主代理/开发子代理不作 PASS/FAIL 结论。

## 六、修改前禁止事项

1. 不要把“已有 API/按钮”直接当成“用户问题已解决”，必须完成对应场景回归。
2. 不要在真实快照目录直接测试删除/清空/撤销。
3. 不要仅通过删除 loading 文案掩盖对比、扫描或网络未收敛。
4. 不要通过字段缺失推断缓存来源；来源应由后端明确返回。
5. 不要为满足自动扫描而删除所有手动恢复入口，除非失败与停止流程已有可用替代方案。
