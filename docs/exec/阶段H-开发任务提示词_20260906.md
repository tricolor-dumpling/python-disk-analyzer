# 阶段 H（=发布后维护批次：残留跟踪 + 环境基线对齐裁定 + 探针卫生 + 文档一致性收口）· 开发任务提示词

> 本提示词为阶段 G（=发布后挂账裁决落地与代码卫生批次）交付后，供下一轮执行代理直接使用的完整任务授权文档。
> 依据：阶段 G 交付报告（docs/exec/阶段G-交付报告_20260906.md）④ 未裁定/未授权清单 + 手册《问题整改_开发执行手册_20260902.md》壹章挂账表剩余项（P-6/P-7/P-8 环境/基线注记类）+ 第叁章阶段状态（阶段 G 已回写）+ 问题核查复现与修改定位清单.md 五-H（阶段 G 实测值）+ 阶段 A 需求决策记录的环境偏差注记（手册壹章/阶段A 状态行「待环境对齐」）+ 阶段 F/G 两轮均记录的 u62 连点组偶发（探针断言硬化候选）。
> 分支起点：`origin/stage-g`（阶段 G 已推送 cb33ecc，变更集 1–9 + 修复 1 独立可回退）。

---

## 一、角色定位

你（执行代理）= PDS 整改项目「阶段 H（=发布后维护批次）执行工程师」，是本阶段在本轮的唯一实现负责人。本阶段**不是新功能开发**，而是把阶段 G 交付后**仍开放**的挂账/未决项收口：残留跟踪（2-2 第八轮，登记不关闭）、环境基线对齐（阶段 A 起挂账七轮的「待环境对齐」注记，需用户裁定口径）、探针卫生（u62 连点组偶发断言硬化，本提示词授权）、文档一致性收口（README 占位符/手册挂账表复查/资料包），以及全量门禁维持性回归。手册说「禁止」的你不做，手册说「必须」的你不省略，手册未授权的你不擅自扩大。**阶段 H 无 UI 美化与功能扩展授权**：一切代码/文档改动必须能在交付报告中列出「为什么现在做」的授权依据（本提示词任务行 or 用户裁定记录）。

## 二、任务来源与授权范围

**任务来源**：
1. 阶段 G 交付报告 `docs/exec/阶段G-交付报告_20260906.md` ④「未裁定/未授权清单」——剩余仅 **2-2 视图残留（七轮未复现，继续跟踪）** 与 **真机 Everything 分钟级 busy（G8 环境注记，口径不变）**；阶段 F ⑥的 P-5/P-4/P-2/P-3 与 P13 已全部在 G 销项，**本阶段无功能类未决项**；
2. 手册壹章挂账表剩余项：**P-6**（u32⑧ DOM 节点 ±5 预存漂移，备注类）、**P-7**（G8 busy，环境注记）、**P-8**（沙箱 LOCALAPPDATA 写受限，环境注记）——本阶段**只复查登记，不实现**（P-7/P-8 为环境性，P-6 为探针口径注记）；
3. **环境基线对齐（阶段 A 起挂账）**：手册阶段 A 设计行冻结基线标注 `Python 3.14.3 / Flask 3.1.3 / node 25`，而实测本机 `Python 3.11.8 / Flask 3.0.2 / Node v24.19.0`——阶段 A 状态行已注记「已记录待环境对齐」，A–G 七轮沿用如实记录，从未落定；本阶段呈交用户裁定；
4. **u62 探针连点组偶发（F/G 两阶段均有记录）**：阶段 F 记录 rapid-450b 单次 themeOk 偶发（复跑 46/46 确认非回归）；阶段 G 记录 rapid-450c 两次同组 FAIL + stage-f 基线 rapid-450b 偶发（复跑 46/46 确认非回归）；根因分析=rapid 组连点两次（双 toggle），探针 target 按单次切换推导，450ms 间隔恰落 `--dur-theme-expand:450ms` 边界致断言误判——探针卫生候选；
5. **文档一致性收口**：README「阶段 G」段归档链接为占位符 `阶段G-交付报告_2026XXXX.md`（实际为 `_20260906.md`）——文号一致性缺陷，无条件修复；阶段 H 交付报告归档。

**授权代码范围**（按任务逐项放行，未列出的一律不碰）：

| 任务 | 前置条件 | 授权范围 |
|---|---|---|
| **H-1 环境基线对齐**（待用户裁定） | 用户裁定口径（①文档基线改以实测为准=推荐；②升级本机环境至手册标注版本=跨大版本高风险） | 裁定①→仅文档：手册阶段 A 设计/状态行基线注记更新 + README 环境说明 + 交付报告记录；裁定②→环境升级 + 全量门禁复核（snapshots.py ctypes/P13-FIX、Flask 3.1 行为、node 25 兼容面优先评估） |
| **H-2 2-2 第八轮跟踪**（无条件） | — | 只登记不实现：u50 view 段桩态（residualDetected 采样）+ u66 桩态（10 断言）+（若真机可行）真机复核；未复现登记「未复现」不关闭 |
| **H-3 文档一致性收口**（无条件） | — | README「阶段 G」段归档占位符改实际文件名；手册壹章挂账表 P-6/P-7/P-8 状态复查（备注/环境注记类确认，不新增挂账）；README/手册/阶段计划/核查清单五-I 与阶段 H 结果一致；资料包重新生成（可独立解压） |
| **H-4 u62 探针卫生**（本提示词授权） | 先复现（u62 连点组连跑 ≥2 轮记录偶发面）再动手 | 仅 scripts/dev/u62_theme_vt_probe.mjs：rapid 组 target 推导修正（双 toggle 语义：连点两次各生效时终态=startTheme）或终态断言前等待延长（600ms→≥800ms 覆盖 450ms 扩散 + VT 收尾余量，二者均不改应用代码）；独立变更集；验收=u62 连续 3 轮 46/46 无复跑依赖 |
| **H-5 全量门禁维持性回归**（无条件） | — | 全套门禁命令（见六）；P13 专项 50 轮（预期 0/50，若 >0 走纪律判定：复跑即绿不判失败，记录并核实是否 P13-FIX 回退） |
| **H-6 交付报告与文档收尾**（无条件） | — | docs/exec/阶段H-交付报告_*.md（六段结构）；核查清单追加五-I；README 阶段 H 段；资料包重新生成 |

**严禁触碰（除非上述授权明确放行）**：snapshots.py（P13-FIX 已落地于 G，**本阶段禁止再改**——H-1 裁定②的环境升级若触发锁语义问题只登记不修）、既有 14 接口语义（13 + compare/status 零变更）、SCAN_LOCK 全局锁对象、UI 布局/主题/动效（无授权）、已推送历史（stage-b/c/d/e/f/g 一律不改写，只能新增 revert/fix commit）、releases 产物入库（显式约定不入库，仅登记路径与体积）。

**每个落地项一个独立变更集、可独立回退**；从新分支 stage-h 开始（`git checkout -b stage-h origin/stage-g` 同源），禁止在 stage-g 上直接开发。

## 三、执行前必读

1. `docs/exec/阶段G-交付报告_20260906.md`（④ 未决清单 = 本阶段 2-2/G8 题源；① 门禁基线 317/317 + node 43 + u68 全绿；⑥ git 核验——本阶段基线与题源）+ `docs/exec/阶段G-开发任务提示词_20260905.md`（本提示词的格式与纪律母本，参照结构与红线——分阶段提示词同构，本提示词为续作）。
2. 手册全文（重点：壹章历史挂账表 P-1/P13 已核销+剩余 P-6/P-7/P-8 注记类、P-2…P-5/P-9 已销项；叁章阶段状态表阶段 G 行；肆章门禁与测试矩阵；伍章禁止事项 1–11；陆章定位索引/证据模板）。
3. 问题核查复现与修改定位清单.md 五-H（阶段 G 实测值，本阶段将追加五-I）+ 问题逐项核查报告_待修复.md「阶段F关闭矩阵」节（21 项最终标记）+ 问题整改阶段开发计划.md（阶段 G 段已回写，本阶段追加阶段 H 段）。
4. 阶段 A 需求决策记录 `docs/exec/阶段A-需求决策记录_20260902.md`（D1–D5 裁定全部落地无新增需求；**环境偏差注记**：手册阶段 A 设计行冻结基线 3.14.3/3.1.3/25 vs 实测 3.11.8/3.0.2/24.19——H-1 题源）。
5. scripts/dev/README_阶段A_夹具与探针.md（探针用法/判读通道）、u50_repro_probe.mjs + u51–u68 探针（全量同步回归，u68a–u68d 为阶段 G 新增）、tests/test_stage_g.py（阶段 G 契约）、u22_smoke_probe.mjs（断言面参照）、README「发布说明/已知限制/阶段 G」段（H-3 一致性对象）。

## 四、硬性要求（红线与纪律，违反即返工）

### 【第 0 条 · 最高目标（总纲，凌驾以下一切条款）】
以一遍过、不返工、高质量产出为最高目标；不需要考虑 token 和时间成本消耗；遇到不确定的问题随时向用户提问；需要任何权限，可提出（合理范围用户可允许）；开发结束请更新相关文档；开发结束后停掉后台任务；能够有更好的方式得到最终效果，就选择更好的方式，不需要考虑时间和金钱成本。结束进程前，一定要先确认进程是否是要暂停的进程，结束后会不会影响别的内容（尤其注意**不得触碰 Everything 进程与用户自带 Edge 实例**——探针/服务清理只限自己启动的进程）。

### 手册伍章红线（1–11 条全量执行）
- 严禁夹带：每个落地项一个独立变更集；禁止把 UI 美化混入功能修复；实现前先看「先复现/先核对」对应证据。
- **先复现/先核对清单（阶段 H 专属）**：H-1 先核对手册阶段 A 基线注记原文（设计行 260 行附近/状态行）与 README 环境说明现状；H-4 先连跑 u62 ≥2 轮记录偶发面（须与 F/G 两阶段记录同组/同现象核对——预期 rapid-450x msedge themeOk 偶发）；H-2 先核对五-H 与阶段 G 报告 2-2 记录——**每个落地项必须先有证据再动手**。
- 动画红线 P7：仅 transform/opacity；hex 门禁 0（tokens.css 豁免）；新增时长 token 必须走 tokens.css（本阶段无动效改动授权，若 H-4 涉及等待时长用探针常量不用 token）。
- 主题扩散动效验收红线 L0-1：任何改动不得拿掉 View Transition（reduced/不支持 VT 直切分支保留）；H-4 只改探针断言/等待，**不得改 settings.js/workspace.js 主题代码**。
- 主题/按钮/弹窗 ID 与既有 smoke 断言面保持不变（U1.3/U3.5）；**改 index.html 需重启 Flask**（模板缓存，debug=False）；CSS 改无需重启（ETag）。本阶段无 index.html/CSS 授权。
- 行为等价红线：改 browse/compare/save 等既有接口一律 additive（本阶段无后端接口改动授权）；switchTheme 签名与键值语义、ready/busy 契约、SCAN_LOCK 全局锁对象三根电线动前必须评估全链路（H-1 裁定②环境升级时优先评估，不评估不动）。
- 需求未决项不得实现：本阶段无 D 系列需求；H-1 未裁定前**不改任何环境基线文档**；P-6/P-7/P-8 只复查登记。
- 每个实现前先看「先复现清单」对应证据；「unittest 绿」不作为任何一条的通过依据（手册零章 #4）；P13 判定始终按「复现允许复跑」纪律——**但 G-6 已修复**，H-5 专项 50 轮预期 0/50，若 >0 先复跑确认非回退再登记，绝不掩盖。

### 【升级为独立强条款 · 视觉验收（必须创建 gpt-5.6-luna 子代理）】
阶段 H 的一切视觉证据判读（本阶段视觉项极少：H-2 截图关键帧若产出、H-4 无截图需求；任何「看起来对不对」的核验）必须创建 gpt-5.6-luna 子代理执行（DSH 唯一可指定模型通道：workflow 工具 `agent(prompt,{provider:"opentoken",model:"gpt-5.6-luna"})`）。判读须给**绝对路径**（Luna 工作目录与主代理不同，相对路径会误报「文件不存在」）；Luna 报告回写 问题核查复现与修改定位清单.md（追加五-I），主代理复核其结论后再定 PASS/FAIL——禁止主代理或默认模型（无图像能力）判读任何截图/录屏。

### 阶段 H 专属纪律
- **裁定先行**：H-1（环境基线对齐口径）第一步是**向用户呈交裁定请求**（现状证据：手册标注 vs 实测版本表 + 推荐方案①文档对齐 + 独立变更集计划），用户裁定后才落地；未裁定一律登记不实现。
- **H-4 探针卫生纪律**：只改 u62 探针断言/等待逻辑，**零应用代码改动**；先连跑 2 轮取证（复现偶发面，与 F/G 记录核对），改后 3 轮全绿且**不需要复跑即有 46/46**；不并入任何其它修复。
- **H-2 跟踪纪律**：2-2 第八轮 u50 view 段 + u66 桩态（+真机若可行），仍未复现登记「未复现」不关闭（红线：确定性复现不绿≠关闭依据）。
- **u20 网络序列纪律**：浏览/对比/扫描网络时序保持既有收口口径；console 不得有未处理 Promise/运行时错误；真机 Everything 分钟级 busy（G8）按「请求发起态」口径。
- **发布后一致性**：README 阶段 G 段占位符修正 + 阶段 H 段新增；手册挂账表 P-6/P-7/P-8 复查；资料包重新生成；交付报告归档。

### 【Git 版本管理纪律（与阶段 C…G 相同，强制执行）】
- **H-1 分支策略**：开工前从远端 origin/stage-g 最新父提交创建新分支 stage-h（`git checkout -b stage-h origin/stage-g` 同源），禁止在 stage-g 上直接开发；每个落地项独立变更集（commit），可独立 revert。
- **H-2 提交粒度与信息**：一个落地项一个 commit，格式「阶段H变更集N：X 摘要」+ 换行列出改动要点与验收证据；禁止夹带；实现前确认工作树干净。
- **H-3 推送纪律**：每完成 1–3 个变更集可推送 stage-h（`git push -u origin stage-h`）；推送前先 `git status` 核对只推本阶段改动，不得夹带 .venv/、dsh-image-gen/ 等本机产物（releases 产物不入库）；推送失败先诊断（git config http.proxy / 端口 7890 监听），不允许绕过代理直连 GitHub 反复重试。
- **H-4 远端同步核验**：每次 push 后 `git status -sb` 确认无 ahead/behind；阶段末最终 push 后核对 `git ls-remote --heads origin stage-h` 与本地 HEAD 一致。
- **H-5 回滚方案**：每个变更集独立可 revert；一旦发现污染提交立即停止后续开发先修复提交历史（rebase/amend 仅限未推送提交，已推送用 revert）。
- **H-6 禁碰已推送历史**：stage-b/c/d/e/f/g 与 stage-h 已推送 commit 一律不改写；需要修改只能新增 revert/fix commit；阶段交接口径以 git 历史为准。

## 五、阶段 H 任务清单（逐条验收）

### H-1（环境基线对齐，**待用户裁定**）
- **前置**：用户裁定口径；否则登记不实现（维持「待环境对齐」注记）。
- **现状证据（呈交用）**：手册阶段 A 设计行冻结基线 `Python 3.14.3 / Flask 3.1.3 / unittest 266 / node 25 / hex 0`；实测本机 `Python 3.11.8 / Flask 3.0.2 / Node v24.19.0 / unittest 317 / node 43`（A–G 七轮均如实记录，未落定）。
- **选项①（推荐）文档基线对齐**：手册阶段 A 设计/状态行基线注记改以实测为准（注明「阶段 H 裁定：以环境实测为文档基线」）；README 环境说明同步；零代码改动；验收=文档一致（手册/README/交付报告三处同源）+ 无功能影响。
- **选项②升级本机环境**：评估面=P13-FIX ctypes `CreateFileW`/`ReadFile` 在 Python 3.14 的表现、Flask 3.0.2→3.1.3 行为变化（含 Host 白名单中间件）、node 24→25 探针运行面；升级后全量门禁 + P13 专项 50 轮复核；风险=跨大版本回归面广，需完整小流程（先复现→实现→测试→验收→文档）。
- **验收**：按裁定选项完成 + 门禁全绿 + 交付报告记录裁定依据。

### H-2（2-2 第八轮跟踪，无条件）
- 执行：u50 view 段（桩态，residualDetected 采样）+ u66 桩态（10 断言）+（若真机可行）真机复核（Everything 就绪时）；仍未复现登记「未复现」不关闭。
- 验收：核查清单五-I 记录第八轮结论（与 F/G 七轮记录并列）。

### H-3（文档一致性收口，无条件）
- README「阶段 G」段归档链接 `docs/exec/阶段G-交付报告_2026XXXX.md` 占位符改实际文件名 `_20260906.md`（先核对实际文件与 commit）；手册壹章挂账表 P-6/P-7/P-8 状态复查（备注/环境注记类，确认无过期表述）；README/手册/阶段计划/核查清单五-I 与阶段 H 结果一致；资料包重新生成（可独立解压）。
- 验收：grep 全仓库无 `2026XXXX`/占位符残留；资料包 15+ 条目不缺项。

### H-4（u62 探针卫生，本提示词授权）
- **先复现**：连跑 `node scripts/dev/u62_theme_vt_probe.mjs` ≥2 轮，记录偶发组（预期 rapid-450x msedge themeOk 偶发，clipPath 空 + vtAnimations 0 + themeOk=false），与 F（rapid-450b）/G（rapid-450c）两段记录核对同现象。
- **实现（二选一或组合，零应用代码）**：①rapid 组 target 推导修正——连点 2 次各生效时终态=startTheme（双 toggle 语义），断言以「终态 ∈ {target, startTheme} 且 clipPath 空 + vtAnimations 0」为真值，或按 E-3 连点防护真实语义精确化；②终态断言前等待 600ms→800ms（覆盖 `--dur-theme-expand:450ms` + VT 收尾余量）。
- **验收**：u62 连续 3 轮 46/46，**无需复跑即全绿**；console 0；不动 stage-f/g 基线记录；独立变更集可 revert。

### H-5（全量门禁维持性回归，无条件）
- 全套门禁（见六）：unittest 317 / ResourceWarning / node 43 / py_compile / smoke 21/21 / u50–u68a-d 全量 / P13 专项 50 轮。
- 验收：全部绿；P13 若 >0 失败→复跑确认非 P13-FIX 回退后登记（绝不掩盖）；u62 若仍偶发→交给 H-4 证据链。

### H-6（交付报告与文档收尾，无条件）
- 交付报告 `docs/exec/阶段H-交付报告_*.md`（六段结构：①门禁结果表（317/317 + node 43 + u68 全绿基线续）+ ②H-1…H-6 逐条 PASS/FAIL/BLOCKED（未裁定项标 BLOCKED-待裁定）+ ③落地项逐条（授权依据/实现/测试/验收/Luna 判读（若有））+ ④未裁定/未授权清单（H-1 若未裁定 + 2-2 + G8 + P-6/P-7/P-8 注记类）+ ⑤H-1 裁定记录（若落地）/环境注记 + ⑥git 版本管理核验表）；核查清单追加五-I。

### 探针（本阶段无新增 u69 系列需求——H-2 复用 u50/u66，H-4 改造 u62；若执行中发现需要新探针的场景，先向用户呈报再建）

### 同步回归（不得破坏）
u22 smoke 21/21、u50 五段、u51–u68d 全量、node 纯函数 43 + 新增、unittest 317 基线 + test_stage_g（不得回归）、test_stage_b/c/d/f.py 不得回归、P13 纪律判定（专项 50 轮，预期 0/50）。

## 六、门禁与验收（肆章全跑 + 阶段 H 专属 + git 纪律）

```powershell
.venv\Scripts\python.exe -m unittest discover -s tests -t . -v            # 后端单元（317 基线 + test_stage_g 增量不变）
.venv\Scripts\python.exe -W error::ResourceWarning -m unittest discover -s tests -t .   # P13 纪律判定（G-6 修复后预期 0）
node --test-isolation=none --test "scripts/dev/*.test.mjs"                # 前端纯函数（43 + 新增）
.venv\Scripts\python.exe -W error::SyntaxWarning -m py_compile <16 个根 py> tests/test_stage_g.py  # 语法卫生
# 静态 8771（python -m http.server 8771 -d .）→ node scripts/dev/u22_smoke_probe.mjs v2
node scripts/dev/u50_repro_probe.mjs                                      # 阶段A 复现回归（五段；H-2 的 2-2 采样）
node scripts/dev/u51_poll_probe.mjs / u52 / u53                           # 阶段B 契约回归
node scripts/dev/u55_trend_probe.mjs / u56 / u57                          # 阶段C 回归
node scripts/dev/u58_auto_scan_probe.mjs / u59 / u60                      # 阶段D 回归
node scripts/dev/u61_scan_anim_probe.mjs / u62 / u63 / u64 / u65 / u66    # 阶段E 回归（u62=H-4 硬化对象）
node scripts/dev/u67_compare_leaf_probe.mjs / u67b / u67c / f3_acceptance_probe.mjs  # 阶段F 回归
node scripts/dev/u68a_calibre_annotation_probe.mjs / u68b / u68c / u68d   # 阶段G 回归
# P13 专项：.venv\Scripts\python.exe -W error::ResourceWarning -m unittest tests.test_budget（50 轮；G-6 修复后=维护性复核）
```

- 测试矩阵：每个落地项 × 适用层（单元/API 契约/前端逻辑/集成/真实浏览器）；视觉项（若有截图）全部 gpt-5.6-luna 判读。
- 验收标准：H-1 按裁定落地（或 BLOCKED-待裁定登记）；H-2 第八轮登记；H-3 无占位符残留 + 资料包完整；H-4 u62 3 轮全绿无需复跑；H-5 门禁全绿 + P13 0/50；浏览器 console 0；git 提交符合 H-1…H-6 纪律。

## 七、文档收尾与交付报告

**文档**：核查清单追加五-I（阶段 H 实测值，含 2-2 第八轮结论与 H-4 硬化前后对比）；手册壹章挂账表剩余项状态复查 +（若 H-1 裁定①）阶段 A 基线注记更新；README 阶段 G 段占位符修正 + 阶段 H 段新增；阶段计划追加「阶段 H」状态；资料包重新生成（可独立解压阅读）。

**交付报告**（docs/exec/阶段H-交付报告_*.md）：①门禁结果表（317 基线续 + u22/u50–u68 结果 + u62 硬化前后）；②H-1…H-6 逐条 PASS/FAIL/BLOCKED（命令/端口/截图证据路径；未裁定项标 BLOCKED-待裁定）；③落地项逐条（授权依据/实现/测试/验收）；④未裁定/未授权清单（H-1 若未裁定 + 2-2 + G8 + P-6/P-7/P-8 注记类，含现状证据与下一步）；⑤H-1 裁定记录（若落地）/环境注记；⑥git 版本管理核验表（每个变更集 commit hash、推送时间、远端同步状态）。

**收尾**：所有后台任务（Flask 服务、静态服务、浏览器/CDP 进程等）在交付前全部停止，并核对端口/进程已清理（只清自己启动的进程；**Everything 与用户自带 Edge 实例不触碰**）；git 状态核验（stage-h 已推送 origin/stage-h、工作树干净、无污染提交）。

**要点提示**：阶段 H 与既有阶段最大差异是——① **无功能类未决项**：阶段 F ⑥的 P-5/P-4/P-2/P-3 与 P13 已在 G 全部销项，本阶段任务来源=残留跟踪（2-2）+ 环境基线对齐裁定（H-1，阶段 A 起七轮挂账）+ 探针卫生（H-4，F/G 两轮记录同现象）+ 文档一致性（H-3，README 占位符为实测缺陷）；② **H-1 是唯一需用户裁定的任务**，第一步呈交裁定请求（现状证据=手册标注 vs 实测版本表 + 推荐①文档对齐），未裁定只登记；③ **snapshots.py 本阶段禁止再碰**（P13-FIX 已落地，P13 纪律=维护性复核 0/50，非修复通道）；④ H-4 只改探针断言/等待，零应用代码，先复现取证再动手；⑤ 视觉验收（若有截图）全部走 gpt-5.6-luna；⑥ 分支从 origin/stage-g（cb33ecc）创建 stage-h。

---

*编制：2026-09-06 · 基于阶段 G（cb33ecc 已推送）交付后的最新工作区与阶段 G 交付报告④未决清单、手册壹章挂账表剩余项、阶段 A 环境偏差注记、阶段 F/G 两轮 u62 偶发记录。*