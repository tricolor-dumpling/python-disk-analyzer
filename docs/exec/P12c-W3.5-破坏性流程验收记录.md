# P12c-W3.5 · 隔离数据目录破坏性流程验收记录

| 字段 | 内容 |
|---|---|
| 执行人 | ox-alpha（DeepSeek Harness 代理，经用户授权执行） |
| 日期 | 2026-08-25 |
| 脚本 | `scripts/dev/destructive_acceptance.ps1`（PowerShell 5.1 兼容，UTF-8 BOM） |
| 隔离根 | `D:\deepseek\dsa-isolated\20260825-195247\`（home 子目录承载重定向后的 %LOCALAPPDATA%） |
| 隔离方案 | 重定向 `%LOCALAPPDATA%` → 隔离根\home（零代码改动；`DSA_SNAPSHOT_DIR` 专用变量不存在，见手册 W3.5 偏差注记） |
| 服务端口 | http://127.0.0.1:5057（仅本机回环，继承隔离环境变量） |
| Python 环境 | 项目内 `.venv`（Python 3.14 + flask 3.1.3，见 W1.0 偏差注记） |

## 步骤结果（第 4 次执行 = 最终有效执行，前 3 次为脚本自身问题调试）

| # | 步骤 | 结果 | 证据 |
|---|---|---|---|
| 0 | 隔离闸门 | PASS | `get_data_dir() = D:\deepseek\dsa-isolated\20260825-195247\home\PythonDiskScanner`（≠真实 LOCALAPPDATA） |
| 1 | 隔离服务启动（含 bootstrap daemon 自动拉起 Everything） | PASS | 30s 内 `health.ready=True` |
| 2 | 全量扫描真实完整流程 + 保存快照 | PASS | roots_done=2/2；session_20260825_195319_*.json 落盘、`.snap.gz` ×2 |
| 3 | 台账/清单/快照文件齐备 | PASS | sessions=1, snapfiles=2 |
| 4 | undo（删快照 + 台账回滚） | PASS | deleted=2、undeleted=0；undo 后 `.snap.gz` 归零 |
| 5 | 指纹谓词恢复 | PASS | `should_auto_save('C:\\Probe', …)` → ok=true（不被旧指纹抑制） |
| 6 | wipe 双确认 | PASS | 错误确认 400 JSON；「确认清空」200 且 ok=true |
| 7 | 清键集合与目录重建 | PASS(部分) | 空 snapshots/ 与 exports/ 已重建（脚本断言）；localStorage 键核对需浏览器 DevTools 人工复核 |
| 8 | windowed 打包版 daemon 冒烟 | SKIP→补做计划 | 本机未安装 PyInstaller（打包链不在现场环境）；源码模式已在步骤 1 等价覆盖「引导态→daemon 自动拉起→≤15s 确定态」。补做计划：打包链就绪后按 README「打包为 exe」重跑 |

## 发现的缺陷（本次验收实际抓到并已修复）

| 缺陷 | 根因 | 处置 |
|---|---|---|
| wipe 任意请求返回 500（含正确确认） | W2.9 插入 Host 守卫时误留一个挂在 `/api/admin/wipe` 上的重复装饰器，使 `_guard_host` 被注册成该路由视图并 return None | commit `bcc1db2` 移除重复装饰器＋新增双确认回归测试（test_wipe_double_confirmation） |

## 挂账（非缺陷）

| 项 | 说明 |
|---|---|
| C 盘聚合口径偏差 ~22% vs 资源管理器 | 硬链接/逻辑尺寸固有差异，批次一闸门已归因；P13 候选工作项 |

## 现场清理确认

- 隔离根保留供人工复核：`D:\deepseek\dsa-isolated\20260825-195247\`（另有调试期产生的 194213 / 194535 / 194916 三个旧根，可用同款命令清理）
- 清理命令：`Remove-Item -Recurse -Force 'D:\deepseek\dsa-isolated\<ts>'` 或脚本 `-Cleanup`
- 真实 `%LOCALAPPDATA%` 全程未被写入（第 0 步闸门强制断言，每次运行均验证）
