# 阶段 G（=发布后挂账裁决落地与代码卫生批次）交付报告

> 日期：2026-09-06　分支：`stage-g`（从 `origin/stage-f` 3a4c051 创建）　执行：主代理 + gpt-5.6-luna 子代理（视觉判读）
> 变更集 1–8 + 修复 1（be2ad42 → 04972f4，共 9 个独立 commit，已推送 origin/stage-g，工作树干净无污染提交）。
> 阶段 G = 阶段 F 交付报告⑥未决项按用户裁定收敛（G-1 P-5 标注 / G-2 P-4 sparkline / G-3 P-2 下拉 / G-4 P-3 折叠）
> + G-5 P-9 代码卫生（本提示词直接授权）+ G-6 P13-FIX（用户显式授权，手册 P-1 禁碰纪律唯一例外通道）
> + G-7 跟踪登记（2-2 第七轮 / P13 纪律判定）+ G-8 发布收尾一致性。

---

## ① 门禁结果表

| 门禁 | 命令 | 结果 | 与 312 基线增量 |
|---|---|---|---|
| 后端单元 | `unittest discover -s tests -t .` | **317/317 OK**（skipped=1） | +5（`tests/test_stage_g.py`：total_by_root 派生口径/旧字段零变化/缺失损坏跳过/skipped 不派生/多会话序列） |
| ResourceWarning | `-W error::ResourceWarning unittest discover` | **317 OK** | +5 |
| 前端纯函数 | `node --test-isolation=none --test "scripts/dev/*.test.mjs"` | **43/43** | +5（motion-core sparklinePath/LastPoint 6 用例净增 5：43=38+5） |
| 语法卫生 | `py_compile` 16 文件 + test_stage_g | **exit=0** | cli.py 双口径：3.11.8 改前改后均 exit=0（如实记录与 F-5 一致）；3.14 面构造性零告警（本机无 3.14 如实注明） |
| smoke | `u22_smoke_probe v2`（静态 8771） | **21/21** | 断言面零破坏 |
| 阶段A 复现回归 | `u50_repro_probe`（五段，跑 2 轮） | **全完成 console 0** | view `residualDetected=false`（**2-2 第七轮桩态未复现**） |
| 阶段B–F 回归 | u51–u66/u67/u67b/f3 | **全绿** | u62 首轮 rapid-450c 单次 themeOk 偶发 → 完整复跑 46/46（stage-f 基线对照 rapid-450b 同现偶发 → 确认非本阶段回归，与 F 阶段 u62 rapid-450b 偶发先例一致）；f3 relate 懒展开首跑失败系空隔离环境无目录数据（真机走索引场景 10/10，见②） |
| **阶段G 新增探针** | u68a/u68b/u68c/u68d | **7/7 + 9/9 + 17/17 + 9/9** | P-5 标注存在性 / sparkline path 与差值卡一致 / 浏览历史下拉全链路 / 面包屑折叠 |
| P13 专项 | `-W error::ResourceWarning unittest tests.test_budget` 50 轮 | **0/50 失败** | G-6 修复验收：修复前 21/50（commit 记录）→ 修复后 0/50，不再依赖「复现允许复跑」纪律 |

## ② G-1…G-8 逐条结论

| 任务 | 状态 | 命令/证据路径 |
|---|---|---|
| **G-1** P-5 口径标注补齐 | **PASS** | 用户裁定「补齐」（阶段 G 裁决请求）；storage.js 图例行尾/chips title/aria-label + settings.js 数据目录区，文案=D5 裁定原文；u68a 7/7（桩态确定性 + 双浏览器）→ Luna 判读 `%TEMP%\u68a_calibre\*.png` 3 张（首判 FAIL 经 DOM 采样：textContent 完整、scrollWidth=1366 无溢出、rect.right=1337 → 复判 PASS） |
| **G-2** P-4 sparkline 数据源 | **PASS** | 用户裁定「补数据源+前端启用」；app.py `/api/snapshots` additive `total_by_root`（从快照文件派生，compare._total_from_root_rows 同口径）+ snapshots.js 趋势卡 sparkline；test_stage_g.py 5 用例 + u68b 9/9 → Luna `%TEMP%\u68b_sparkline_v2\*.png` 3 张（首判 FAIL 经 DOM 采样：pathD=M0 0 L60 14 L120 28 三顶点、终点 circle、暗终态 bodyBg 深色无覆盖 → 复判 PASS）；旧字段零变化（test_stage_g 断言逐键） |
| **G-3** P-2 浏览历史下拉 | **PASS** | 用户裁定「形态收口」；workspace.js 时钟按钮 → 下拉面板（最近 8 倒序、命令面板同款样式、键盘 ↑↓/Enter/Esc、外部点击关闭、<2 隐藏）+ style.css 新增类；u68c 17/17 → Luna `%TEMP%\u68c_history_v5\*.png` 2 张 PASS；smoke 21/21 断言面保持 |
| **G-4** P-3 面包屑折叠 | **PASS** | 用户裁定「折叠实现」；workspace.js renderBreadcrumb >6 层折叠（首+…+末两段，点击展开当次渲染瞬态）+ style.css .crumb-ellipsis；u68d 9/9（8 层夹具折叠/展开/回跳/键盘聚焦/窄屏/暗色/双浏览器）→ Luna `%TEMP%\u68d_breadcrumb_v4\*.png` 4 张 PASS |
| **G-5** P-9 代码卫生 | **PASS** | 本提示词授权；cli.py 2 处文档字符串非法转义改原始字符串 r"""（零功能改动，__doc__ 无运行时引用）；`py_compile -W error::SyntaxWarning cli.py` exit=0（3.11.8 改前改后均 0 如实记录）；test_cli 全绿 |
| **G-6** P13-FIX | **PASS** | 用户显式授权方案①；snapshots.py `_is_stale` CreateFileW(FILE_SHARE_DELETE) + `_release_lock` unlink 重试 3 次；专项 50 轮 **0/50**（修复前 21/50 对比记录见 commit a9c755c）；test_stale_lock/test_budget/test_undo 13/13 OK |
| **G-7** 跟踪登记 | **PASS（登记）** | 2-2 第七轮：u50 桩态 2 轮 residualDetected=false + u66 10/10 → 登记「未复现」不关闭；P13 已授权修复 → 50 轮纪律判定由「挂账」转「核销」（0/50） |
| **G-8** 发布收尾一致性 | **PASS** | README 已知限制段逐项销项（P-2/P-3/P-4/P-5/P-9/P13 ✅）+ 新增「阶段 G」发布段；手册壹章挂账表状态列更新（6 项 ✅ 已销项）；阶段计划追加「十、阶段 G」；核查清单追加五-H；资料包重新生成（见⑥）；交付报告本文件 |

**验收修复 2 件套（独立变更集 8，走完整小流程：先复现→实现→测试→验收→文档）**：

| 变更集 | 复现 → 修复 → 测试 → 验收 |
|---|---|
| 8a（04972f4） | relate 路径栈对齐死循环：复现=u68c 第三次浏览（非下钻跳转）主线程忙循环卡死（tick 停止增长 + Debugger.pause 栈顶 browsePath:843）；**stage-f 基线同复现**（stage-b 既有缺陷）；根因=`getPathStack()` 副本 + `popPath()` 改模块数组 → while 副本 length 永不减少；修复=实时取栈 + 保险丝 64；测试=u68c 17/17 + smoke 21/21 + u50 五段 + u56 16/16 回归全绿 |
| 8b（04972f4） | 面包屑展开态粘滞：复现=u68d ④ 失败（点击「…」展开后同路径重渲染不复位）；修复=非折叠渲染一律复位（P-3 语义：展开=当次渲染瞬态）；测试=u68d 9/9 |

## ③ 落地项逐条（授权依据 / 实现 / 测试 / 验收 / Luna 判读）

| 落地项 | 授权依据 | 实现文件 | 测试 | 验收证据 |
|---|---|---|---|---|
| G-1 P-5 标注 | 用户裁定「补齐」（D5 定稿=逻辑尺寸+UI 全处标注，F-5 已登记缺口） | web/static/js/app/components/storage.js（CALIBRE_NOTE 导出 + 图例行尾 + chips title + aria-label）、settings.js（.calibre-note 幂等注入）、style.css（2 新增类） | u68a 7/7 + node --check | Luna PASS（复核改判）+ DOM 采样完整 |
| G-2 sparkline | 用户裁定「补数据源+前端启用」 | app.py（/api/snapshots additive total_by_root）、snapshots.js（collectDriveTotals/trendSparkline）、motion-core.js（sparklinePath/LastPoint）、style.css（.trend-spark*）、motion-core.test.mjs（+6 用例） | test_stage_g.py 5 用例 + u68b 9/9 + node 43/43 | Luna PASS（复核改判）+ 旧字段零变化断言 |
| G-3 历史下拉 | 用户裁定「形态收口」 | workspace.js（#btn-browse-history + 下拉面板 + 键盘 + 外部点击）、style.css（.browse-history-panel） | u68c 17/17 + smoke 21/21 | Luna PASS + 键盘全链路 |
| G-4 面包屑折叠 | 用户裁定「折叠实现」 | workspace.js（renderBreadcrumb fold/crumbExpanded）、style.css（.crumb-ellipsis） | u68d 9/9 | Luna PASS + 键盘聚焦 + 行为等价 |
| G-5 P-9 卫生 | 本提示词显式授权 | cli.py（2 处 r"""） | py_compile 双口径 + test_cli | exit=0（3.11.8/3.14 面记录） |
| G-6 P13-FIX | 用户显式授权方案① | snapshots.py（_read_lock_pid_win + _is_stale + _release_lock 重试） | P13 专项 50 轮 + test_stale_lock/budget/undo 13/13 | 0/50（修复前 21/50） |

## ④ 未裁定/未授权清单（登记不实现）

| 项 | 现状证据 | 下一步 |
|---|---|---|
| 2-2 视图残留 | 七轮（A–G）未复现：阶段 G u50 桩态 2 轮 residualDetected=false + u66 桩态 10/10（真机 5/5 于阶段 E） | 继续登记「未复现」不关闭（红线：确定性复现不绿≠关闭依据）；若后续真机高强度场景复现再开专项 |
| 真机 Everything 分钟级 busy（G8） | 环境常态（SDK 直扫分钟级）；compare 无缓存走 202 异步 + 前端 30s 超时已兜底 | 按「请求发起态」口径记录，不列为缺陷 |

> 阶段 F 交付报告⑥的 P-5/P-4/P-2/P-3 四项未决已全部经用户裁定落地（G-1…G-4）；P13 经显式授权修复（G-6）。本阶段无新增未决项。

## ⑤ P-9 双口径记录（G-5）

| 环境 | 命令 | 改前 | 改后 |
|---|---|---|---|
| Python 3.11.8（本机现状） | `.venv\Scripts\python.exe -W error::SyntaxWarning -m py_compile cli.py` | exit=0（不告警，与 F-5 记录一致） | exit=0（零告警） |
| Python 3.14（告警面差异目标） | 同命令（本机无 3.14 环境） | 预期 2 处 SyntaxWarning（`exports\\` 非法转义） | 构造性零告警（raw string 消除非法转义源） |

> 如实说明：3.11.8 下改前改后均 exit=0（SyntaxWarning 在 3.12+ 才成为常规告警面），3.14 面为理论差异——本机无 3.14 解释器，以「源码层消除非法转义」为验收事实，与 F-5 记录口径一致。

## ⑥ Git 版本管理核验表（G-1…G-6）

| 项 | 结果 |
|---|---|
| G-1 分支 | `stage-g` 从 `origin/stage-f`（3a4c051）创建；未在 stage-f 上直接开发（仅用 stage-f worktree 做基线对照，已移除） |
| G-2 提交粒度 | 9 个独立 commit（变更集 1–8 + 修复 1），每任务一 commit、正文列改动要点与验收证据：be2ad42（G-5 卫生）/ b5fdebf（G-1 标注）/ 63060f7（G-2 sparkline+test）/ 6c5f9c6（G-3 下拉）/ 384320e（G-4 折叠）/ a9c755c（G-6 P13-FIX）/ 60e6c24（G-3 模板字符串修复）/ 84a852c（变更集 7 u68 探针）/ 04972f4（变更集 8 验收修复 2 件套） |
| G-3 推送纪律 | 推送前 git status 核对仅本阶段改动（.venv/dsh-image-gen/releases 未夹带）；代理 7890 正常 |
| G-4 远端同步 | push 后 `git status -sb` 无 ahead/behind；最终 `git ls-remote --heads origin stage-g` 与本地 HEAD 一致 |
| G-5 回滚方案 | 每个变更集独立可 revert（be2ad42→04972f4 线性可逆；修复提交 60e6c24 为新增 commit 非改写） |
| G-6 禁碰历史 | 未改写 stage-b/c/d/e/f 或 stage-g 已推送历史；G-3 模板字符串错误以新增 fix commit（60e6c24）修正 |

## ⑦ 文档收尾清单（G-8）

- README「发布说明」：已知限制段 P-2/P-3/P-4/P-5/P-9/P13 逐项销项（✅）+ 新增「阶段 G（2026-09-06）」发布段（落地项/验收修复/门禁/视觉验收/已知限制更新）。
- 手册壹章挂账表：P-1(P13)/P-2/P-3/P-4/P-5/P-9 状态列更新（✅ 已核销/已销项）；叁章阶段状态追加「阶段 G」行。
- 问题核查复现与修改定位清单：追加 **五-H**（阶段 G 门禁表/G-3/G-4 验收修复/Luna 判读表/环境与遗留）。
- 阶段计划：追加「十、阶段 G」段（授权/落地项/门禁/遗留）；原「十一、阶段日志模板」顺延为「十二」。
- 资料包：重新生成（docs/问题核查资料_20260902/ 可独立解压阅读）。
- 交付报告：本文件。

---

## 收尾核验

- 后台任务全部停止：Flask 5000 / 静态 8771 / stage-f 对照 5001/5002（worktree 已移除）全部 kill；netstat 确认 5000/8771/5001/5002 无监听。
- Everything 进程未触碰。
- 工作树干净，`stage-g` 与 `origin/stage-g` 同步无 ahead/behind；无污染提交；调试脚本（_debug_*.mjs）已清理。

*执行记录：2026-09-06 · 阶段 G · 主代理执行 · 视觉判读经 workflow `agent(...,{provider:"opentoken", model:"gpt-5.6-luna"})` 判读 u68 共 12 张截图（2 项首判 FAIL 经 DOM 客观采样复核改判 PASS，主代理复核一致）。*
