# 阶段 P0 交付报告（基线冻结与视觉验收基建 · 2026-09-08）

> 分支：`stage-p0`（自 `main @ 17c036c` 创建）　tag：`p0-visual-harness`
> 执行：P0 阶段开发子代理（唯一开发代理，leader 长期对话）　零生产代码改动
> 覆盖：P0-1…P0-6 六项任务全部 PASS；门禁数字全部实测，不照抄文档

---

## 一、范围与裁定

- **P0-1 未跟踪文件处置（leader 已裁定，本阶段登记执行）**：
  - `问题/`（问题清单.md + img/ 2 张截图）已在 commit `0fd7c95` 入库为**已跟踪文件**——不移动、不删除，保持原位；
  - `问题.zip`、`docs/问题核查资料_20260902.zip` 已在 `.gitignore`（45/47 行）——维持 ignore；
  - 当前工作树仅剩 `docs/exec/_p0_*` 5 个 leader 管理的工作文件（`git status` 可见，交付前由 leader 统一清理）；
  - 无新增未跟踪文件需要登记。
- **不在范围内**：任何生产代码改动（`app.py`、`web/**`、`*.py` 零改动）；`tests/**` 零改动；问题 1–10 的修复（属 P1–P8）。
- **环境现实（如实登记，不掩盖）**：
  1. `u22_smoke_probe v2` 实测 **20/21，A1 FAIL**——本机 chromium-1234 下 ViewTransition 从触发到 `data-theme` 生效实测 **约 593ms** > A1 分支②的 80ms 断言窗口（阶段 H 基线 21/21 是在不同环境）。已三重验证与本阶段变更无关：① 不经 `_harness.mjs` 的独立 chromium 运行同样失败；② 前端 `web/` 零改动；③ 真机 5000 点击 `#btn-theme` 复现 593ms 延迟。**判为既有环境挂账**（P2/P7 阶段如需收紧可后续评估；本阶段按纪律不改测试断言）。
  2. `favicon.ico` 404：项目 `web/` 无 favicon 文件，真机 5000 下所有探针的 console 断言被此既有杂音触发失败（**功能断言全部 PASS**）。环境事实，登记不改。

## 二、实现

| 变更集 | commit | 提交信息 | 涉及文件（关键） |
|---|---|---|---|
| 1 | `eeaf4e3` | 阶段P0变更集1：视觉验收工具链 `_harness.mjs` + 三支 p00 脚本 + 探针引入路径统一 | `scripts/dev/_harness.mjs`、`scripts/dev/p00_frame_recorder.mjs`、`scripts/dev/p00_theme_screencast.mjs`、`scripts/dev/p00_viewport_shots.mjs`、`scripts/dev/refactor_probe_imports.mjs`、`scripts/dev/gen_manifest.mjs`、`scripts/dev/u2x–u6x` 41 支探针、`scripts/dev/README_阶段A_夹具与探针.md` |
| 2 | `e2c8693` | 阶段P0变更集2：P0-4 隔离夹具三类扩展（growth/flat/series） | `scripts/dev/fixture_snapshots.mjs` |
| 3 | `ee237bc` | 阶段P0变更集3：2026-09-08 复现清单与交付模板 | `docs/问题核查复现与修改定位清单.md`（五-J）、`docs/exec/阶段P-交付报告模板.md` |
| 4 | `63e961d` | 阶段P0变更集4：取证证据入库 + 门禁基线 | `docs/问题核查资料_20260908/p00/**`（manifest 等） |
| 5 | `828815c` | 阶段P0变更集5：计划文档 P0 状态回写 | `docs/后续开发计划_用户实测问题10项_20260908.md` |
| 6 | `cdaa4a9` | 阶段P0变更集6：交付报告 | `docs/exec/阶段P0-交付报告_20260908.md` |
| 7 | `71d6b8b` | 阶段P0变更集7：P0-3 修正帧记录器像素证据 + theme 圆心量化 + timeline 排序（返工1/2/3） | `scripts/dev/_harness.mjs`、`scripts/dev/p00_frame_recorder.mjs`、`scripts/dev/p00_theme_screencast.mjs`、`scripts/dev/gen_manifest.mjs`、`scripts/dev/README_阶段A_夹具与探针.md`、`docs/问题核查资料_20260908/p00/**`、`docs/exec/阶段P0-交付报告_20260908.md` |
| 8 | `本次变更集8` | 阶段P0变更集8：P0 交付报告与证据清单登记 Luna 复判结论与 P7 补强项（manifest + 交付报告） | `docs/问题核查资料_20260908/p00/manifest.json`、`p00/theme_screencast/circle_fit.json`（meta valid_range）、`docs/exec/阶段P0-交付报告_20260908.md` |

- **实现说明**：
  - `_harness.mjs` 用 **Proxy 包装 `chromium.launch`**：未传 `executablePath` 且未传 `channel` 时自动注入 `CHROMIUM_EXE`（默认 chromium-1234 路径，`PDS_CHROME` 覆盖）；`channel:"msedge"` 时不注入。既有探针 `chromium.launch({headless:true})` 等调用**零改动**即可跑通。
  - 41 支既有探针（u13–u68d + f3 + u22 smoke 等）硬编码 `C:/Users/26024/...` 全部改为 `import { chromium, launch } from "./_harness.mjs"`；`refactor_probe_imports.mjs` 为一次性可复跑工具（`--check` 预览）。**断言逻辑、采样节奏、阈值零改动**（git diff 证明见三）。
  - `p00_frame_recorder.mjs`：**DOM 帧（rAF ≈16.7ms）+ 像素帧（CDP screencast）双轨同步记录**，同一轮序列内采集、按相对触发点时间戳对齐（双时钟锚点）；输出 frames.json + summary.json + `screencast/` 像素帧 + `violation-window/` 违规窗口像素副本 + 终态静态图。**像素级证据一律 CDP screencast**（`page.screenshot`≈130ms/张无法覆盖 33ms 级窗口，仅作终态图、文件名不含帧时间戳）。
  - `p00_theme_screencast.mjs`：CDP `Page.startScreencast` 逐帧 JPEG + 亮度/暗区面积曲线 + **点击坐标 + 每帧暗区边界圆拟合**（clickCoord 记录于 meta.json；circle_fit.json 输出 center/radius/radial_rms=dev_from_click_px/arc；判据 |圆心−点击|≤4px）。
  - `p00_viewport_shots.mjs`：三视口（1366×768 / 1440×900 / 1920×1080）× 三页静态截图。
  - `fixture_snapshots.mjs` additive 新增 `growth` / `flat` / `series`（`--fixture all|growth|flat|series` 选择）；既有五类语义与文件名不变（含 `--fixture all` 一并生成）。

## 三、测试

门禁基线表（全部实测）：

| # | 命令 | 实际数字 | 耗时 | 结论 |
|---|---|---|---|---|
| 1 | `.venv\Scripts\python.exe -m unittest discover -s tests -t . -v` | **Ran 317 tests / OK** | 2.017s | 绿 |
| 2 | `python -W error::ResourceWarning -m unittest discover -s tests -t .` | **OK（317）** | 2.7s | 绿 |
| 3 | `python -m py_compile app.py cli.py datadir.py env.py sdk.py scan.py tui.py utils.py exceptions.py keyrouter.py messages.py snapshots.py compare.py fullscan.py session.py main.py` | **exit 0** | — | 绿 |
| 4 | `node --test scripts/dev/*.test.mjs` | **43 pass / 0 fail** | 195.4ms | 绿 |
| 5 | 静态 8771 + `u22_smoke_probe v2` | **20/21**（A1 FAIL） | — | 既有环境挂账（见一，非本次回归） |
| 6 | `u66_view_residual_real --browsers chromium` | **5/5 PASS** | — | 绿（改造后跑通） |
| 7 | `p00_frame_recorder --repeats 3`（双轨） | 最差轮 **88 DOM 帧 / 9 违规 / badOff=[11,144]ms**；16 像素帧、**8 落违规窗口（8/8 unique）** | — | **硬闸门通过**（对照 P2 63/9；像素轨为返工1 CDP 证据） |
| 8 | `p00_theme_screencast`（v2） | 105 帧 + 曲线 + **circle_fit：88 帧可拟合，偏差 min=1.61 max=2.28 均值=1.68px**；light→dark 已切换；console 0 | — | 绿（返工2/3 完成） |
| 9 | `p00_viewport_shots` | 9 张 PNG + meta.json | — | 绿 |

- **既存挂账（登记，不修）**：
  - `tests/test_budget.DayBudgetTests.test_concurrent_saves_serialize_accounting` 单轮实测 `Ran 1 test in 0.109s / OK`（未触发竞态）；该竞态复跑即绿=既有，非本阶段回归；
  - Everything 真机 busy 口径：本阶段全程未做真机浏览耗时对比，仅在检查进程时确认其运行（2 进程）；
  - smoke A1（VT 时序）与 favicon 404：见一，均判为既有环境行为。
- **探针改造验证（引入路径改造后）**：u50 exit 0（四段全跑通）；u66 5/5；u56/u58 功能断言全部 PASS（console 断言仅被 favicon 404 触发，非功能问题）；41/41 语法检查通过；`git diff` 确认仅 import 行变化。
- 环境实测：Python 3.14.3 / Flask 3.1.3 / Node v24.19.0 / Everything 2 进程（PID 17812、21752）/ 端口 5000（本会话 Flask PID 20616）/ 8771（本会话静态 PID 13532）/ chromium-1234 存在 / playwright 1.63.0。

## 四、视觉验收

证据清单根：`D:\deepseek\python-disk-analyzer\docs\问题核查资料_20260908\p00\manifest.json`（6 组、139 张图（151 unique 字节中的 139 实体），逐项绝对路径/用途/采样节奏/时间窗/量化摘要；供 gpt-5.6-luna 逐张判读）。

**Luna 判读两轮结论（gpt-5.6-luna，2026-09-08）**：一轮逐张打开 52/52 张（详 `docs/exec/_p0_luna_verdict.md`，交付前删）→ 总体 WARN；二轮（`_p0_luna_rejudge_prompt.md`，交付前删）**复核返工后的违规窗口像素帧与 theme circle_fit**。二轮结论如实登记如下：

| 组 | 绝对路径 | 视口 | 采样节奏 | 时间窗 | 量化摘要 | Luna 判读（两轮） |
|---|---|---|---|---|---|---|
| frame_recorder DOM | `p00/frame_recorder/frames.json` + `summary.json` | 1366×768 | rAF ≈16.7ms/帧 | 触发前 100ms → 动画 + 200ms | 最差轮 88 DOM 帧 / 9 违规 / badOff=[11,144]ms（对照 P2 63/9） | 一轮：DOM 量化有效；二轮：**PASS**（与 P2 门禁口径一致，与像素轨互相印证） |
| frame_recorder 像素 | `p00/frame_recorder/screencast/` + `violation-window/` | 1366×768 | CDP screencast（修复后 ts 单调不减） | 触发前 → 动画+200ms | 16 像素帧；**8 张落违规窗口 [11,144]ms**，8/8 unique，7/7 区别于基线（off=+11ms=合成器滞后帧==基线，如实标注） | 一轮：关键帧 4 张同 SHA → **MISMATCH（返工1）**；二轮：**PASS（硬闸门成立）**——+78/+80ms 起可见 treemap 矩形压于目录/关系区，随后逐帧变淡，与 twOpacity 递减及窗口一致；上下文帧（-71…-17ms 无残留、+153…+183ms 消失）吻合 |
| theme_screencast | `p00/theme_screencast/`（含 circle_fit.json） | 1366×768 | CDP screencast（ts 单调不减，rawTs 保留） | 1800ms 覆盖 450ms 扩散+收尾 | 105 帧；light→dark；暗区 0.09→0.63；**88 帧可拟合；可靠区间 valid_range=ts 221–439ms（7 帧）radial_rms min=1.69 max=2.28 mean=1.82px（全部 ≤4px）**；点击坐标=(995,30) | 一轮：平滑扩散/无触底跳变 PASS、圆心 WARN（无坐标无拟合）；二轮：**有条件接受**——圆心视觉位于右上点击区≈(995,30) 与 radial_rms 一致，但 radial_rms 以点击点为固定圆心、**非独立圆心估计**（独立拟合归 P7） |
| viewport_shots | `p00/viewport_shots/` | 三视口×三页 | page.screenshot（≈130ms/张） | 每态 1 张 | 9 张 | 一轮 + 二轮：**9/9 PASS**（无硬切/重叠/溢出） |
| fixtures | `p00/fixtures/fixture_validation.json` | — | 静态数据 | — | load 19/19；growth 混合增量+4 层深；flat 全 0；series 单调递增；session 15 | 数据层（无需 Luna） |
| baseline | `p00/baseline/gate_baseline.json` | — | — | — | 门禁基线表 + 环境挂账登记 | — |

**timeline 排序修复（返工2）**：`_harness.screencast()` 增加 ts 单调夹取（按 seq 稳定序 + 单调不减，保留 rawTs）。修复前（旧 evidence）：`seq8 ts=160 → seq9 ts=140`（1 处非单调，Luna 指出）；修复后（新 evidence）：`seq8 ts=195 → seq9 ts=198 → seq10 ts=221`，**105 帧 0 非单调**，`ts_clamped=false`（本轮无抖动即天然单调）。

**工具链自证（P0 硬闸门，返工1 后）**：帧记录器「排行→关系」违规帧（DOM + 像素双轨）与 P2 基线语义对齐：

| 序列 | DOM 帧数 | DOM 违规帧 | 违规窗口 | 窗口内像素帧 | P2 基线（rank2relate） |
|---|---|---|---|---|---|
| ranking2relate | 88 | 9 | [11,144]ms | 8（8/8 unique） | 63 帧 / 9 违规 / 首违规 ts=9946（绝对时间戳，口径不同不可直接比） |

- 技术偏差登记：CDP screencast 实测平均 ~30–46ms/帧（本机负载相关），满足「捕捉 450ms 扩散全程 + 收尾」；rAF 帧记录稳定 ~16.7ms；`page.screenshot`（≈130ms/张）**不可**用于 33ms 级窗口，本阶段像素级证据一律 CDP screencast，`page.screenshot` 仅作终态静态图（`terminal-relate.png`，文件名无时间戳）。
- **问题2 已知对齐差异（必须登记）**：窗口内 `+11/+28ms` 两帧像素与触发前基线帧相同（合成器滞后）——**DOM 状态变化早于可见像素**；像素可辨识残留始于 `+78ms`（manifest `frame_recorder_pixel` 组 note 已写明）。
- **问题10 circle_fit 量化矛盾（已查明并登记）**：`circle_fit.json` 自 ts≈495–528ms 起拟合半径**饱和 529–531px**，而同期 `darkFrac` 已达 **0.976**（圆心 (995,30)、半径 530px 的圆不可能覆盖 97.6% 视口）→ **暗区边界检测在后期失效**（darkFrac≥0.95 后 clip-path 已结束，边界检测锁定到非圆弧边缘，ts≥1618 半径还跌至 458px 为同源伪迹）。**可靠拟合帧区间 `valid_range = ts 221–439ms`（7 帧，darkFrac 0.153–0.940），区间内 radial_rms_px min=1.69 / max=2.28 / mean=1.82**（已写入 `circle_fit.json` meta 与 manifest）。
- **问题 4「扫描中右栏纵向挤压」Luna 观察核实**：未能定位到对应画面——结构性原因：P0 三支探针（viewport/theme/frame）**均未触发扫描**（桩态 fetch 恒返回 running:false，无扫描点击），右栏恒为待机/浏览态，P0 证据中**不存在扫描中右栏图**；该观察若出自证据帧，最接近的仅是右栏静态布局（viewport_shots 工作台图），非扫描态。扫描态动态截图归 **P3**（见 §五）。
- 视觉结论按纪律经 `workflow agent(provider:"opentoken", model:"gpt-5.6-luna")` 通道判读；本报告如实收录 Luna 两轮结论（9/9 PASS；问题2 PASS；问题10 有条件接受；关键帧 MISMATCH 已返工）。

## 五、遗留与挂账

| 项 | 说明 | 下一步 |
|---|---|---|
| smoke A1（VT 80ms 断言） | chromium-1234 下 VT 完成 ~593ms，A1 分支② 80ms 窗口过紧；既有环境行为，非本次回归 | P2/P7 若需收紧可评估；按纪律不改测试 |
| favicon 404 | 项目无 favicon，真机探针 console 断言受扰 | 可后续补 favicon 或探针过滤；本阶段不改 |
| `test_budget` 并发锁竞态 | 既有挂账，本轮未触发 | P1–P8 均不得顺手修复（计划红线） |
| 主题 screencast 帧间隔 | 平均 ~30–46ms/帧（负载相关） | P7 圆心拟合需更高帧率时可单独压测 |
| **问题10 圆心拟合：条件性证据（P7 必补）** | Luna 二轮有条件接受：radial_rms 以点击点为固定圆心，**非独立圆心估计**；且 darkFrac≥0.95 后边界检测失效（valid_range=ts 221–439ms）。**P7 必须补：①边界像素筛选规则（排除矩形图/裁切干扰）②覆盖角度分布 ③不依赖点击点的鲁棒圆心拟合（全弧/部分弧分别处理）④底部触底关键帧** | P7 `p07_theme_pixel_probe.mjs` |
| 问题 10 三路径圆心缺陷 | 顶栏路径圆心已量化（valid_range 内 radial_rms 1.69–2.28px）；键盘/命令面板/设置慢点击三路径仍为 P7 修复对象 | P7 `p07_theme_pixel_probe.mjs` |
| 问题 4「扫描中右栏纵向挤压」动态截图 | P0 证据**未定位**到扫描中右栏画面（结构性：P0 三探针均未触发扫描）；该观察归于 **P3 范围，P0 不补** | P3 提供扫描态动态截图 + 三视口扫描中布局 |

## 六、回滚与进程清理

- **回滚**：`git revert` 或 `git reset --hard p0-visual-harness`；回滚后重跑门禁最小集（py_compile + node test）。本阶段全部为**新增文件/工具/文档**，回滚=删除新增，不影响生产。
- **清理前进程/端口清单**：

```text
# Get-NetTCPConnection -LocalPort 5000,8771 -State Listen
LocalPort  OwningProcess
5000       20616   （本会话 Flask app.py --no-browser）
8771       13532   （本会话静态 http.server -d .）

# Get-Process python,node,chrome,chromium,Everything
Id      ProcessName   StartTime           （节选）
20616   python        <本会话启动>          Flask 服务
13532   python        <本会话启动>          http.server 8771
17812   Everything    <用户进程，不触碰>
21752   Everything    <用户进程，不触碰>
```

- **清理动作**：仅停止本会话启动的 20616（Flask）、13532（静态 8771）；Everything 保持。探针产物：证据已入 `docs/问题核查资料_20260908/p00/`（入库），`%TEMP%\u*_p0`、`%TEMP%\pds_p0_fixtures*`、`%TEMP%\pds_fixture_snapshots_*` 等自建临时目录清理。
- **清理后进程/端口清单**：（见收尾核对，端口 5000/8771 应无监听；Everything 保持 2 进程）

---

*编制：2026-09-08 · P0 阶段开发子代理 · 分支 `stage-p0` · tag `p0-visual-harness` · 零生产代码改动*