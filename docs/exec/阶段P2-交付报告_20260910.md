# 阶段 P2 交付报告（工作台视图切换残留 · 问题 2）

> 阶段：P2（工作台视图切换残留）　日期：2026-09-10　分支：`stage-p2`（起点 `p1-autosave` @ `750176a`）
> 覆盖问题：#2　前置阶段：P0（视觉验收基建）、P1（问题 1）
> 执行者：**主代理本人**（未创建任何子代理；`subagent*` / `workflow` 全程未使用）

---

## 一、范围与裁定

- **覆盖范围**：
  - 问题 2「切到排行/关系后矩形图闪透」的根因修复（R1 方向判定 / R2+R3 层序语义 / R4 暂停与 hover 卫生）；
  - 帧级探针正式化（`p02_view_frame_probe.mjs`）并作为**唯一关闭依据**；
  - A/B/C 三类视觉证据入库 `docs/问题核查资料_20260908/p2/`（`manifest.json` 登记 5 证据项 / 11 张图片）；
  - 文档回写：README（问题 2 条目 + **新增「D2-4 关闭口径」章节**）、复现清单问题 2 状态、计划文档第八章 P2 状态与 DoD 实测对照。
- **冻结口径（D2-1…D2-4，直接执行）**：
  - **D2-1** 保留「矩形图参与时」的 120ms 交叉淡化，方向判定改「上一视图 vs 下一视图」；
  - **D2-2** 补齐 `.v-crossfade` 的 CSS 语义（入向在上 + `pointer-events` 正确）并核对与 `#browse-loading`(z-index:5)、全屏态的层序；
  - **D2-3** `treemap.pause()` 真暂停（断重绘、收束终帧、暂停期间不响应 hover/指针）；
  - **D2-4** 关闭依据只能是帧级断言（已写入 README，见 §四 与交付报告 §六）。
  - **禁止项全部遵守**：未用「120ms → 0ms」、未关闭任何转场、未删除 `.v-crossfade` 类（改为补齐规则）、未改 `--dur-1` 等令牌、未引入 `[hidden]`/`.hidden` 之外的第二套可见性语义。
- **用户/leader 裁定记录**：用户 2026-09-10 指示本阶段全部任务由**主代理自行完成、不创建任何子代理**；视觉判读由主代理本人 `read_image` 逐张执行。
- **不在范围内**：问题 3/4（P3 右栏排版与密度开关）、问题 5/6（P4 对比深度）、问题 7（P5）、8/9（P6）、10（P7）——均未触碰。
- **阶段中新增的授权外产物（如实登记，供审阅裁定）**：
  - `scripts/dev/p02_b_screencast.mjs`、`scripts/dev/p02_c_viewport_shots.mjs`、`scripts/dev/p02_manifest.mjs`——三者是 §五 视觉验收 A/B/C 三类与 `manifest.json` 的**取证/清单工具**，属计划「证据入库 `docs/问题核查资料_20260908/p2/`」交付物的必要工具链；均在 `scripts/dev/`（非生产代码）。
  - 未改动 `scripts/dev/_harness.mjs`（过程中曾试加 `everyNthFrame` 参数，因超出授权清单**已 revert**，改用页内心跳维持合成器损伤达成同等效果）。

## 二、实现

| 变更集 | commit hash | 提交信息 | 涉及文件 |
|---|---|---|---|
| 1 | `2f3f0cc` | 阶段P2变更集1：帧级探针正式化 + 修复前基线复现（红线A自证 166/497 违规帧） | `scripts/dev/p02_view_frame_probe.mjs`（新）、`docs/问题核查资料_20260908/p2/baseline_prefix/*`、`docs/问题核查资料_20260908/p2/_datadir_baseline_prerun.json`、`docs/exec/阶段P2-执行计划_20260910.md` |
| 2 | `1ce4ecb` | 阶段P2变更集2：R1 视图切换方向判定 + R4 hover 可见性守卫 | `web/static/js/app/pages/workspace.js` |
| 3 | `a4773ee` | 阶段P2变更集3：R2/R3 补齐 `.v-crossfade` 层序语义 + pointer-events | `web/static/css/style.css` |
| 4 | `0cab312` | 阶段P2变更集4：R4 `treemap.pause()` 真暂停 | `web/static/js/app/viz/treemap.js` |
| 5 | 见 §七（收尾提交） | 阶段P2变更集5：`u66` 结尾改跨视图序列 + 探针与证据入册 + 文档回写 + 交付报告 | `scripts/dev/u66_view_residual_real.mjs`、`scripts/dev/p02_b_screencast.mjs`、`scripts/dev/p02_c_viewport_shots.mjs`、`scripts/dev/p02_manifest.mjs`、`README.md`、`docs/问题核查复现与修改定位清单.md`、`docs/后续开发计划_用户实测问题10项_20260908.md`、`docs/问题核查资料_20260908/p2/**`、本报告 |

- tag：`p2-view-residual`（收尾打于 §七 的收尾提交）
- **生产代码改动面（三条，全部在授权清单内）**：
  - `web/static/js/app/pages/workspace.js`：`setBrowseView` 方向判定改为 `treemapInvolved = prev === "treemap" || mode === "treemap"`；`crossfadeView` 按入向/出向加 `.v-crossfade-in` / `.v-crossfade-out`；`finishViewSwap` 清理两类语义类；新增 `setTreemapPaused()` 并可见性驱动调用；`#dir-body` mouseover 联动加 `#treemap-wrap` 可见性守卫。
  - `web/static/css/style.css`：`.treemap-wrap` 与 `.view-area .table-wrap` 补终态 `z-index:1`；新增 `.v-crossfade{pointer-events:none}`、`.v-crossfade-in{z-index:3}`、`.v-crossfade-out{z-index:2}`（层序低于骨架屏 z-index:5）。
  - `web/static/js/app/viz/treemap.js`：新增 `paused`/`dirty` 状态与 `setPaused()`；`doResize()`/`highlightKey()`/`setTiles()`/指针链/主题观察器在暂停时短路（隐藏态零重绘）；`pause()` 转发 `setPaused(true)`；恢复时补画终帧。
- **零改动的相邻面**：后端生产代码（`app.py`/`cli.py`/`compare.py`/`fullscan.py`/`snapshots.py`/`session.py`/`tui.py` 等）**零改动**；`tests/**` **零改动**。

### 根因与修复对应

| 根因 | 内容 | 修复 |
|---|---|---|
| **R1（主因）** | `crossfadeView(seq, showTreemap)` 把方向判成「矩形图 vs 表格」二元，目标非矩形图时矩形图被当作退场元素 → `removeAttribute("hidden")` + `style.opacity="1"` | 方向判定改「上一视图 vs 下一视图」；非矩形图↔非矩形图直接 `finishViewSwap` |
| **R2（形态）** | 两容器同为 `position:absolute;inset:0` 且无 z-index，`#treemap-wrap` DOM 靠后 → 叠加时在上层 | 补 `z-index:1` 终态基值 + `.v-crossfade-in/out` 显式 3/2 |
| **R3（半成品）** | JS 已加 `.v-crossfade` 类，CSS 中**不存在该规则** | 补齐完整语义（层序 + `pointer-events`），与 JS 类名保持一致 |
| **R4（卫生）** | 终态隐藏推迟到两个 WAAPI `finished`；`pause()` 只 `cancelAnim()`；列表 hover→矩形图联动无可见性守卫 | `setPaused()` 真暂停（断重绘/断交互/恢复补终帧）+ `highlightKey` 与 mouseover 双守卫 |
| **R5（教训）** | 上批 8 轮「未复现」= 探针只断言 700–800ms 终态 + `u66` 结尾幂等点击 | 探针改帧级 + 计账窗显式定义；`u66` 结尾改跨视图序列（断言语义/阈值/采样节奏不变） |

## 三、测试

| # | 命令 | 实际数字 | 结论 |
|---|---|---|---|
| 1 | `.venv\Scripts\python.exe -m unittest discover -s tests -t .` | **Ran 327 tests / OK / exit 0** | 绿 |
| 2 | `.venv\Scripts\python.exe -W error::ResourceWarning -m unittest discover -s tests -t .` | **Ran 327 tests / OK / exit 0** | 绿 |
| 3 | `.venv\Scripts\python.exe -m py_compile main.py cli.py env.py sdk.py scan.py tui.py utils.py exceptions.py keyrouter.py messages.py snapshots.py compare.py app.py fullscan.py session.py datadir.py` | **exit 0**（16 文件） | 绿 |
| 4 | `node --test scripts/dev/motion-core.test.mjs scripts/dev/treemap.test.mjs scripts/dev/trend-window.test.mjs` | **tests 43 / pass 43 / fail 0** | 绿 |
| 5 | `node scripts/dev/u22_smoke_probe.mjs --base http://127.0.0.1:5000/ --out %TEMP%\p2_regress\u22`（静态服务 8771 提供 `tests/web/smoke.html`） | **`[v2] summary=21/21 通过 ✔`** | 绿（断言全过；探针自身因 favicon 404 计 1 条 console 错误，见下） |
| 6 | `node scripts/dev/u50_repro_probe.mjs --base http://127.0.0.1:5000/ --out %TEMP%\p2_regress\u50` | **5 段全完成 / exit 0**；`view.residualDetected=false`、`uniqueOk=true`、`treemapUniqueOk=true` | 绿 |
| 7 | `node scripts/dev/u56_relate_probe.mjs --base http://127.0.0.1:5000/ --out %TEMP%\p2_regress\u56` | **15/16**；唯一失败 = `console 无未处理错误`（favicon 404） | 见下「既存环境差异」 |
| 8 | `node scripts/dev/u66_view_residual_real.mjs --base http://127.0.0.1:5000/ --out %TEMP%\p2_regress\u66b --browsers chromium` | **5/5 全绿 / exit 0**（高频连点 29 次；终态唯一性；无残留；切回矩形图稳定态；console 0） | 绿 |
| 9 | `node scripts/dev/p02_view_frame_probe.mjs --base http://127.0.0.1:5000/ --out docs\…\p2\postfix --label postfix` | **7/7 全绿 / exit 0** | 绿 |

- **断言变更理由（逐条）**：
  1. `scripts/dev/u66_view_residual_real.mjs` 结尾连点序列 `"ranking","table","treemap","ranking","table","table"` → `"ranking","table","relate","ranking","table"`。**代码事实**：末两次点击原为**同一视图幂等点击**（`prev === mode`），既走不到 `crossfadeView` 分支、也无法在拍关键帧前暴露「非矩形图→非矩形图」路径（R5 断言口径盲区）。**旧断言为何不成立**：并非旧断言为假，而是它**测不到**本问题——终态断言在任何实现下都通过。**未改**任何断言语义、阈值或采样节奏（连点总数 30 → 29，仍 ≥20）。
  2. 未改动 `tests/**`。未为了让测试变绿放宽任何断言。
- **既存环境差异（非本次改动面，如实登记）**：u50/u56/u22 的 console 检查命中 1 条 `Failed to load resource: 404`。定位结论：应用未提供 `favicon.ico`（浏览器自动请求），同批 P0/P1 探针与本次新增的 P2 三个探针均显式过滤 `favicon.ico`，而 u50/u56 未过滤。**证据**：P2 探针在应用（5000）上 `console/pageerror 0`；C 类三视口探针加 favicon 过滤后亦为 0；用 `page.on("response")` 直采 `smoke.html` 无任何非 2xx 响应。故该项与 P2 改动无关，登记为既存探针口径差异（未在本次「仅允许改 u66 结尾」的授权内扩大修改面）。

## 四、视觉验收

### 4.1 红线 A · 探针有效性自证（同一支探针跑修复前/后）

| 序列 | 修复前 帧数 / 窗口内 | 修复前 **违规** | 修复前 闪烁 | 修复后 帧数 / 窗口内 | 修复后 **违规** | 修复后 闪烁 |
|---|---:|---:|---:|---:|---:|---:|
| s1_tm2rank2table | 85 / 66 | **20** | 1 | 83 / 64 | **0** | 0 |
| s2_tm2relate2rank | 83 / 64 | **17** | 1 | 82 / 64 | **0** | 0 |
| s3_rank2table2relate | 82 / 64 | **16** | 1 | 82 / 64 | **0** | 0 |
| s4_relate2table2treemap | 82 / 63 | **9** | 0 | 81 / 63 | **0** | 0 |
| s5 全序列（6 步） | 189 / 169 | **42** | 4 | 187 / 169 | **0** | 0 |
| s6 连点 30ms×12 | 90 / 62 | **24** | 0 | 90 / 62 | **0** | 0 |
| **合计** | **611 / 488** | **128** | **7** | **605 / 486** | **0** | **0** |

- 修复前判据 `PFFFFPP`（DoD1/2/3/4 失败），修复后 `PPPPPPP`（7/7 全绿）。
- 首违规形态（修复前 s1 实测）：`ts=106` 帧即 `#treemap-wrap` 无 `hidden`、`display:block`、命中 `treemap-canvas`，随后 `opacity` 按 `1 → 0.384 → 0.143 → 0.053 → 0.018 → 0.0046 → 6.0e-4 → 2.3e-6` 递减淡出（≈120ms 交叉淡化）——与用户「闪过」描述一致。
- **复现方式（可复跑）**：`git stash push` 三份生产文件 → 重启服务（已用 HTTP GET 校验实际伺服内容为前修复态）→ 跑探针 → `git stash pop` → 重启服务。服务 PID：8936 → 8268（前修复态）→ 6996（修复态）。

### 4.2 证据清单（`manifest.json` 登记）

| 证据 | 绝对路径 | 视口/节奏 | 时间窗 | 量化摘要 | 主代理判读 |
|---|---|---|---|---|---|
| A 类 修复前 | `D:\deepseek\python-disk-analyzer\docs\问题核查资料_20260908\p2\baseline_prefix\{frames,summary}.json` | rAF ≈16.7ms/帧 | 首次点击 → 末次点击+421ms/步+200ms | 611 帧 / 窗内 488 / **违规 128** / 闪烁 7 | 量化判 FAIL（预期） |
| A 类 修复后 | `D:\…\p2\postfix\{frames,summary}.json` | 同上（同一支探针） | 同上 | 605 帧 / 窗内 486 / **违规 0** / 闪烁 0 | 量化判 PASS |
| B 类 screencast | `D:\…\p2\screencast\frame_analysis.json` + `timeline.json` + `screencast-frames\` | CDP `Page.startScreencast`（JPEG q=80，每帧）+ 页内心跳；**实测 17.7ms/帧** | 采集 2200ms；触发（切回矩形图）于 `ts=382ms` | 122 原始帧 / **37 唯一帧**（丢弃 85 重复）/ 混合 10 / 列表态 4 / 矩形图态 21 | **PASS**（5 张关键帧逐张判读，见 §4.4） |
| C 类 三视口 | `D:\…\p2\viewport_shots\viewport-{1366x768,1440x900,1920x1080}.png` + `viewports.json` | `page.screenshot`（终态静态；中间态一律走 B 类） | 加载后 1000ms 收尾 | 视区 `1018×391 / 1076×519 / 1556×699`；canvas 设备像素 `1016×389 / 1074×517 / 1554×697`；`twHidden=false / opacity=1 / table display=none / tooltip hidden` | **PASS**（3 张逐张判读，见 §4.4） |
| 红线 B 数据目录 | `D:\…\p2\_datadir_baseline_prerun.json` | 跑测前全量清单 | 基线快照 | `%LOCALAPPDATA%\PythonDiskScanner`：**43 文件 / 5,361,990 B** | 见 §6.2（收尾复采比对） |

### 4.3 B 类像素分类的**阈值自证**（先证工具，再判产品）

用 PNG 参照帧自身按 screencast 同参数（JPEG q=0.8）重编码后分类，验证阈值对「JPEG 伪影 + 1×1 心跳」的可分性：

| 自证样本 | `frac_like_treemap` | `frac_like_list` | 分类结果 | 期望 | 判定 |
|---|---:|---:|---|---|---|
| 矩形图参照的 JPEG 化样本 | **0.9698** | 0.5858 | `treemapLike` | `treemapLike` | ✔ |
| 列表参照的 JPEG 化样本 | 0.5861 | **0.9678** | `listLike` | `listLike` | ✔ |

分类阈值：单像素 RGB 差和容差 30；`treemapLike ≥ 0.93`；`listLike ≥ 0.88`；混合帧「有解释像素」≥ 0.90。

**交叉淡化进度（37 个唯一帧，按时序）**：

| ts(ms) | 34 | 455 | 493 | **509** | **561** | **599** | **635** | **651** | **688** | **706** | 725 | 899 | 989 | 1114 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 与矩形图参照相似度 | 0.586 | 0.597 | 0.597 | 0.597 | 0.597 | 0.597 | 0.597 | 0.876 | 0.917 | **0.928** | 0.937 | 0.967 | **0.9705** | 0.9665 |
| 与列表参照相似度 | 0.968 | 0.956 | 0.900 | 0.864 | 0.757 | 0.705 | 0.669 | 0.654 | 0.631 | 0.623 | 0.616 | 0.588 | 0.586 | 0.586 |
| 分类 | list | list | list | blend | blend | blend | blend | blend | blend | blend | tm | tm | tm | tm |

- 混合帧的「取到矩形图参照的像素占比」单调上升：`0.0169 → 0.2012 → 0.2367 → 0.2626 → 0.2859 → 0.3045 → 0.3207 → 0.3338 → 0.3444 → 0.3526`（无回退 → 交叉淡化方向正确，无重启）。
- 触发前（ts=34/455/473/493）为 `listLike`，末期（ts≥725）为 `treemapLike`，终值 0.9705 → 与 DOM 终态（`activeView=btn-view-treemap`、`twHidden=false`）互证。

### 4.4 逐张 `read_image` 判读自证

- **自证口径**：`manifest.json` 登记图片 **N = 11**；主代理本人 `read_image` 实际打开 **M = 11**；`N == M` ✔；11 张 SHA-256 **全部唯一**（重复 0），满足「同哈希的不同帧视为无效证据」。
- **像素级判读结论（摘要，完整逐条见 `manifest.json` → `images.readback.verdicts`）**：
  - **C 类三视口（3/3 PASS）**：三张均为蓝色「矩形图」按钮为 primary 的终态，矩形图铺满视区、表格完全缺席；主导蓝色块 `pagefile.sys · 976.56 KB · 95.8%` 与右缘 `archive/data/media/docs` 条带与桩数据（4 目录 2 文件）逐项吻合；1920 宽下块标签（`19.53 KB`/`11.72 KB`/`7.81 KB`/`3.91 KB`）全部可辨。矩形图边界内无白区、无残留、无空白画布。
  - **B 类参照帧（3/3 PASS）**：`refRanking.png` 为表格终态（表头 `名称/大小/占比/类型`、行 `pagefile.sys 976.56 KB 95.8% 文件` 等、页脚 `共 6 项 已选 0 项`），**视区内无任何矩形图画布** —— 即修复后应有的参照；`refTreemap.png` 为矩形图终态；`refListSelfCheck.png` 为独立时刻的排行态（哈希 ≠ `refRanking`）。
  - **B 类关键帧（5/5 PASS）**：`493ms` 为淡蓝块叠在列表白底上（早期混合帧）；`509ms` 蓝块更饱和更大且右侧出现淡条（进度严格推进）；`635ms` 蓝块扩至 x≈22→685、条带趋饱和；`706ms` 接近全不透明、视区内无列表残留；`1114ms` 终态与 `refTreemap` 同态、含块内标签。**五帧像素观察与量化 `0.0169→0.3526→0.9665` 方向一致。**
- **主代理自纠（记录在案，避免误判入档）**：首轮判读 C 类截图时，把 `#treemap-wrap` 的**块内标签**（`paintTile` 在块左上角绘制的 `name + size + pct`）误认为「幽灵 tooltip」。经交叉核对：加强 tooltip 卫生断言前后，截图 **SHA-256 与字节数完全不变**（`1eb815ce…`/107970 B 等），证明像素未变、判断有误；探针的 `tooltipHidden=true` 断言为准。**未据此改动任何产品代码**，并已在 C 类探针中保留 tooltip 卫生断言（更严，且不需要额外修改）。

## 五、遗留与挂账

| 项 | 说明 | 下一步 |
|---|---|---|
| favicon 404 噪声 | 应用未提供 `favicon.ico`；u22/u50/u56 未过滤该请求，故 console 检查各计 1 条 404（P2 三个新探针已过滤） | 属探针口径差异，非本阶段授权面；建议后续批次统一「探针 favicon 过滤」口径或补 favicon |
| `docs/问题核查资料_20260908/p2/_datadir_baseline_prerun.json` 首版带 UTF-8 BOM | PowerShell `Set-Content -Encoding UTF8` 在 PS 5.1 默认写 BOM，导致 JSON.parse 失败 | **已就地修复**（去 BOM 重写）；记入本地环境备忘 |
| B 类 screencast 帧数与合成器损伤耦合 | `Page.startScreencast` 仅在页面产生损伤时投递帧；本脚本用 1×1 心跳维持，实测 17.7ms/帧、122 原始帧 | 已固化在 `p02_b_screencast.mjs`；后续同类动画取证沿用该手法 |
| 历史 P0 基线文件口径 | `P2-视图切换帧级证据.json` 用的是「`opacity>0` 即违规」的旧口径（含矩形图参与切换的合法出向淡化） | 已在 README D2-4 与计划文档第八章写明口径修正；旧文件按「历史基线」保留不改 |

## 六、DoD 逐条对照（计划第八章 6 条）

| # | DoD 原文 | 结论 | 实测依据（真实数字） |
|---|---|---|---|
| 1 | 全部序列动画窗口内 `#treemap-wrap` 的 `hidden` 恒真、`opacity` 恒为 0、命中测试不落回矩形图 | **达成**（含口径修正） | 6 组序列 / 计账窗内 **486 帧 / 违规 0**（修复前 488 帧 / 违规 128）。判据 = `hidden` 属性 + computed `display` + 视区中心命中三项；**合法例外**为矩形图参与切换的出向交叉淡化（`.v-crossfade-out`，须在 `--dur-1`=120ms 内收束，超 250ms 判停滞）。⚠️ 原文「`opacity` 恒为 0」与 DoD 2 互斥（出向淡化期间矩形图必然带 `opacity>0`），且 `display:none` 容器上的 opacity 残值不产生可见像素/不影响命中——以 opacity 判定会在连点序列产生 15 帧假阳性，故 P2 以「显示/命中」为准并已写入 README D2-4 |
| 2 | 矩形图↔列表切换仍保留 120ms 交叉淡化且视觉正常 | **达成** | 叠加帧 9 帧、双中间透明度帧 7 帧；入向 `z-index=3` > 出向 `z-index=2`；`.v-crossfade` 相位内两者 `pointer-events:none`、收束后恢复 `auto`（`finalZ.twPE=auto`、`tbDisplay=none`）。B 类像素：与矩形图参照相似度 `0.586→0.9705`、混合帧进度单调 `0.0169→0.3526` |
| 3 | 连点（30ms 间隔 ≥10 次）无闪烁式重现 | **达成** | 30ms×**12** 连点：窗内 62 帧 **违规 0**、**闪烁式重现事件 0**（修复前 24 违规 / 4 事件）。重现事件定义 = 窗内矩形图由隐藏→可见的跃迁，且**目标为矩形图时不计**（那正是合法入向淡化） |
| 4 | 隐藏态下 hover 列表行不触发矩形图重绘（像素签名不变） | **达成**（判据已升级为直接计数） | 6 行 hover：canvas 2D **绘制调用增量 = 0/0**（总量/隐藏期）；对照静置 1200ms 亦为 **0/0**；隐藏容器代理几何恒 `0×0 / display:none`。修复前同口径为 **70/70**（全部发生在隐藏期）。⚠️ 原「像素签名」判据已作废：`display:none` 的 canvas 位图后备存储会在指针重入文档时被浏览器丢弃/重建（实测 `nonZeroPixels` 0→38400，与页面代码无关），对修复前代码亦「失败」，属口径缺陷 |
| 5 | `browse-loading` 骨架屏仍能覆盖视区 | **达成** | `view-area 1018×391` / overlay `1016×389`（1px 边框内缩，容差 2px，`coversViewArea=true`）；`overlay z-index=5` > `treemap z-index=1`；视区中心命中为 overlay（`hitIsOverlay=true`） |
| 6 | 门禁全绿 | **达成** | 327/327 单测 OK；`-W error::ResourceWarning` 327/327 OK；`py_compile` 16 文件 exit 0；`node --test` 43/43；`u22` 21/21；`u50` exit 0 且 `residualDetected=false`；`u56` 15/16（唯一失败为环境 favicon 404）；`u66` 5/5；P2 探针 7/7 |

## 七、回滚与进程清理

### 7.1 红线 B · 用户真实数据目录零写入

- **基线（跑测前）**：`%LOCALAPPDATA%\PythonDiskScanner` = **43 文件 / 5,361,990 B**（`_datadir_baseline_prerun.json`，含每文件 `LastWriteTimeUtc`）。
- **跑测隔离方式**：应用服务以 `LOCALAPPDATA=%TEMP%\pds_p2_iso\home` + `DSA_SNAPSHOT_DIR=%TEMP%\pds_p2_iso\snapshots` 重定向启动；全部视觉探针走**桩态**（覆写 `window.fetch`，不发真实 `/api/browse`）。
- **收尾复采比对**：见本报告收尾附记（§7.4）。

### 7.2 回滚

- 回滚：`git revert 0cab312 a4773ee 1ce4ecb`（或 `git reset --hard p1-autosave`）。
- R1 单点回滚即可恢复旧行为（但会恢复缺陷，需同时回退探针断言口径）。
- 数据层不随代码回滚。回滚后最小门禁：`unittest discover` + `u22` + `u66`。

### 7.3 进程/端口清理

- 本阶段自行启动的进程：Flask 应用（127.0.0.1:5000，PID 8936/8268/6996/23236 数轮重启）、静态服务（127.0.0.1:8771，PID 18244）、探针 Chromium（`p02_*`/`u22`/`u50`/`u56`/`u66` 各自 `browser.close()`）。
- **不触碰**：Everything（PID 3340/19964）、用户自带 Edge（21 个进程）。
- 清理动作与清理前后清单：见 §7.4。

### 7.4 收尾附记（提交后回填）

- `git status` 干净（仅 `_p2_progress.md` 为未跟踪的检查点文件，按计划**不提交**）。
- tag `p2-view-residual` 已打于收尾提交。
- 数据目录收尾比对结果、进程/端口清理前后清单、`%TEMP%` 自产产物清理清单：见提交后的收尾记录。

---

*编制：2026-09-10 · 主代理本人 · 分支 `stage-p2` · tag `p2-view-residual`*
