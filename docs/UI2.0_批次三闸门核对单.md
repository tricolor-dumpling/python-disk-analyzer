# UI 2.0 · 批次三闸门核对单（定稿第二节 F01–F24 / N01–N13 全表）

> 工作项：U3.5 弹窗族与引导收尾（手册 §U3.5 批次三闸门项）
> 核对日期：2026-08-29（本机；分支 `ui2-u3.5`，基线 62e155b / U3.4）
> 依据：①各工作项执行记录（《UI2.0_开发执行手册.md》头部 U1.0–U3.4/U3.5）；
> ②验收探针：u31 45/45·u33 31/31·u34 48/48·u24 34/34+扫描态 3/3·u25 46/46·u23 29/29·u32 54/54；
> ③本工作项新增 `scripts/dev/u35_acc_probe.mjs` = **60/60**（桩态 43 + reduced 7 + 真机 10）；
> ④smoke v2 **20/20**（A19 新接入）、unittest **266 OK**、node **20/20**、hex **0**；
> ⑤本页实测（Flask 5000 真机走查 + 双档零滚动 + 视觉截图）。
> 判定口径：✅=已实现且验收通过；◐=功能等价/降级达成（字段/形态差异，零后端改动）；⚠️=缺失/未接（挂账有据，见§3）。

## 一、F01–F24（现有功能 → 新版归宿）

| ID | 项 | 归宿/实现处 | 状态 | 依据 |
|----|----|------------|------|------|
| F01 | Everything 健康徽章（三态+呼吸） | 顶栏徽章（N11 文案「已就绪·可开始扫描」/检查中/未就绪 + 呼吸 L2-8 badge-breathe 2.4s——U3.5 起 token 化 `--dur-breathe`）；点击弹出环境详情 popover（数据目录/驱动状态/重试=红线#8 第二求值点） | ✅ | U3.1；u31（popover 三字段/重试）；L2-8 参数见 u31 探针 |
| F02 | 首启 4 步使用指引 | `#onboarding` 壳级弹层（4 步纵排+进度点；pds_onboarding_dismissed_v1 沿用；入弹窗栈；「使用指引」重开） | ✅ | U3.1；u31；U3.5 核对未重做（A0 closeModal 清场纪律保持） |
| F03 | 设置弹窗（自动保存/数据目录/健康状态/危险区） | id 不变；**U3.5 新增「主题：亮/暗/跟随系统」**（与顶栏按钮同源 state.theme；单选即生效）；保持弹窗不设子页 | ✅ | U3.5（本项）：u35 ①（三态/持久化/联动/跟随系统 matchMedia 监听/刷新解析一致）+ smoke A19 ① + 真机 ⑨a–d |
| F04 | 一键清空（输入「确认清空」） | 设置→危险区→确认弹窗；**U3.5 视觉升级：红描边脉动 2.4s（wipe-pulse，--dur-breathe，仅 opacity）+ 输入匹配后 3s 倒计时解锁（--dur-wipe-countdown；倒计时禁用+显示秒数；失配重计；重开复位）** | ✅ | U3.5（本项）：u35 ② 全链（脉动 2.4s/倒计时/失配/执行 POST /api/admin/wipe/联动复位）+ 真机清空流程走查（输入→倒计时→执行→目录重建，⑨e–g） |
| F05 | 空间概览（汇总条+点击浏览） | 右栏存储概览卡（环形图+盘符 chips+「浏览此盘」）；⚠️ `/api/overview` 无总容量字段 → 环形=「已使用之环比」（零后端改动） | ✅◐ | U2.4；u24 34/34+扫描态 3/3 |
| F06 | 路径输入+浏览+返回上级 | 面包屑栏左侧路径输入（datalist）+「返回上级」；启动浏览首根；⚠️ **「启动恢复上次浏览位置」（pds_last_browse_v1）未实现**（§3.2 键表项，U2.x 未接） | ✅⚠️ | U2.1/U2.3/U3.4（workspace.js 模板/竞态/空守卫）；挂账见 §3-G1 |
| F07 | 最近访问/浏览历史 chips | 面包屑栏 `#browse-history` chips（保留）+ 命令面板数据源（getBrowseHistory 访问器，最近 8 条倒序）；⚠️ **「历史下拉（时钟图标）收纳」形态未落地**（功能等价：chips 闭环 + 面板可搜） | ✅◐⚠️ | U3.1（面板数据源）；挂账见 §3-G2 |
| F08 | 面包屑导航 | 主页内容区顶部 `#breadcrumb`（当前路径起头，各层可点回跳） | ✅ | U2.1/U3.4；u31/u21 |
| F09 | 排行/表格视图切换+紧凑密度 | 工具栏三态（矩形图/排行/表格，默认矩形图=N01 接管）+ 密度按钮；三视图 120ms 交叉淡化不滚动 | ✅ | U2.5；u25 46/46 |
| F10 | 筛选（名称/类型）与排序 | 排行/表格上方筛选行（browse-filter/kind/sort）语义沿用 | ✅ | U2.5（A4/A5/A6 断言语义保持） |
| F11 | browse-chart 构成图 | 由 Treemap 取代（D12；容器/CSS 死规则已删，U2.0 物理删除） | ✅ | U2.0（D12 落地记录） |
| F12 | 缓存命中徽标 | 筛选行「来自缓存」徽标 slide-in（L2-9 200ms） | ✅ | U2.5；u25（L2-9） |
| F13 | 错误态+重试、加载态 | 视图区空/错/载态（定稿 6.5 文案）；加载态=骨架屏 shimmer（L1-5 1.4s；spinner 仅存于按钮） | ✅ | U2.5/U3.3/U3.4；u25/u33/u34 |
| F14 | 全量扫描（进度条/chips/状态行） | 右栏扫描控制卡四态（空闲/扫描中[停止按钮红描边 L2-2+计时+chips 三态]/完成[L2-3 绿光+对勾+L2-4 粒子]/中止[toast+保存可用]）+ 顶栏微型进度环（N05） | ✅ | U3.2；u32 54/54 |
| F15 | 保存快照+完成后保存提示 | 扫描卡「保存快照」按钮 + 完成 toast + 保存提示条（立即保存/暂不保存）；快照页页头「创建快照」复用保存流程（isSaveAvailable 可用性镜像） | ✅ | U3.2/U3.3；u32/u33 |
| F16 | 撤销最近保存 | `#/snapshots` 页头「撤销最近保存」+ 通用确认弹窗流（红线 #9 confirm-modal 语义）；主页迷你卡撤销入口已随迁 | ✅ | U3.3；u33（F16 确认弹窗流） |
| F17 | 快照列表（会话分组/标签/逐盘对比/跳过原因） | `#/snapshots` 会话分组列表（面板内滚；auto·manual 标签；逐盘「对比此快照」=预填+跳转；跳过原因 tooltip=红线#7 SKIP_REASON_TEXT；三处空态=定稿 6.5） | ✅ | U3.3；u33 31/31 |
| F18 | 历史对比（基线/摘要/图表/表格） | `#/compare` 对比工作台整页（页头基线+目标只读+开始对比 → 摘要 3 卡 → 红绿发散图 L3-6 → 表格 F19 内滚）；主页仅留「最近对比」迷你卡；⚠️ 数据源口径：目标=同盘符最新快照为**展示口径**（/api/compare 当前侧=fullscan.result 缓存 or SDK 直扫，非快照文件；零后端改动） | ✅ | U3.4；u34 48/48（桩 40+reduced 4+真机 4） |
| F19 | open-path 行内操作（定位） | 列表行 hover 操作区三图标（下钻/定位/复制路径，触屏长按）+ 对比表格「定位 open-path + 复制路径 act-copy-cmp」；顺修 act-open/act-copy 连带下钻 | ✅ | U2.5+U3.4；u25/u34（⑤e 复制路径实调） |
| F20 | 导出 CSV/JSON | 命令面板「导出 CSV/JSON」（/api/export 既有 + encodeURIComponent root）；表格页脚「导出所选 CSV」（D9 前端 Blob，文件/列/BOM/转义） | ✅ | U2.5+U3.1；u25（CSV 内容抽查）+ palette 命令执行器 |
| F21 | Toast | 右上 toast 全路由可用；**U3.5·L2-6 全参数升级**：滑入 320ms spring（--dur-3/--ease-spring）+ 剩余时间线（自动消失进度 scaleX；TTL 4000/6500ms）+ 成功描边 300ms（--dur-toast-stroke）+ 错误脉动 2 次（--dur-toast-pulse 600ms，延迟至滑入结束）+ hover 暂停 + aria-live=polite；reduced：滑入 ≤120ms、装饰跳过、功能保留（时间线暂停） | ✅ | U3.5（本项）：u35 ③④⑧（全参数+reduced）+ smoke A19 ③ + 真机 console 0 |
| F22 | 页脚（数据目录/版本） | 底部常驻状态栏 32px：左=数据目录·版本；右=Everything 纯文本（不可点，唯一交互=顶栏徽章 F01）；⚠️ **「已选 N 项」未接**（预存挂账，U4.x 补） | ✅⚠️ | U1.3（状态栏壳）+预存挂账（u25 注记 7：F22 已选 N 项 U4.x 补）；挂账见 §3-G3 |
| F23 | 键盘可达（行 Enter 下钻） | 部分：Backspace 上级/Enter·空格键盘事件守卫/A4 键盘断言（U2.3/U2.5）；**全量键盘矩阵（7.4：Ctrl K/Esc 已接；treemap 方向键/路由快捷键等）属 U4.1 专项** | ◐⚠️ | U3.1（Ctrl K/Esc/Tab/R）+ U2.3（Backspace）+ 挂账 §3-G4（U4.1 前置=本闸门） |
| F24 | 通用确认弹窗 | 保留（撤销保存等场景：confirm-modal 入弹窗栈 + confirmResolver Promise 语义冻结；换肤 token 化） | ✅ | U2.0/U3.3（F16 撤销流）；u33 |

## 二、N01–N13（设计图新元素）

| ID | 新元素 | 行为定义 | 状态 | 依据 |
|----|--------|---------|------|------|
| N01 | Treemap 主视图 | 默认视图；单/双击+hover tooltip（150ms/glass/blur/边界翻转）；标签三级（≥48 全/24–48 名称/<24 无；>1500 关小标签）；双向 hover 联动 L2-5；扫描中实时生长 L3-2 | ✅ | U2.2/U2.3/U2.5（默认视图接管）；u22 24/24·u23 29/29·u25 |
| N02 | 顶栏搜索框（Ctrl K） | 240×36 按钮态+kbd「Ctrl K」+占位「搜索或跳转…」；命令面板（模糊匹配 fuzzyScore；↑↓/Enter/Esc；开合 80ms <100ms；入弹窗栈；执行器/数据源 main.js 注入零新 API） | ✅ | U3.1；u31（45/45）+ smoke A7 |
| N03 | 主题切换 | 顶栏太阳/月亮（亮↔暗显式翻转）；View Transitions 圆形扩散 450ms（L0-1；无 VT/reduced 直切）；**设置弹窗三态（亮/暗/跟随系统）**——U3.5：偏好单一来源（state.theme↔pds_theme_v1 三值；system=matchMedia 解析与 head 防闪烁脚本一致；OS 变化实时跟随；改一处另一处反映） | ✅ | U1.1（基础）+U3.1（移正）+**U3.5（三态）**；u35 ① + smoke A1/A19 ① + 真机 |
| N04 | 存储概览环形图 | 数据源 /api/overview（无总容量 → 环比降级）；中心 count-up；chips 只切环形不切目录（D15，0 次 browse）；扫描自动跟随+手选锁定（本扫描期）；「浏览此盘」唯一跳转入口 | ✅ | U2.4；u24（D15/跟随锁定）+smoke A14 |
| N05 | 扫描控制卡 | 空闲开始/扫描中停止（红描边 L2-2）+进度条流光+耗时计时+chips 三态；完成 L2-3 绿光+对勾；**子页面：顶栏按钮=微型进度环点击回主页**；**页面重开自动恢复（启动即轮询；K7 防重复提示）** | ✅ | U3.2；u32（54/54：含真机即点即停） |
| N06 | 快照迷你卡（右栏） | 最近一份（时间+auto/manual+盘符）+「管理快照」跳 #/snapshots；条目点击=与上一份对比（预填+跳 #/compare）；空态「还没有快照」+「打开快照管理」单按钮；「创建快照」=保存流程（无全量置灰+提示） | ✅ | U2.4+U3.3（空态按钮/可用性镜像）；u24/u33；smoke A17 ④ |
| N07 | 历史对比趋势卡 | #/snapshots 顶部×2：较昨日（同盘 0<Δt≤24h 最近）/较上周（(24h,7d] 最近，排除 ≤24h 去重）；目标=该盘最新；无基线「暂无对比基线」；Δ=复用 /api/compare（缓存防重发；路由往返回灌不重发）；点击=预填 trio+跳 #/compare；⚠️ 无逐次总量字段 → sparkline 降级差值卡（▲/▼+百分比；零后端改动） | ✅◐ | U3.3；u33（基线口径/0 svg）+U3.4（预填消费）；挂账 L3-5（后端补字段后启用） |
| N08 | 表格页脚（共 N 项/已选 N 项/导出） | 首列 checkbox+表头全选/半选+Shift 范围选+anchor；页脚 sticky「共 N 项 · 已选 N 项 · 定位/导出所选 CSV」；D9 Blob（BOM/转义） | ✅ | U2.5；u25（多选全套/CSV 抽查）+smoke A15 |
| N09 | 迷你条带 | 仅矩形图视图底部 48px 上级构成（browsesCache LRU16；点击跳回；盘根/无缓存隐藏） | ✅ | U2.3；u23 |
| N10 | 视图工具栏 | 三视图切换 + 合并阈值 −/+（1..200，步长 10，仅矩形图）+ 全屏（fixed 铺满+veil+FLIP 300ms+Esc 退） | ✅ | U2.3/U2.5；u23/u25 |
| N11 | 「系统就绪/可开始扫描」徽章 | =F01；文案统一：检查中…/已就绪·可开始扫描/Everything 未就绪（三态+dll 详情 popover） | ✅ | U3.1（徽章/popover）+U1.3（busy 不降级=A8 红线#10） |
| N12 | 面包屑 home 图标与折叠 | home=盘符根：**首级 crumb 可点回根（功能等价）**；⚠️ **>6 层中间折叠「…」下拉未实现**（视觉收口项） | ◐⚠️ | U2.1/U3.4（面包屑各层可回跳）；挂账 §3-G5 |
| N13 | 顶部导航标签 | 三标签（工作台/对比/快照）+L2-11 下划线（240ms 仅 transform）+非活动标签圆点（扫描完成/保存成功/对比完成三触发，点击消除；仅非活动显示）+命令面板同批页面直达 | ✅ | U2.1+U3.1；u31；smoke A3 |

## 三、挂账清单（缺失项/形态降级——本闸门不阻塞，均有去向）

| # | 事项 | 现状 | 去向 | 依据 |
|---|------|------|------|------|
| G1 | F06 启动恢复上次浏览位置（pds_last_browse_v1）未实现 | 启动恢复=首根 D:\（getLastRoots[0]）；无 lastBrowse 读写 | U4.x（收口补；与 F22 同类） | §3.2 键表；workspace.js 无 pds_last_browse_v1 |
| G2 | F07 浏览历史「下拉（时钟图标）」形态未落地 | 功能等价：面包屑栏 chips 闭环（最近 5）+ 命令面板浏览历史（最近 8） | U4.x（形态收口） | U3.1 面板数据源已接；chips 保留（workspace.js:121） |
| G3 | F22 状态栏「已选 N 项」未接 | 状态栏 32px 已就位（数据目录/版本/Everything 纯文本）；已选计数缺 | U4.x（U2.5 u25 注记 7 预存挂账；勿顺手补） | 手册 U2.5 执行记录 |
| G4 | F23 全量键盘矩阵（7.4） | 已接：Ctrl K/Esc/Tab 循环/R/Backspace/Enter·空格守卫；treemap 方向键/路由快捷键等未接 | U4.1（专项，前置=本闸门） | 手册 §U4.1 |
| G5 | N12 面包屑>6 层折叠「…」下拉未实现 | home 语义等价（首级可回根）；折叠缺失（视觉形态） | U4.x | workspace.js renderBreadcrumb |
| G6 | L3-5 sparkline 降级差值卡（沿用） | /api/snapshots 无逐次总量字段（双核对）→ 趋势卡差值卡；零后端改动 | 后端补总量字段后启用（手册 U3.3 预案） | U3.3 执行记录（字段核对结论） |
| G7 | P13 test_budget 并发保存竞态（预存） | 偶发/连续失败；复跑即绿窗口随负载变化；禁碰 snapshots.py | backlog/P13（既存） | U1.0 挂账；本轮复现见本文档 §四 |
| G8 | 本机 Everything SDK 直扫分钟级（环境性） | /api/compare 无后台缓存时直扫 >300s；真机对比完成不可达 → 真机阶段断言「发起态」；完整链桩态验证 | 环境注记（正常桌面不受影响） | U3.4 偏差注记③ |
| G9 | 本机沙箱 %LOCALAPPDATA% 写受限（环境性） | POST /api/settings 500（前端静默容忍）；本轮真机清空走查实测**成功路径**（数据目录可写 → 目录重建完成——⑨f/g 真实成功） | 环境注记 | U3.4/u35 备注；真机 **⑨f 成功**（非 500） |

## 四、本页实测摘录（U3.5 真机 + 桩态）

- smoke v2 = **20/20**（A19：主题三态语义/危险区倒计时/toast 时间线 4000ms·scaleX 1→0/成功描边 300ms/错误脉动 600ms delay 320ms/hover 暂停/aria-live）。
- u35_acc_probe = **60/60**（① 主题三态 13 项：缺失 key=system、显式 system、单选即生效+持久化、顶栏翻转同源联动、OS 实时跟随（emulateMedia 切暗/亮）、刷新后 head 三态解析一致、listener 重挂；② 危险区 11 项：wipe-pulse 2.4s、倒计时禁用+秒数、3s 解锁、失配重计、重开复位、执行链 confirm 正确+弹窗关闭+复位联动；③ toast 11 项；④ reduced 5 项；⑤ 弹窗栈 Esc 逆序 2 项；⑥ 双档零滚动 2 项+截图；⑦⑧ console 0；⑨ 真机 10 项：设置三态切换、清空走查真实成功、console 过滤后 0、双档零滚动）。
- 双档零滚动：1366×768 / 1920×1080（设置弹窗态/清空弹窗态/toast 态）body scrollHeight ≤ clientHeight+1 实测全部成立。
- 视觉目检：亮/暗设置弹窗（三态分段控件选中态高亮）、清空弹窗（红描边脉动环+倒计时（3s）禁用态）、success toast（绿描边+绿时间线）、error toast（红边+红时间线）、reduced 态（脉动截断静态）——截图入 `%TEMP%\u35_acc_shots`（11 张目检合格）。
- 真机清空流程：输入「确认清空」→ 3s 倒计时 → 解锁点击 → POST /api/admin/wipe **成功**（toast「数据目录已清空」+ 两弹窗关闭 + 数据目录重建）；console 0（过滤资源状态日志）。
- unittest **266 OK**（P13 复现一次、复跑即绿；本轮**零 py 改动**——snapshots.py/compare.py/scan.py/app.py 均未触碰）；node **20/20**；hex **0**（style.css 无新增十六进制；tokens.css +4 时长 token：--dur-breathe/--dur-wipe-countdown/--dur-toast-stroke/--dur-toast-pulse）。

## 五、结论

**批次三闸门：通过。** F01–F24 全部有归宿（22 项 ✅ / 3 项 ◐ 功能等价降级 / F06·F22·F23 部分项挂账 G1/G3/G4）；N01–N13 全部落地（12 项 ✅ / N07 ◐ 降级差值卡 / N12 ◐ 折叠形态挂账 G5）；缺失 6 项均挂账有据（G1–G6）+ 环境性 2 项（G8/G9）。批次三 → U4.1 无障碍（键盘矩阵 G4 专项）。
